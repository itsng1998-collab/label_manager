# SESSION RULES

이 파일은 세션이 바뀌어도 유지되는 상시 작업 규칙이다.
현재 저장소의 handoff, 메모, 최근 작업 관례를 합쳐 정리한다.

## 1. 적용 우선순위
- 사용자의 현재 요청을 가장 우선한다.
- 이 파일은 세션 공통 규칙으로 항상 적용한다.
- [SESSION_HANDOFF.md](SESSION_HANDOFF.md)는 현재 상태와 다음 작업만 기록한다.

## 2. 기본 작업 원칙
- 방어코드보다 원인 해결을 우선한다.
- 구현은 가능한 한 레거시 동작과 실제 코드 경로를 기준으로 맞춘다.
- 로그 기록과 로그 파싱 코드는 관측과 진단만 담당하고, 업무 판단, 상태 변경, 저장, 재시도 같은 비즈니스 로직을 직접 수행하지 않는다.
- 동일 문제를 반복 디버깅할 때는 이전 미해결 실험 코드를 그대로 재사용하지 말고, 필요하면 주석이나 제거로 구분을 명확히 한다.

## 3. 탐색과 검증
- 먼저 가장 가까운 제어 지점, owning abstraction, 인접 테스트를 확인한다.
- 넓은 탐색보다 작은 가설과 좁은 확인을 우선한다.
- 첫 실질 수정 뒤에는 바로 범위가 좁은 검증을 수행한다.
- 가능하면 해당 기능의 focused test를 우선 실행한다.
- 테스트가 없으면 해당 파일 범위의 analyzer 또는 최소 실행 검증을 수행한다.
- 로그 기반 재현이 가능하면 최신 로그부터 확인한다.

## 4. label_manager 저장소 전용 규칙
- 최신 로그는 `.tmp/log/app_YYYY-MM-DD_HH-mm-ss.log` 패턴 중 최신 파일 기준으로 본다.
- 사용자 테스트 후 제출된 로그를 분석하는 흐름을 우선한다.
- 로그에서 테스트 버전이 맞는지 버전 표기를 확인한다.
- 저울출력, 라벨시트, 품목관리 등 UI 포팅 작업은 레거시 문서와 실제 레거시 코드 둘 다 대조한다.
- SQL Server 관련 수정 전에는 git history와 인접 테스트를 확인해 현재 저장소가 맞추고 있는 대상 버전과 기존 금지 구문을 먼저 확인한다.
- 이 저장소의 SQL 작성과 수정은 SQL Server 2017 호환 기준을 기본값으로 삼고, 대상 버전 확인 없이 그보다 새로운 구문이나 함수 문법을 도입하지 않는다.

## 5. 코드 수정 규칙
- 변경 범위는 가능한 작게 유지한다.
- 원인과 무관한 리팩터링, 포맷 churn, 주변 기능 수정을 섞지 않는다.
- 기존 사용자 변경이 섞여 있으면 되돌리지 않는다.
- 특히 범위 밖 파일은 수정, stage, commit에서 제외한다.
- 현재 반복적으로 제외된 사용자 변경 파일: [lib/core/app.dart](lib/core/app.dart).

## 6. 테스트 규칙
- Dart/Flutter 테스트는 전체 모델 동등성보다 필요한 필드 계약을 우선 검증한다.
- FortuneSheet/LabelSheet 류 작업은 raw 필드 보존, selection, inline editor, popup, raster 계약을 구체적으로 잠근다.
- 큰 테스트 파일은 필요 없는 전면 포맷을 피하고 작은 변경만 유지한다.

## 7. 커밋 규칙
- 사용자가 명시적으로 멈추라고 하지 않는 한, 현재 요청 범위가 검증까지 끝났다면 같은 턴에서 관련 변경만 커밋까지 진행한다.
- 코드 수정이 들어간 턴에서는 final 응답 전에 반드시 아래 셋 중 하나를 끝낸다: 관련 변경 커밋 완료, 사용자가 커밋 보류를 지시함, 또는 커밋 불가 blocker를 final에 명시함.
- 커밋 전에는 `git status --short`로 범위 밖 변경이 섞였는지 확인한다.
- 관련 파일만 stage 하고 `git diff --cached --stat`로 커밋 범위를 다시 확인한다.
- 범위 밖 사용자 변경은 유지한 채, 현재 작업 관련 파일만 분리 커밋한다.
- 검증 없이 커밋하지 않는다. 최소한 focused test, analyzer, 또는 실행 가능한 좁은 검증 하나는 통과시킨다.

## 8. 버전 자동 갱신 규칙
- 코드, 설정, 문서 등 저장소 변경을 완료해 커밋하는 사용자 요청은 기능 커밋 전에 [pubspec.yaml](pubspec.yaml)의 `version`을 자동으로 갱신한다.
- 버전은 현재 사용하는 build suffix 없는 `MAJOR.MINOR.PATCH` 형식을 유지한다.
- PATCH 증가: 버그 수정, 국소 UI/동작 조정, 테스트·문서·설정 변경처럼 기존 호환성을 유지하는 작은 범위. 예: `1.0.0` → `1.0.1`.
- MINOR 증가: 사용자에게 보이는 새 기능, 여러 production 모듈에 걸친 새 흐름, 기존 데이터와 호환되는 큰 기능 확장. MINOR를 1 올리고 PATCH는 0으로 초기화한다. 예: `1.0.1` → `1.1.0`.
- MAJOR 증가: 저장 포맷, 외부 API, 실행 계약의 비호환 변경처럼 기존 사용자나 데이터가 별도 전환 없이 호환되지 않는 변경. MAJOR를 1 올리고 MINOR/PATCH는 0으로 초기화한다.
- 파일 개수는 판단 근거 중 하나일 뿐이며, 최종 단계는 사용자 영향과 호환성, 변경된 실행 경로의 범위로 결정한다.
- 한 사용자 요청에 여러 수정이 포함되면 가장 높은 변경 단계로 요청 전체에서 한 번만 증가시킨다.
- 기능 커밋 해시를 [SESSION_HANDOFF.md](SESSION_HANDOFF.md)에 기록하는 후속 문서 전용 커밋, formatter가 만든 동작 없는 정리, 생성 산출물 갱신은 같은 요청에서 버전을 다시 증가시키지 않는다.
- 분석·질문 답변만 수행해 저장소 파일을 변경하지 않으면 버전을 증가시키지 않는다.
- 버전 변경 파일은 해당 기능/수정 커밋에 함께 포함하고, handoff에 이전 버전, 새 버전, 판단 단계와 근거를 기록한다.

## 9. 문서 운영 규칙
- 상시 규칙은 이 파일에 유지한다.
- [SESSION_HANDOFF.md](SESSION_HANDOFF.md)에는 아래만 남긴다.
- 현재 작업 상태
- 최근 완료 항목 요약
- 남은 이슈와 다음 액션
- 범위 밖 변경 또는 주의사항
- 작업 중에는 항상 [SESSION_HANDOFF.md](SESSION_HANDOFF.md)를 실시간으로 갱신한다. 세션 종료 직전 정리만 기다리지 않는다.
- 구현 완료, 검증 결과, 남은 리스크, 다음 시작 지점이 바뀌면 바로 handoff에 반영한다.
- OOM, IDE 종료, 프로세스 강제 중단 같은 비정상 종료가 발생해도 다음 세션이 이어질 수 있도록 상태 변경이 생길 때마다 즉시 handoff에 반영한다.
- handoff에 현재 작업을 이어가는 데 필요 없는 오래된 완료 로그, 종료된 이슈, 지난 임시 메모가 누적되면 즉시 정리하거나 제거한다.
- handoff에는 항상 다음 세션 시작에 필요한 최신 상태만 남기고, 장기 이력은 git history와 커밋 메시지로 넘긴다.
- 과거 상세 완료 로그는 필요하면 git history와 관련 커밋 메시지로 추적한다.

## 10. 세션 종료 전 체크리스트
- 요청 범위 구현 완료
- 관련 focused 검증 완료
- 변경 파일 diagnostics 확인
- 수정 범위에 맞는 `pubspec.yaml` 버전 자동 갱신 확인
- 관련 파일만 분리 stage/commit 완료
- handoff가 작업 도중부터 현재 상태 기준으로 계속 갱신되었고, 종료 시점에도 최신 상태임