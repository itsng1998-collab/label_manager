# 레거시 애플리케이션 메뉴 및 기능 포팅 작업지시서

## 0. 문서 목적

- 이 문서는 레거시 LabelManager 네이티브 창의 `파일`, `검색`, `설정` 메뉴를 현 Flutter 프로젝트의 상단 AppBar에 그룹별 아이콘 메뉴로 포팅하기 위한 구현 계약이다.
- 인접한 `도움말` 메뉴는 이번 AppBar 포팅 범위에서 제외하며, 확인된 항목은 후속 범위 참고용으로만 기록한다.
- 현재 동작하는 기능은 기존 owning 화면과 command를 재사용해 연결한다.
- 현 프로젝트에 없는 기능은 레거시 화면, model, DAO와 실제 DB 스키마를 확인한 뒤 공용 차단형 dialog, model, DAO를 구현한다.
- 메뉴 항목만 먼저 만들고 눌렀을 때 `준비 중`, 빈 화면, 임의 안내만 표시하는 상태를 완료로 간주하지 않는다.
- 한 기능을 완료할 때는 메뉴 노출, 권한, 실제 command, 데이터 조회/저장, 오류 전달, 회귀 테스트까지 함께 끝낸다.

## 1. 최우선 구현 원칙

### 1.1 범위와 원인 중심 구현

- 레거시 동작과 현재 프로젝트의 실제 코드 경로를 기준으로 구현한다.
- 과도한 보완, 추측성 fallback, 자동 재시도, 임의 데이터 보정, 레거시에 없는 업무 규칙을 추가하지 않는다.
- 단순 null 방어를 여러 계층에 중복 추가하지 말고, 상태와 command를 소유한 계층에서 원인을 해결한다.
- 메뉴 UI에 업무 로직이나 SQL을 넣지 않는다. 메뉴는 권한/상태 표시와 command 전달만 담당한다.
- 로그 함수에 권한 판단, 저장, 상태 변경, 재시도 같은 비즈니스 로직을 넣지 않는다.
- 이미 탭이나 화면에 있는 기능을 AppBar용으로 다시 구현하지 않는다. owning controller 또는 명시적 command API를 통해 같은 기능을 호출한다.
- 이 포팅으로 추가하는 관리 목록과 조회 결과의 반복 행 테이블은 공용 `FortuneTable<T>`와 `FortuneTableColumn<T>`을 사용한다.
- AppBar 메뉴에서 새로 여는 관리·편집·조회 UI는 `BlockingModelessDialog`와 `BlockingModelessDialogFrame`을 사용한다. 별도 route page, 신규 메인 탭 또는 네이티브 창으로 만들지 않는다.

### 1.2 데이터베이스 절대 제약

- DB schema migration을 구현하거나 실행하지 않는다.
- 테이블 생성, 컬럼 추가/삭제/변경, index 생성/변경, compatibility level 변경을 금지한다.
- 레거시 코드 이름만 보고 테이블명이나 컬럼명을 추정해 SQL을 작성하지 않는다.
- 각 기능 구현 전 다음 순서로 기존 스키마를 확인한다.
  1. 레거시 DAO/model의 SELECT/INSERT/UPDATE/DELETE SQL 확인.
  2. 현 프로젝트의 model/DAO와 doc SQL 확인.
  3. 필요하면 읽기 전용 metadata 조회로 운영 스키마의 기존 테이블/컬럼 확인.
  4. 필요한 기존 스키마가 없으면 기능을 임의 스키마로 대체하지 말고 blocker로 기록한다.
- SQL Server 엔진은 2017이지만 DB `compatibility_level=100`을 SQL 문법 하한선으로 사용한다.
- `OPENJSON`, `JSON_VALUE`, `TRY_CONVERT`, `STRING_SPLIT`과 검증되지 않은 신문법을 사용하지 않는다.
- 구조화된 다중 행 payload가 필요하면 escaped XML과 `CONVERT(XML, ...)`, `.nodes()`, `.value()`, `.query()`, `.exist()`를 사용한다.
- SQL parameter 처리 시 `@@ROWCOUNT`, `@@TRANCOUNT` 같은 SQL Server system variable을 placeholder로 변환하지 않는다.

### 1.3 저장과 transaction 제약

- 읽기 전용 조회에는 불필요한 transaction을 추가하지 않는다.
- 서로 함께 성공해야 하는 다중 DML만 하나의 transaction batch로 묶는다.
- transaction은 다음 범위까지만 책임진다.
  - DML 실행.
  - commit 시도.
  - DML 또는 commit 오류를 원인 보존 상태로 호출자에게 전달.
  - 오류 시 `@@TRANCOUNT > 0`이면 가능한 rollback 수행.
- 여러 statement를 묶는 기본 경로는 기존 `DbClient.transaction(List<DbTransactionStatement>)`을 사용한다. DAO는 DML statement와 parameter, 결과 decoding만 소유하고 공용 driver가 담당하는 `BEGIN TRANSACTION`, `COMMIT TRANSACTION`, 가능한 rollback을 중복 작성하지 않는다.
- commit 오류를 성공으로 바꾸거나, 일부 성공을 정상 처리하거나, 자동 재시도하지 않는다.
- rollback 자체가 실패해도 원래 오류를 숨기지 않는다. 가능한 범위의 rollback을 시도한 뒤 원래 오류를 전달한다.
- 하나의 server-side SQL batch 안에서 rowcount 검증과 여러 DML을 함께 처리해야 할 때만 저장소의 기존 검증된 아래 pattern을 사용한다. 이 batch를 다시 `DbClient.transaction`의 중첩 transaction으로 감싸지 않는다.

```sql
SET XACT_ABORT ON;
SET NOCOUNT ON;
BEGIN TRY
  BEGIN TRANSACTION;

  -- 기존 스키마에 대한 필요한 DML만 수행

  COMMIT TRANSACTION;
  SELECT CAST(1 AS INT) AS RESULT;
END TRY
BEGIN CATCH
  IF @@TRANCOUNT > 0
    ROLLBACK TRANSACTION;
  THROW;
END CATCH
```

- 선택한 본체 한 건 수정·삭제처럼 개별 command 계약에서 정확히 1건을 요구한 DML만 driver 영향 행 또는 같은 server-side batch의 `@@ROWCOUNT`로 검증한다. 선택적 연관 행 삭제·상태 갱신은 레거시가 행 존재를 보장하지 않으면 0건을 허용하며, 기존 저장 프로시저에 확인되지 않은 rowcount·반환값 계약을 추가하지 않는다.
- `SQL_NO_DATA(100)` rowcount noise가 가능한 batch는 `SET NOCOUNT ON`과 최종 명시적 `SELECT`를 사용한다.
- commit 전 DML/validation 단계의 확정 실패는 UI draft, 변경 전 snapshot, 임시 선택 상태를 유지하고 같은 저장을 다시 시도할 수 있게 한다.
- `DbCommitOutcomeUnknown`은 저장 실패나 성공으로 단정하지 않는다. 오류를 한 번 전달하고 같은 draft의 저장을 다시 실행하지 못하게 한 뒤 dialog를 닫는다. 사용자가 다시 열어 최신 데이터를 조회하게 하며 별도 읽기 전용 mode나 수동 재조회 command를 추가하지 않는다.
- commit 성공 후 목록/cache reload만 실패하면 저장된 결과를 UI의 committed 상태로 반영해 dirty를 해제하고 같은 draft의 재저장을 막는다. `저장은 완료됐지만 화면 갱신에 실패했습니다.`를 한 번 안내한 뒤 dialog를 닫고, 다시 열 때 최신 데이터를 조회한다. rollback, 자동 retry, 별도 수동 재조회 command를 추가하지 않는다.

### 1.4 model과 DAO 작성 기준

- 현 model/DAO에 같은 책임의 API가 있으면 재사용하거나 최소 확장한다.
- 기능 경계가 다르거나 기존 API를 억지로 확장해야 하면 해당 기능 전용 model/DAO를 작성한다.
- 하나의 거대한 `AppMenuDAO` 또는 모든 관리 기능을 처리하는 generic DAO를 만들지 않는다.
- model은 DB row의 의미와 UI draft를 분리한다.
- DAO는 SQL과 row mapping을 소유하고, dialog는 SQL 문자열을 소유하지 않는다.
- 삭제/순서 변경/복사처럼 여러 row를 저장하는 기능은 payload codec과 DAO transaction을 분리해 테스트한다.
- schema가 확인되지 않은 기능은 model/DAO 이름과 책임까지만 계획하고 SQL을 확정하지 않는다.

## 2. 레거시 기준 파일

### 2.1 메뉴와 command

- 메뉴 리소스: `.tmp/LabelManager/LabelManager/LabelManager.rc`의 `IDR_MAINFRAME`.
- command ID: `.tmp/LabelManager/LabelManager/resource.h`.
- command message map과 handler: `.tmp/LabelManager/LabelManager/MainFrm.cpp`, `MainFrm.h`.
- 로그인 사용자 권한: `.tmp/LabelManager/LabelManagerLib/User.h`, `User.cpp`.
- 메인 화면 observer/화면 이동: `.tmp/LabelManager/LabelManager/MainWndEvent*.h/.cpp`, `MainWindowDlg.cpp`, `MainWindowModel.cpp`.

### 2.2 현 프로젝트 기준

- AppBar, 서버 상태, 로그인/로그아웃, 종료 흐름: `lib/home_page.dart`.
- 서버 상태 아이콘: `lib/database/db_connection_status_icon.dart`.
- 로그인 사용자와 등급: `lib/models/user.dart`.
- 메인 탭, 편집 상태, 프린터/저울 설정 command: `lib/home_page_manager.dart`.
- 품목관리: `lib/page_home/item_manage.dart`.
- 라벨출력: `lib/page_home/label_print_page.dart`.
- 저울출력: `lib/page_home/scale_output_page.dart`.
- 자동품목갱신: `lib/page_home/automatic_item_update_page.dart`.
- 로그인 이력 prototype: `lib/page_login/login_history_page.dart`.
- 로그인 로그 DAO: `lib/models/login_log.dart`.
- 업데이트 공지 조회: `lib/models/notice.dart`.
- 발행 이력/통계 저장 schema와 codec: `lib/printing/label_print_persistence.dart`.
- DB 실행 API: `lib/database/db_client.dart`.

## 3. 목표 UI

### 3.1 AppBar 배치

넓은 데스크톱에서는 다음 순서를 사용한다.

```text
[애플리케이션명 + 버전]  [파일/관리] [조회/이력] [설정]  ...  [서버 상태] [로그인/로그아웃]
```

- 각 그룹은 텍스트가 들어간 둥근 버튼이 아니라 익숙한 아이콘 버튼을 사용한다.
- 모든 그룹 아이콘에 명확한 tooltip을 제공한다.
- 권장 아이콘:
  - 파일/관리: `Icons.menu`
  - 조회/이력: `Icons.manage_search`
  - 설정: `Icons.settings_outlined`
  - 좁은 화면 통합 메뉴: `Icons.more_vert`
- `파일/관리`는 비로그인·저권한 상태에서 종료만 남을 수 있으므로 관리자 전용으로 오인되는 아이콘을 사용하지 않는다.
- 팝업 내부에는 전체 한글 명령명을 기본으로 표시한다. command 아이콘은 의미를 즉시 구분하는 데 실제로 도움이 되는 경우에만 선택적으로 사용하고, 모든 항목에 일괄 적용하지 않는다.
- popup 표시명은 현재 앱의 command button처럼 간결한 한글 기본 `label`을 그대로 사용한다. dialog를 연다는 이유만으로 `...`를 붙이거나 별도 metadata를 추가하지 않는다.
- 실제 활성 전역 shortcut이 해당 AppBar command를 실행할 때만 명령명 문자열에 합치지 않고 popup 오른쪽의 고정 보조 영역에 `F12` 같은 `shortcutLabel`을 표시한다. `검색출력모드`의 `F12`에 적용하고 semantics에는 기본 명령명과 shortcut을 함께 제공한다. 레거시 `F1`은 `OnEditItemInfo`가 아니라 기존 `품목관리` 탭 이동이므로 `품목별 정보 편집` command에 표시하거나 연결하지 않는다.
- separator는 레거시 메뉴의 command group 경계를 유지한다.
- 위험 command의 확인은 팝업 메뉴가 아니라 해당 화면의 저장/삭제 command 시점에 수행한다.

### 3.2 반응형 기준

- 화면 전체 폭만 하드코딩해 판단하지 말고 AppBar title과 actions에 실제 제공된 폭을 `LayoutBuilder`로 판단한다.
- 그룹 3개, 서버 상태, 로그인/로그아웃이 겹치지 않는 폭에서는 그룹 아이콘을 각각 표시한다.
- 폭이 부족하면 그룹 버튼을 먼저 `more_vert` 하나로 합친다.
- 통합 overflow의 첫 단계에는 현재 표시할 command가 있는 `파일/관리`, `조회/이력`, `설정` 그룹만 표시하고, 그룹을 선택하면 해당 command의 2단계 submenu 또는 panel을 연다. 전체 command를 하나의 긴 평면 목록으로 합치지 않는다.
- 그룹 submenu/panel은 화면의 사용 가능 높이로 clamp하고 필요한 경우 세로 scroll을 제공한다. 키보드 focus 항목이 viewport 밖으로 이동하면 자동으로 reveal한다.
- 서버 상태와 로그인/로그아웃은 항상 오른쪽 끝에 남긴다.
- 메뉴를 합친 뒤에도 폭이 부족하면 title은 `maxLines: 1`, `softWrap: false`, `TextOverflow.ellipsis`로 줄인다. 서버 상태와 현재 로그인/로그아웃 command를 title보다 먼저 숨기거나 줄이지 않는다.
- 좁은 작업 영역에서는 AppBar collapse와 title ellipsis로 주요 command에 접근할 수 있어야 하며 서버 상태와 로그인/로그아웃 command를 숨기지 않는다. native minimum이나 window lifecycle 변경은 이 메뉴 포팅 범위에 포함하지 않는다.
- 버튼 출현/소멸로 AppBar 높이나 body 위치가 바뀌지 않게 한다.
- Tab으로 그룹 아이콘에 focus하고 Enter 또는 Space로 메뉴를 연다. 메뉴 안에서는 위/아래 방향키로 항목을 이동하고 Enter로 실행한다. 통합 overflow에서는 오른쪽/Enter로 그룹에 진입하고 왼쪽/Esc로 한 단계 돌아가며, 첫 단계의 Esc는 메뉴를 닫는다.
- Esc 또는 바깥 클릭으로 메뉴만 닫으면 원래 그룹 아이콘에 focus를 복원한다. command가 top-level dialog를 열면 그룹 focus 복원을 보류하고 dialog의 초기 control로 focus를 넘긴다.
- AppBar menu, modal dialog, `BlockingModelessDialog`, `DbReconnectOverlay`처럼 배경 입력을 막는 surface가 하나라도 보이는 동안 Home의 전역 F1/F2/F3/F5/F12 command를 실행하지 않는다. 여러 차단 surface가 겹치면 마지막 surface가 닫힐 때까지 차단을 유지한다.
- `PreviewFloatingWindow`처럼 배경 작업을 허용하는 non-modal preview는 단축키 차단 surface로 간주하지 않는다. 차단 상태의 구현은 현재 route/overlay 구조에 맞는 가장 작은 방식으로 하며 특정 observer, identity set 또는 lease 자료구조를 필수로 하지 않는다.
- command를 선택하면 AppBar popup을 닫은 뒤 owning command를 정확히 한 번 실행한다. dialog가 열리면 첫 입력으로 focus를 넘기고, 메뉴만 취소하거나 command가 실행되지 않으면 가능한 경우 원래 menu trigger로 focus를 돌린다.
- 넓은 폭과 좁은 대표 폭, 긴 버전 문자열에서 AppBar와 핵심 command 접근성을 focused widget test로 확인한다.

### 3.3 권한 표시 방식

- 레거시 MainFrm의 비활성 표시를 그대로 복제하지 않고 현재 Flutter 앱의 권한 UX를 따른다.
- 현재 로그인 상태나 사용자 등급 policy에서 표시 대상이 아닌 command는 `visible=false`로 메뉴에서 숨긴다. 로그인/로그아웃 또는 사용자 변경 시 policy를 다시 계산한다.
- dirty, busy, 필수 선택 누락처럼 현재 상태를 해결하면 실행할 수 있는 command만 표시한 채 비활성화한다.
- 비활성 popup item은 명령명 아래 또는 오른쪽에 짧은 `disabledReason`을 직접 표시한다. hover에 의존하는 tooltip을 유일한 안내로 사용하지 않는다.
- `disabledReason`은 현재 품목관리 popup처럼 읽을 수 있는 길이로 직접 표시하며 필요한 경우 두 줄까지 허용한다. 실제 공간 때문에 문구를 줄일 때만 tooltip으로 전체 내용을 보조하고, 기본 text semantics로 정보가 전달되지 않는 custom renderer에만 별도 semantics label을 추가한다.
- `legacyInactive`와 `legacyUnreachable` command는 inventory와 숨김 policy만 유지한다. `legacyInactive`는 레거시가 명시적으로 비활성화한 command, `legacyUnreachable`은 메뉴 리소스에서 활성 handler까지 도달하는 경로가 확인되지 않은 command에 사용한다. 별도 활성화 요구나 실제 도달 경로가 확인될 때 기능 범위와 노출 policy를 함께 검토한다.
- 그룹 안에 표시할 command가 하나도 없으면 빈 그룹 아이콘도 숨긴다.
- 로그인/로그아웃 command는 inventory와 controller에 유지하되 `파일/관리` popup에는 렌더링하지 않는다. 현재 상태에 맞는 AppBar 오른쪽 고정 아이콘 하나만 표시한다.
- 종료 command는 AppBar 고정 아이콘으로 중복하지 않고 `파일/관리` popup에 유지한다.
- 비로그인 상태에서는 서버 상태, 로그인과 종료만 사용할 수 있다.
- 일반 사용자 UI의 기존 레거시 정책은 유지한다.
  - 공용라벨관리/자동품목갱신 탭은 숨김.
  - 품목관리 편집 command는 숨김.
  - 조회 가능한 command는 유지.
- `tester01` 테스트 override는 `lib/models/user.dart`의 `User.clientUserTestOverrideId`를 사용하며 메뉴 자체에 별도 예외를 만들지 않는다.

## 4. command 구조

### 4.1 신규 공용 타입

다음 책임을 가진 작은 타입을 추가한다. 파일명은 저장소 구조에 맞춰 조정할 수 있으나 책임은 섞지 않는다.

- `lib/models/app_menu_command.dart`
  - `AppMenuGroup`: file, search, settings.
  - `AppMenuCommandId`: Flutter에서 사용자가 실행하는 owning action별 안정 ID.
  - runtime command 값에는 포팅 추적만을 위한 legacy command ID를 넣지 않는다. 레거시 handler와 ID 대응 관계는 inventory test fixture에 보존한다.
  - command metadata는 기본 `label`, optional icon, optional `shortcutLabel`을 포함한다. shortcut 보조 영역은 이 metadata로 렌더링하며 command별 widget 문자열로 중복하지 않는다.
  - `AppMenuCommandState`: visible, enabled, disabledReason.
  - DB나 BuildContext를 참조하지 않는 순수 값 타입.
- `lib/core/app_menu_policy.dart`
  - 로그인 상태, `UserGrade`, 현재 작업 상태로 command 상태를 계산하는 순수 policy.
  - command 실행은 하지 않는다.
- 전역 단축키 차단 상태
  - 기존 controller 또는 작은 app-scope helper가 차단 surface의 활성 여부를 제공하고 Home의 전역 key handler가 command 분기 전에 확인함.
  - non-modal floating preview는 차단 상태에 포함하지 않음.
- `lib/widgets/app_menu_bar.dart`
  - 넓은/좁은 레이아웃과 popup 표시만 담당.
  - command 상태를 받아 렌더링하고 선택된 command ID를 콜백으로 전달.
- `lib/core/app_menu_controller.dart` 또는 동등한 controller
  - `HomePage` AppBar와 `HomePageManager` command 실행을 연결.
  - controller attach/detach는 기존 controller pattern을 따른다.
  - blocking surface 상태를 전역 handler에 제공한다.
  - AppBar popup을 닫은 뒤 owning command를 한 번 실행한다.
  - 화면별 private method를 복제하지 않는다.

### 4.2 상태 소유권

- `HomePage`가 소유할 command:
  - 로그인, 로그아웃, 종료.
  - 후속 도움말 구현 시 애플리케이션 정보와 외부 링크 실행.
- `HomePageManager`가 소유할 command:
  - 탭 이동.
  - 품목/공용라벨/자동갱신 관련 기능.
  - 라벨출력/저울출력 프린터 설정.
  - 저울 연결 설정.
  - 업무 DB를 사용하는 관리/조회 dialog의 실행과 refresh callback.
  - 기존 메인 탭의 로그아웃/종료 상태 제공. 현재 출력·연결·저장 `writeBusy`, 활성 셀/행 편집 `activeEditing`, 품목·자동갱신·공용라벨 `dirty`를 소유 상태에서 계산하고, dirty 작업의 짧은 표시명과 폐기 callback을 확인창 없이 반환하는 exit snapshot API로 노출한다.
- 신규 AppBar dialog를 여는 owning controller는 해당 dialog의 active와 close 상태, 실제로 존재하는 dirty, write-busy, active editing 상태만 소유한다. dirty이면 짧은 표시명과 폐기 callback을 exit snapshot으로 제공한다. 읽기 전용 dialog에 dirty나 write-busy 상태를 만들지 않는다. 같은 dialog의 open 요청이 처리 중이거나 이미 열려 있으면 중복 entry를 만들지 않는다. 서로 다른 dialog의 실행을 위한 전역 queue나 coordinator는 추가하지 않는다.
- 로그아웃·메뉴 종료·OS 종료의 공용 lifecycle guard는 요청 시점의 활성 dialog owner와 `HomePageManager` exit snapshot만 수집한다. 하나라도 write-busy 또는 active editing이면 dirty 확인창 없이 종료를 차단하고, 그 외에는 모든 dirty 표시명을 한 확인창에 합산한다. 사용자가 폐기를 승인한 경우에만 각 owner의 폐기 callback을 정확히 한 번 실행한다.
- 위 수집은 종료 요청에만 사용하는 작은 `LifecycleManager` 계약이며 dialog open/close, focus, command 실행을 조정하는 범용 coordinator로 확장하지 않는다.
- 기존 `showDialog`/`showGeneralDialog` owning command는 AppBar와 원래 화면의 호출 경로를 그대로 공유한다. 동일 dialog를 coordinator 소유 overlay로 바꾸거나 `BlockingModelessDialog`로 다시 감싸지 않는다.
- 각 dialog가 소유할 command:
  - 자기 화면의 편집 draft, 저장, 취소, 행 선택.
- AppBar는 `HomePageManager` private state를 직접 읽지 않는다. controller가 노출하는 command state snapshot 또는 `ValueListenable`을 사용한다.

### 4.3 편집 중 차단

- 메뉴 선택이 brand, label size, 탭, draft context를 바꾸는 경우 기존 `_block...ContextChange` 경로를 재사용한다.
- 읽기 전용 이력 조회까지 일괄 차단하지 않는다.
- 저장 command busy 중 DB를 다시 갱신하거나 같은 데이터를 수정하는 command만 비활성화한다.
- 로그인 상태/등급/`legacyInactive`/`legacyUnreachable`처럼 현재 policy에서 표시 대상이 아닌 command는 `visible=false`로 처리하고 disabled reason을 만들지 않는다.
- 편집 중, command busy, 필수 context 누락은 서로 다른 disabled reason으로 계산한다.
- disabled reason은 테스트 가능한 순수 policy 결과로 유지하고 popup item 안의 보조 문구로 직접 안내한다. tooltip은 전체 문구 보조에만 사용하고 별도 경고 dialog를 띄우지 않는다.

## 5. 레거시 메뉴 inventory와 포팅 계약

## 5.1 파일/관리 그룹

### 5.1.1 로그인 (`IDM_LOGIN`)

- 레거시: 로그아웃 상태에서만 활성. Flutter에서는 AppBar 오른쪽 고정 로그인 아이콘으로만 표시하고 `파일/관리` popup에는 표시하지 않는다.
- 현행 연결: `HomePage._showStartupDialog()` 또는 DB 연결이 없을 때 `_loginToServerDB()`.
- 새 로그인 화면을 만들지 않는다.
- command inventory와 AppBar 로그인 아이콘은 같은 method를 참조한다.
- 사용자 확정과 활성 레거시 `CLoginDlg::CheckLogin`을 따라 로그인 방식을 구분한다. `SYSTEM` 계정의 직접 비밀번호·시스템 비밀번호와 시스템 관리자 계정의 자기 비밀번호 로그인은 `IsFirstConnectByAdmin`에 대응하는 상태를 설정하되 master-key로 취급하지 않는다. 비시스템 사용자가 자기 비밀번호 대신 레거시 직접 비밀번호로 로그인하면 별도 master-key 로그인 상태를 설정한다. 일반 사용자에게 시스템 비밀번호까지 허용하거나 두 상태를 하나의 관리자 연결 flag로 합치지 않는다.
- 사용자·지점·거래처·협력업체 context가 확정된 뒤 master-key가 아닌 로그인만 `BM_LOGIN_LOG`에 `LOGIN`을 한 번 기록한다. master-key 로그인은 `LOGIN`을 기록하지 않는다. 이력 저장 Future를 처리하지 않은 채 dialog를 닫지 않으며, 저장 오류는 한 번 전달하되 인증 성공과 main view 초기화를 실패로 바꾸거나 자동 재시도하지 않는다.
- 검증:
  - 비로그인 상태에는 AppBar 고정 로그인 아이콘만 표시하고 `파일/관리` popup에는 로그인/로그아웃 command를 렌더링하지 않음.
  - DB 연결 유무에 따른 기존 분기 유지.
  - 일반·`IsFirstConnectByAdmin` 로그인은 `LOGIN` 1회, master-key 로그인은 0회이며 이력 저장 실패 후에도 성공한 로그인 상태와 main view 초기화를 유지함.

### 5.1.2 로그아웃 (`IDM_LOGOUT`)

- 레거시: 로그인 상태에서만 활성. Flutter에서는 AppBar 오른쪽 고정 로그아웃 아이콘으로만 표시하고 `파일/관리` popup에는 표시하지 않는다.
- 현행 연결: `HomePage._onLogout(false)`.
- clean 상태에는 일반 로그아웃 확인창을 새로 추가하지 않고 바로 기존 로그아웃을 진행한다. 확인은 저장하지 않은 작업을 폐기할 때만 표시한다.
- `_doLogout()` 전에 공용 lifecycle guard로 활성 신규 dialog와 `HomePageManager`의 exit snapshot을 함께 확인한다. 라벨 발행, 저울 발행·연결, 품목·자동갱신 저장 같은 `writeBusy`는 로그아웃을 거부하고 해당 owner의 차단 사유를 한 번 안내한다. 품목·자동갱신·저울출력 또는 신규 dialog의 active editing은 자동 확정하거나 폐기하지 않고 먼저 편집 완료 또는 취소를 요구한다.
- 신규 dialog와 품목·자동갱신·공용라벨 중 하나 이상이 dirty면 영향받는 작업명을 한 번의 폐기 확인에 합산한다. 승인 시 각 owner의 폐기 callback을 한 번씩 실행하고, 화면별 확인창을 연속으로 띄우거나 일부 상태만 확인하고 나머지를 우회하지 않는다.
- lifecycle guard를 통과한 명시적 로그아웃은 현재 로그인 상태이고 master-key가 아니며 원래 관리자 `ConnectOrigin`이 없을 때만 현재 사용자·거래처 값으로 `BM_LOGIN_LOG`에 `LOGOUT`을 한 번 기록한다. 관리자 `Connect` 중인 명시적 로그아웃과 master-key 로그아웃은 기록하지 않는다. 이력 기록을 시도한 뒤 사용자·지점·거래처·협력업체 context, master-key, `IsFirstConnectByAdmin`, 두 관리자 연결 flag와 원래 관리자를 초기화하고 main view를 숨기고 reset한다.
- `LOGOUT` 저장 오류는 한 번 전달하되 로그아웃과 session·화면 초기화를 중단하거나 자동 재시도하지 않는다. 이력에 필요한 값을 session 초기화 뒤에 다시 읽거나 관리자 접속 이력을 대신 기록하지 않는다.
- 검증: 로그인 상태에는 AppBar 고정 로그아웃 아이콘만 표시한다. clean 로그아웃은 추가 확인 없이 진행되고, busy/activeEditing은 차단되며, dirty일 때만 폐기 확인한다. 일반 명시적 로그아웃은 `LOGOUT` 1회, master-key·관리자 `Connect` 중 명시적 로그아웃은 0회이고 이력 성공·실패 뒤 session과 main view 초기화가 각각 한 번 실행되는지 확인한다.

### 5.1.3 협력업체 관리 (`IDM_COOP_MANAGE`)

- 레거시 기준: `CMainFrame::OnCoopManage` → `CCooperatorManagerDlg`.
- 권한: 6.1의 `systemAdminCommand`.
- 구현 지시:
  - 기존 `Cooperator` model/조회 API를 먼저 확인한다.
  - 목록, 추가, 협력업체 ID·이름 수정, 삭제를 제공하는 `BlockingModelessDialog` 기반 관리 dialog를 구현한다.
  - 추가·수정은 별도 입력 dialog의 적용/취소 단위로 수행하고, 적용 성공 시 즉시 저장한 뒤 목록을 갱신한다. 목록 전체의 전역 저장/취소 footer는 두지 않는다.
  - 신규 CRUD가 필요하면 `CooperatorDAO`를 최소 확장한다.
- 추가 command는 항상 사용할 수 있고 삭제 command는 선택이 없어도 실행할 수 있으나, 미선택이면 DML 없이 `삭제할 행을 먼저 선택해주세요!!`에 대응하는 짧은 안내를 한 번 표시한다. 별도 수정 button·context command를 추가하지 않고 유효 행 더블클릭만 수정 진입점으로 사용한다.
- 목록의 유효 행을 더블클릭하면 해당 행을 선택한 뒤 수정 입력 dialog를 열고 header·빈 영역 더블클릭은 무시한다.
- 레거시 `CCoopAddModifyDlg`는 협력업체 ID·이름의 빈 값과 중복을 검사하지 않으므로 신규 필수값·중복 검증을 추가하지 않는다. 기존 DB 제약으로 거부되면 해당 오류를 그대로 전달한다.
- 삭제는 레거시처럼 시스템 비밀번호 확인 후 종속 거래처 삭제 경고를 한 번 표시하고 실행한다. 시스템 비밀번호는 현재 `startup_dialog.dart`의 `_getSystemPassword()`와 같은 `월 * 3 + 일` 4자리 계산을 작은 공용 helper로 추출해 재사용하며 다른 삭제 command로 확대하지 않는다.
- 레거시 `CCoopDAO::Delete`는 별도 종속성 검사를 하지 않고 기존 FK cascade에 의존한다. 실제 schema의 기존 cascade만 사용하고 앱에서 별도 사전 검사나 수동 연관 삭제를 추가하지 않는다. `DELETE FROM BM_COOPERATORWHERE`가 되는 레거시 SQL의 공백 누락은 복제하지 않는다.
- DB gate: `BM_COOPERATOR` 실제 컬럼과 레거시 SQL을 확인하기 전 DML SQL을 작성하지 않는다.
- 추가·수정·삭제의 child/parent 저장 경계와 취소·실패·성공 후 상태는 7.3의 네 관리 dialog 전용 lifecycle을 따른다. 저장 성공 후 관리 dialog 목록만 다시 조회하고 선택을 해제한다. 현재 로그인 협력업체를 수정해도 session context 자동 reload나 강제 로그아웃을 추가하지 않는다.

### 5.1.4 거래처 관리 (`IDM_CUST_MANAGE`)

- 레거시 기준: `OnCustManage` → `CCustManagerDlg`.
- 권한: 6.1의 `adminCommand`.
- 협력업체 선택에 종속된 거래처 목록과 CRUD를 포팅한다.
- command 접근 권한과 selector 변경 권한을 구분한다. 협력업체 selector는 레거시와 같이 `IsSystemAdmin` 또는 `IsAdminConnect`일 때만 활성화하고, 그 외 허용 사용자는 현재 로그인 context의 협력업체를 표시하되 변경하지 못하게 한다.
- 협력업체 selector를 변경하면 해당 협력업체의 거래처 목록만 DAO 반환 순서로 다시 조회하고 기존 행 선택을 해제한다. 첫 거래처 자동 선택이나 별도 빈 상태 fallback을 추가하지 않는다.
- 선택 거래처 `Connect`는 레거시 `CMarketDAO::SelectByCustID` 반환 순서의 첫 지점과 그 지점 사용자 반환 순서의 첫 `RICH_USER_GRADE=2` 사용자를 대상으로 한다. 별도 정렬·사용자 선택 UI, 관리 사용자 자동 생성이나 다른 등급·지점 대체 fallback을 추가하지 않는다.
- 첫 반환 지점 또는 그 지점의 grade 2 사용자가 없으면 context·연결 플래그·화면을 변경하기 전에 전체 Connect를 중단하고 오류를 한 번 전달한다. 레거시의 빈 배열 접근·assertion과 grade 2 부재 시 마지막 사용자 선택 결함은 복제하지 않는다.
- 대상이 확정되면 원래 관리자 계정을 최초 한 번만 보존한다. 현재 상태가 `IsSystemAdmin` 또는 `IsAdminConnect`이면 `(IsAdminConnect, IsCoopAdminConnect)=(true, false)`, `IsCoopAdmin` 또는 `IsCoopAdminConnect`이면 `(false, true)`로 설정한다. 이후 대상 사용자와 해당 지점·거래처·협력업체 context로 전환하고 기존 관리자 접속 로그를 저장해 메인 화면을 다시 초기화한다.
- 기존 `Customer` model을 우선 사용하고 CRUD가 없을 때만 DAO API를 추가한다.
- 추가·수정 입력은 선택 협력업체에 속한 거래처 이름만 편집하고 레거시 `CCustAddModifyDlg`의 최대 50자만 적용한다. 빈 값·이름 중복 검증은 추가하지 않으며 기존 DB 제약 오류는 그대로 전달한다.
- 추가 command는 항상 사용할 수 있다. 삭제·Connect command는 선택이 없어도 실행할 수 있으나 각각 DML·context 변경 없이 선택 요청 안내를 한 번 표시한다. 별도 수정 button·context command를 추가하지 않고 유효 행 더블클릭만 수정 진입점으로 사용한다.
- 목록의 유효 행을 더블클릭하면 해당 행을 선택한 뒤 수정 입력 dialog를 열고 header·빈 영역 더블클릭은 무시한다.
- 삭제는 레거시처럼 시스템 비밀번호 확인 후 삭제 경고를 한 번 표시하고 `BM_CUSTOMER` 한 건을 삭제한다. 실제 schema의 기존 FK cascade만 사용하고 앱에서 별도 종속성 사전 검사나 수동 연관 삭제를 추가하지 않는다.
- `BM_CUSTOMER` 실제 schema를 확인한다. 레거시 `CCustomerDAO` 조회에는 `ORDER BY`와 이름 중복 검증이 없으므로 Flutter에서 이름 정렬·중복 제한을 추가하지 않고 DAO 반환 순서를 유지한다.
- 추가·수정·삭제의 child/parent 저장 경계와 취소·실패·성공 후 상태는 7.3의 네 관리 dialog 전용 lifecycle을 따른다. 저장 성공 후 현재 협력업체의 거래처 목록을 다시 조회하고 선택을 해제하며 선택·scroll을 복원하지 않는다.
- 협력업체 관리와 하나의 generic CRUD dialog로 합치지 않는다. 공용 table shell 정도만 재사용한다.

### 5.1.5 지점 관리 (`IDM_MARKET_MANAGE`)

- 레거시 기준: `OnMarketManage` → `CMarketManagerDlg`.
- 권한: 6.1의 `managerCommand`.
- 현재 레거시 구현은 `IsSystemAdmin`, `IsCoopAdmin`, `IsAdminConnect`, `IsCoopAdminConnect` 중 하나만 활성이고 `MANAGER_USER`는 제외하므로 이를 visibility policy test로 고정한다.
- command 접근 권한과 selector 변경 권한을 구분한다. dialog는 현재 로그인 context의 협력업체·거래처로 초기화한다. 레거시 `CMarketManagerDlg::OnInitDialog`와 같이 `IsManager`이면 두 selector를 모두 비활성화하고, `IsCoopAdmin`이면 협력업체만 비활성화하며, `IsSystemAdmin`, `IsAdminConnect`, `IsCoopAdminConnect`이면 두 selector를 모두 활성화한다. 현재 command visibility에서는 `MANAGER_USER`를 숨기므로 `IsManager` 분기를 도달시키기 위해 권한을 넓히지 않는다.
- 협력업체 selector를 변경하면 해당 거래처 목록을 DAO 반환 순서로 다시 조회하되 첫 거래처를 자동 선택하지 않는다. 현재 거래처·지점 목록을 비우고 추가·삭제 command를 비활성화한다. 사용자가 거래처를 선택하면 해당 지점 목록을 다시 조회하고 추가·삭제 command를 활성화한다. 다른 거래처 fallback이나 이전 거래처 복원을 추가하지 않는다.
- 기존 `Market` model/조회 API를 확장해 거래처별 지점 이름 CRUD를 구현한다.
- 레거시 `CMarketAddModifyDlg`는 지점 이름의 빈 값과 중복을 검사하지 않으므로 신규 필수값·중복 검증을 추가하지 않는다. 기존 DB 제약으로 거부되면 해당 오류를 그대로 전달한다.
- 유효 거래처가 있을 때만 추가·삭제 command를 활성화한다. 삭제 command는 유효 거래처 안에서 행 선택이 없어도 실행할 수 있으나 DML 없이 선택 요청 안내를 한 번 표시한다. 별도 수정 button·context command를 추가하지 않고 유효 행 더블클릭만 수정 진입점으로 사용한다.
- 목록의 유효 행을 더블클릭하면 해당 행을 선택한 뒤 수정 입력 dialog를 열고 header·빈 영역 더블클릭은 무시한다.
- 추가는 `BM_MARKET`에 지점을 저장하고 같은 transaction에서 생성된 지점 ID를 받은 뒤, 해당 거래처의 모든 기존 품목에 대응하는 `ItemOfMarket` 행을 일괄 생성한다. 레거시의 전역 `SelectLastOneRecord()`를 복제하지 않는다.
- 수정은 선택 지점 이름만 갱신한다.
- 삭제는 레거시처럼 시스템 비밀번호 확인 후 해당 지점의 사용자·품목 매핑 삭제 경고를 한 번 표시한다. 하나의 transaction에서 `ItemOfMarket`을 지점 ID로 먼저 삭제한 뒤 `BM_MARKET` 한 건을 삭제한다.
- 추가·수정·삭제의 child/parent 저장 경계와 취소·실패·성공 후 상태는 7.3의 네 관리 dialog 전용 lifecycle을 따른다. transaction commit과 필요한 목록 reload가 성공한 뒤에만 성공 안내를 표시하고 선택을 해제한다. 레거시의 commit 전 reload·성공 안내 순서는 복제하지 않는다.
- 현재 로그인 지점을 수정·삭제해도 session context 자동 reload나 강제 로그아웃을 추가하지 않는다.

### 5.1.6 사용자 관리 (`IDM_USER_MANAGE`)

- 레거시 기준: `OnUserManage` → `CUserManagerDlg`, `CUserManagerModel`.
- 권한: 지점 관리와 같은 6.1의 `managerCommand`.
- 현재 레거시 구현은 `IsSystemAdmin`, `IsCoopAdmin`, `IsAdminConnect`, `IsCoopAdminConnect` 중 하나만 활성이고 `MANAGER_USER`는 제외하므로 이를 visibility policy test로 고정한다.
- 권한별 selector 활성 범위를 레거시 model에서 그대로 포팅한다.
  - 협력업체 selector: `IsSystemAdmin` 또는 `IsAdminConnect`.
  - 거래처 selector와 전체 표시: `IsSystemAdmin`, `IsCoopAdmin`, `IsAdminConnect`, `IsCoopAdminConnect` 중 하나.
  - 지점 selector: 위 조건 또는 `IsManager`. 다만 현재 command visibility에서는 `MANAGER_USER`를 숨기므로 도달하지 않는 상태를 별도 예외 UI로 만들지 않는다.
  - 생성·수정 가능한 사용자 등급은 `CLIENT_USER`와 `MANAGER_USER`뿐이며 신규 기본값은 `CLIENT_USER`다. `SYSTEM_ADMIN_USER`와 `COOP_ADMIN_USER` 생성 UI를 추가하지 않는다.
- 협력업체 selector를 변경하면 거래처 목록을 다시 조회하고 지점·사용자 목록을 비우며 거래처 selector만 활성화한다. 거래처를 선택하면 지점 목록을 조회하고, 지점이 있으면 DAO 반환 순서의 첫 지점을 선택해 사용자 목록을 조회하며 추가를 활성화한다. 지점이 없으면 사용자 목록을 비우고 추가를 비활성화한다. 지점을 직접 선택하면 해당 사용자 목록을 조회한다.
- `전체 표시`를 켜면 현재 협력업체 전체 사용자를 조회하고 거래처·지점 selector와 추가 command를 비활성화한다. 끄면 현재 지점이 있을 때 해당 사용자 목록과 추가를 복원하고, 현재 지점이 없으면 빈 목록과 추가 비활성 상태를 유지한다. 첫 거래처 fallback이나 별도 지점 생성 유도를 추가하지 않는다.
- 목록은 거래처, 지점, 사용자 ID, 비밀번호, 이름, 등급을 표시한다. `IsFirstConnectByAdmin`이 아니면 레거시처럼 사용자 ID와 비밀번호 column을 숨기며 별도 마스킹·보기 전환 UI를 추가하지 않는다.
- 추가·수정 입력은 사용자 ID, 비밀번호, 비밀번호 확인, 이름, 등급으로 제한한다. 모든 값을 필수로 검사하고 비밀번호와 확인값 일치, 사용자 ID 중복을 레거시처럼 검증한다. 수정 시 기존 사용자 ID와 같은 값은 허용한다.
- 유효 지점이 있고 `전체 표시`가 꺼졌을 때만 추가 command를 활성화한다. 삭제·Connect command는 유효 사용자 행을 선택했을 때만 활성화하고 목록 reload나 selector 변경으로 선택이 해제되면 함께 비활성화한다. 별도 수정 button·context command를 추가하지 않고 유효 행 더블클릭만 수정 진입점으로 사용한다.
- 목록의 유효 행을 더블클릭하면 해당 행을 선택한 뒤 수정 입력 dialog를 열고 header·빈 영역 더블클릭은 무시한다.
- 사용자 이름 부분 일치 검색은 현재 선택 행 다음부터 찾고 끝에 도달하면 처음부터 한 번 이어서 검색하는 레거시 흐름을 포팅한다.
- 삭제는 별도 시스템 비밀번호 gate 없이 레거시의 사용자 삭제 확인을 한 번 표시한 뒤 선택 사용자 한 건만 삭제한다.
- 사용자 목록은 레거시 `CUserDAO`처럼 거래처 ID, 지점 ID 순서를 유지하고 이름·사용자 ID 정렬을 추가하지 않는다.
- 선택 사용자의 `Connect`는 원래 관리자 계정을 최초 한 번만 보존한다. 현재 상태가 `IsSystemAdmin` 또는 `IsAdminConnect`이면 `(IsAdminConnect, IsCoopAdminConnect)=(true, true)`, 그 외 허용 상태인 `IsCoopAdmin` 또는 `IsCoopAdminConnect`이면 `(false, true)`로 설정한다. 선택 사용자와 해당 지점·거래처·협력업체 context로 전환하고 기존 관리자 접속 로그를 저장해 메인 화면을 다시 초기화한다. 거래처 Connect와 플래그 계산을 하나의 공용 mapping으로 합치지 않는다.
- 거래처·사용자 `Connect` 외에 일반적인 사용자 전환, 임의 권한 상승 또는 별도 impersonation 이력 UI로 확장하지 않는다.
- 로그인 `User.instance`와 `UserDAO.selectByUserId`는 session 로그인 mapping으로 유지한다. 사용자 관리 목록·CRUD는 별도 `ManagedUser` row/codec을 사용해 DB의 실제 `RICH_USER_GRADE`를 그대로 보존하고 로그인 전용 `_effectiveGrade`나 `clientUserTestOverrideId`를 적용하지 않는다. `UserGrade` enum은 공용으로 재사용한다.
- 비밀번호는 레거시 `CUserDAO`와 같이 입력 문자열을 변환하지 않고 기존 `BM_USER.RICH_PWD`에 그대로 조회·저장한다. hash·암호화·별도 credential table이나 migration을 추가하지 않는다.
- 추가·수정·삭제의 child/parent 저장 경계와 취소·실패·성공 후 상태는 7.3의 네 관리 dialog 전용 lifecycle을 따른다. 저장 성공 후 현재 범위의 사용자 목록을 다시 조회하고 선택을 해제하며 선택·scroll을 복원하지 않는다. 레거시 model의 오래된 row index나 Connect 활성 상태를 유지하지 않는다.
- 현재 로그인 사용자의 권한이 변경돼도 자동 반영/강제 로그아웃을 추측으로 추가하지 않는다.

### 5.1.7 관리자 복사 (`IDM_ADMIN_COPY`)

- 레거시 기준: `OnAdminCopy` → main window observer → `CAdminCopyDlg`, 성공 후 `CMainWindowModel::AdminCopyFinish`와 `UpdateAdminCopyFinish` observer 흐름.
- 권한: 6.1의 `adminCommand`.
- 협력업체 selector는 시스템 관리자만 변경할 수 있고 그 외 허용 사용자는 현재 협력업체로 고정한다. 원본과 대상 거래처는 같은 단일 협력업체 범위에서만 선택하며 별도 원본/대상 협력업체 selector를 만들지 않는다.
- 일반 label size 복사는 원본 거래처·브랜드·label size와 대상 거래처·브랜드·label size를 순서대로 선택한다. 대상 label size에 기존 column이 있으면 레거시 데이터 손실 경고를 한 번 표시하고, 사용자가 취소하면 column·label size·품목 복사를 모두 실행하지 않는다. 기존 column이 없으면 별도 확인 없이 실행한다.
- 일반 label size 복사에서 기존 column이 있으면 레거시의 실제 실행 범위대로 대상 label size의 `BM_GS1_COLUMN_INFO`, `BM_RICH_COLUMN`, `BM_RICH_COL_MIN`, `BM_RICH_CHECK_COLUMNS`를 삭제하고, `BM_RICH_STATUS.RICH_ID_CHANGE_DELETE_DATE`를 갱신한 뒤 `BM_RICH_ITEM`을 삭제한다. 레거시 `CColumnDAO::DeleteBySizeID`가 SQL만 만들고 실행하지 않는 `BM_GS1_CONTAIN_COLUMN` 삭제와 `BM_RICH_STATUS_DATA` 갱신은 추가하지 않는다.
- 이어서 원본 label size의 설정·서식을 대상의 brand ID·label size ID·이름은 유지한 채 덮어쓰고, 원본 `BM_RICH_COLUMN` 행을 대상 label size ID로 복사한다. `BM_RICH_COL_MIN`, `BM_RICH_CHECK_COLUMNS`, GS1 연관 정보처럼 레거시 `AddColumnData`가 복사하지 않는 데이터를 보완해서 복사하지 않는다.
- `브랜드 복사`가 체크된 실행 분기는 원본 거래처·브랜드와 대상 거래처만 사용한다. 대상 거래처에 원본과 같은 이름의 브랜드를 만들고 원본 브랜드의 모든 label size를 같은 이름으로 생성한 뒤, 각 label size의 설정·서식과 `BM_RICH_COLUMN`을 복사한다. 대상 브랜드·label size를 선택하거나 이름 중복 검사·자동 변경을 추가하지 않는다.
- `브랜드 복사` checkbox 값 변경 자체에는 selector를 초기화·활성화하는 별도 처리를 추가하지 않는다. 레거시처럼 이후 원본 브랜드 선택 event가 체크 상태에 따라 원본 label size 또는 대상 거래처를 활성화하고, 대상 거래처 선택 event가 대상 브랜드 또는 복사 command를 활성화하는 순서를 유지한다.
- 원본 label size 서식은 현 `LabelSizeDAO`의 `COALESCE(NULLIF(RICH_FORM_SHEET, ''), RICH_FORM_DATA)` 유효값을 사용하고 대상 `RICH_FORM_SHEET`에 저장한다. 현재 sheet payload를 레거시 `RICH_FORM_DATA`만으로 축소하거나 별도 신규 저장 형식을 만들지 않는다.
- `품목까지 복사`가 체크되면 대상 거래처의 첫 지점 ID를 레거시처럼 사용해 `proc_copy_item(fromLabelSizeId, toLabelSizeId)`, `proc_copy_item_content(targetFirstMarketId, fromLabelSizeId, toLabelSizeId)`, `proc_copy_item_of_market(targetFirstMarketId, fromLabelSizeId, toLabelSizeId)` 순서로 실행한다. 여러 지점에 반복 적용하지 않는다.
- `품목까지 복사` 대상 거래처에 지점이 없으면 지점을 생성하거나 다른 context로 대체하지 않고 DML 시작 전에 전체 복사를 중단해 오류를 전달한다. 세 저장 프로시저의 존재와 parameter 계약은 읽기 전용 DB metadata로 먼저 확인하며 없거나 다르면 migration·대체 SQL 없이 blocker로 기록한다.
- 브랜드 복사는 하나의 DAO 소유 server-side batch statement 안에서 `OUTPUT INSERTED`와 table variable로 생성 brand ID와 원본/대상 label size ID mapping을 유지하고, 해당 ID로 column 복사와 선택적 세 저장 프로시저를 실행한다. 이 batch 하나를 `DbTransactionStatement`로 `DbClient.transaction`에 전달하고 내부 `BEGIN TRANSACTION`/`COMMIT TRANSACTION`을 중복하지 않는다. 앞 statement 결과를 다음 parameter로 연결하기 위한 `DbClient.transaction` callback·동적 statement API를 추가하지 않고, 레거시 `SelectLastInsert()`나 별도 XML payload 경로도 추가하지 않는다.
- 경고·필수 selector·대상 첫 지점·프로시저 capability 검증을 DML 전에 끝내고, 선택한 복사 분기의 삭제·생성·갱신·column 복사와 선택적 품목 프로시저를 하나의 transaction으로 처리한다.
- 일부 테이블 복사 성공을 전체 성공으로 표시하지 않는다.
- commit 전 DML/validation 단계의 확정 실패는 가능한 rollback 후 오류를 그대로 전달하고 선택 상태를 유지한다. `DbCommitOutcomeUnknown`이면 같은 복사를 다시 실행하지 못하게 하고 오류 안내 후 dialog를 닫는다. 다시 열 때 최신 데이터를 조회하며 별도 읽기 전용 mode를 추가하지 않는다.
- commit과 필요한 reload가 성공하면 dialog를 닫고 현재 선택 label size를 다시 조회한 뒤 기존 owner API로 라벨 편집·자동품목갱신 데이터를 reload하고, 공용라벨 설정·column·서식·크기를 reload하며, 라벨출력·저울출력의 선택·발행 데이터·preview를 초기화한다. app restart, session reload나 강제 로그아웃을 추가하지 않는다.

### 5.1.8 종료 (`ID_APP_EXIT`)

- 현행 `PopScope`와 `LifecycleManager.notifyExitRequested()` 경로를 command API로 추출해 재사용한다.
- `SystemNavigator.pop()`을 메뉴에서 직접 중복 호출하지 않는다.
- 메뉴 종료와 OS 창 닫기는 같은 command에서 `활성 owner exit snapshot 수집 → write-busy/active editing 차단 → 전체 dirty 통합 확인과 승인된 폐기 callback 실행 → 열린 overlay 정리 → DB 정리 → 실제 종료` 순서로 처리한다.
- 활성 신규 dialog와 기존 메인 탭 중 하나라도 write-busy이면 종료를 거부한다. active editing은 자동 확정하거나 폐기하지 않고 종료를 중단해 먼저 편집 완료 또는 취소를 요구한다.
- 차단 상태가 없으면 신규 dialog와 품목·자동갱신·공용라벨의 dirty를 한 번의 폐기 확인에 합산한다. 취소하면 어떤 상태도 변경하지 않고, 승인하면 각 dirty owner의 폐기 callback을 정확히 한 번 실행한다.
- 모든 owner가 clean이면 일반 확인창을 추가하지 않는다. 메뉴 종료와 OS 창 닫기는 같은 차단 사유, 통합 폐기 확인, DB 정리 순서를 사용한다.
- 실제 종료 시 현재 로그인 상태이고 master-key가 아니면 원래 관리자 `ConnectOrigin` 유무와 관계없이 현재 사용자·거래처 값으로 `BM_LOGIN_LOG`에 `LOGOUT`을 한 번 기록한다. 따라서 관리자 `Connect` 중 명시적 로그아웃의 미기록 규칙을 앱 종료에 적용하지 않는다. 이력 오류는 한 번 전달하되 DB·session 정리와 종료를 중단하거나 자동 재시도하지 않으며, 종료 요청과 detached callback에서 같은 이력을 중복 기록하지 않는다.

## 5.2 조회/이력 그룹

### 5.2.1 검색 및 치환 (`IDM_SEARCH_AND_REPLACE`)

- 레거시 `OnSearchAndReplace` → `CSearchAndReplaceDlg`, `CSearchAndReplaceModel` 범위를 포팅한다.
- 권한: 6.1의 `managerCommand`.
- FortuneSheet 내부 검색/치환 기능과 레거시 업무 데이터 검색/치환을 동일 기능으로 간주하지 않는다.
- 검색 종류는 `품명`과 `주원료`만 제공하고 기본값은 `품명`이다. 레거시에서 주석 처리된 `추가 주원료` 검색을 활성화하지 않는다.
- 검색 범위는 현재 거래처의 품목으로 제한하고 선택적으로 브랜드, 브랜드의 label size 순서로 좁힌다. `CItemDetailDAO`의 품명·주원료 부분 일치 조회 범위를 사용한다.
- dialog를 열면 브랜드·label size filter checkbox를 모두 해제하고 두 selector와 label size checkbox를 비활성화한다. 브랜드 filter를 켜면 브랜드 selector와 label size checkbox를 활성화하고, label size filter는 브랜드 filter 안에서만 켤 수 있다. 검색어는 필수이며 켠 filter의 selector를 선택하지 않았으면 조회하지 않고 현재 상태를 유지한다. 그 외 신규 검색 검증은 추가하지 않는다.
- AppBar `검색 및 치환` command는 빈 초기 검색어로 dialog만 열고 자동 조회하지 않는다. 기존 메인 검색 button은 검색출력 mode가 꺼져 있을 때 현재 입력값을 전달하며, 값이 있으면 dialog가 열린 직후 한 번 검색하고 값이 없으면 자동 조회하지 않는다. 최근 검색어 복원이나 별도 자동 조회를 추가하지 않는다.
- 결과 column은 브랜드, label size, 품명, 주원료로 제한한다. 선택 품목의 개별 주원료 변경, 선택 행 일괄 치환, 변경된 품목 저장과 기존 품목편집/출력 이동 command를 포팅한다.
- command 진입 권한과 dialog 내부 쓰기 권한을 분리한다. 실제 로그인 사용자 등급이 6.1의 `editable`이 아니면 결과 checkbox column, 전체 선택, 개별·일괄 치환과 저장을 숨기고 더블클릭 치환을 막되 검색·결과 선택·품목편집/출력 이동은 유지한다. `IsAdminConnect`, `IsCoopAdminConnect`, `IsFirstConnectByAdmin`만으로 치환·저장 권한을 부여하지 않는다.
- 검색 결과 table에서 대상 행을 선택하고 일괄 치환한 변경값을 같은 table에 반영해 저장 전에 확인할 수 있게 한다. 레거시의 일괄 치환 확인과 최종 저장 확인을 유지하고 별도 preview dialog나 대상 건수 panel을 추가하지 않는다.
- `품목편집 이동`은 현재 선택 행 1건의 브랜드·label size·품목 ID를 사용한다. `출력 이동`은 체크 품목이 1건 이상이어야 하며 모두 같은 브랜드·label size일 때만 첫 체크 품목의 브랜드·label size와 전체 체크 품목 ID를 사용한다. 조건을 만족하지 않으면 dialog와 현재 선택·변경값을 유지하고 이동하지 않는다.
- 두 이동 command는 변경된 치환 내용을 자동 저장하지 않는다. 사용자 확정과 레거시 동작대로 미저장 치환 내용은 별도 폐기 확인 없이 버리고 dialog를 닫은 뒤 이동한다. 이 동작은 두 이동 command에만 적용하는 7.1 dirty 확인의 명시적 예외이며 일반 닫기·로그아웃·종료까지 확대하지 않는다.
- 변경된 행의 저장은 하나의 transaction으로 처리한다.
- 조회는 기존 `CItemDetailDAO`, 저장은 변경된 품목에 대응하는 기존 `CItemDAO` 책임으로 제한한다.
- 전체 DB 문자열을 임의로 검색하거나 범위를 확장하지 않는다.
- 권한은 실제 `OnEnableManager` 구현과 동일하게 적용한다.

### 5.2.2 발행내역 보기 (`IDM_PRINT_LOG`)

- 레거시 기준: `OnPrintLog` → `CPrintLogDlg`, `CPrintLogModel`.
- 권한: 6.1의 `loggedIn`.
- 레거시 `CPrintLogDAO`와 현행 `label_print_persistence.dart`가 함께 사용하는 기존 `BM_RICH_PRINT_LOG`를 조회 source로 고정해 read model과 DAO를 작성한다. `BM_RICH_STATUS`와 `BM_RICH_STATUS_DATA`는 5.2.6 발행 통계 책임으로 유지한다.
- filter UI는 기간, 권한별 협력업체/거래처 selector와 검색 종류 `품명`·`사용자ID`·`거래처`로 제한한다. 품명은 부분 일치, 사용자 ID와 거래처는 완전 일치를 사용하며 다른 filter를 추가하지 않는다.
- 기간은 선택한 시작일·종료일을 `yyyyMMdd`로 변환해 `RICH_DATE_YYYYMMDD BETWEEN 시작일 AND 종료일`로 조회하며 양끝 날짜를 모두 포함한다. timestamp의 `00:00:00`·`23:59:59` 경계나 timezone 변환을 만들지 않고, 시작일이 종료일보다 늦어도 자동 교환·추가 검증 없이 선택값 그대로 DAO에 전달한다.
- 초기 협력업체·거래처는 현재 로그인 context이고 검색 종류 기본값은 `품명`이다. 목록에 `[전체 보기]`가 있어도 dialog를 열 때 자동 선택하지 않는다.
- selector 범위는 레거시처럼 시스템 관리자는 협력업체·거래처를 변경할 수 있고, 협력업체 관리자는 현재 협력업체 안의 거래처만 변경할 수 있으며, 그 외 사용자는 현재 로그인 거래처로 고정한다.
- selector 표시 범위와 실제 query 범위를 구분한다. 사용자 확정과 레거시 `CPrintLogModel::Search`대로 선택 협력업체는 거래처 목록만 바꾸고 조회 SQL에는 사용하지 않는다. 거래처 `[전체 보기]`에서는 사용자 등급이나 선택 협력업체와 무관하게 전 협력업체의 내역·총 누계·기간 합계를 조회한다. 특정 거래처에서 `품명`·`사용자ID`로 조회할 때만 선택 거래처 ID 조건을 추가한다. 이 전역 범위를 현재 협력업체로 좁히거나 별도 경고를 추가하지 않는다.
- 검색 종류별 입력 동작도 레거시를 유지한다. `품명`의 빈 입력은 `LIKE '%%'`로 현재 거래처 선택 범위의 전체 품명을 조회하고, `사용자ID`의 빈 입력은 `RICH_USER_ID=''` 완전 일치로 조회한다. `거래처` 검색 종류를 선택하거나 거래처 selector를 바꾸면 검색 입력을 선택 거래처 표시명으로 채우고, 조회 시 입력이 비어 있으면 선택 거래처 표시명을 다시 사용한다. 거래처 검색은 입력한 이름의 완전 일치만 사용하며 선택 거래처 ID·협력업체 조건을 추가로 결합하지 않는다. 세 종류에 공통 필수 검색어 검증을 추가하지 않는다.
- 활성 child가 먼저 소비하지 않은 dialog-level Enter는 레거시 `CPrintLogDlg::OnOK`와 같이 현재 filter로 조회 command를 실행한다. 검색 입력의 Enter도 같은 조회 command를 한 번 실행한다.
- 결과는 레거시의 거래처, 브랜드, label size, 품명, 발행 수량, 사용자 ID·이름·등급, 발행 시각, 프린터, 서식 크기와 margin/push 값을 표시한다.
- 결과는 레거시 DAO의 `ORDER BY RICH_DATETIME ASC`대로 오래된 발행 시각부터 표시한다. 동일 시각에 ID·이름 등의 2차 정렬을 추가하지 않고 DB 반환 순서를 유지하며 별도 정렬 UI를 추가하지 않는다.
- 합계는 화면 결과 행을 합산하지 않고 레거시의 별도 DAO query를 사용한다. table 상단에 `[총 누계]`, `[기간별 합계]`를 이 순서로 각각 한 행 표시하고 그 뒤에 조건부 `[라벨사이즈별 합계]`, 마지막에 실제 결과 행을 표시한다. 실제 결과가 0건이어도 앞의 두 합계행은 유지하고 별도 0건 안내를 추가하지 않는다. `[총 누계]` 배경은 레거시 `RGB(230, 185, 184)`에 대응하는 `Color(0xFFE6B9B8)`, `[기간별 합계]`와 `[라벨사이즈별 합계]` 배경은 `RGB(215, 228, 188)`에 대응하는 `Color(0xFFD7E4BC)`로 표시한다.
- `[전체 보기]`의 총 누계는 조건 없는 전체 기간 합계, 기간별 합계는 날짜만 적용한 전 협력업체 합계다. 특정 거래처의 총 누계는 거래처 이름만, 기간별 합계는 날짜와 거래처 이름만 적용하며 현재 검색 종류·검색어는 적용하지 않는다. 실제 결과가 1건 이상이고 하나의 거래처 ID로만 구성되면 결과에 나타난 서로 다른 label size마다 날짜·거래처 이름·label size만 적용한 합계를 조회해 0이 아닌 행만 `[라벨사이즈별 합계]`로 표시한다. 현재 검색 종류·검색어를 적용하거나 결과 사이에 그룹별 합계를 삽입하지 않는다.
- 실제 결과 행만 상세를 열 수 있고 각 `FortuneTable` row가 자신의 `CPrintLog` 대응 read model을 직접 보유한다. 합계행에는 상세 command를 연결하지 않으며 레거시의 초기화되지 않을 수 있는 `curRow`나 화면 행 번호 차감 계산을 복제하지 않는다. 결과 상세는 `RICH_COLUMNS` 순서에 맞춘 `RICH_SAVE_IN_DB_CELLS` 저장값 표와 `RICH_PRINT_CELLS` 출력값 표를 각각 read-only로 표시한다. 같은 index의 저장값과 출력값이 다르면 출력값 표의 해당 행 배경만 레거시 `RGB(255, 192, 0)`에 대응하는 `Color(0xFFFFC000)`으로 표시한다. 아이콘·상태 문구·다른 표의 강조, 일반 before/after diff, 문자열 보정, 누락 column 추정이나 별도 비교 규칙을 추가하지 않는다.
- 전체 조회 결과를 read-only `FortuneTable`로 표시하며 pagination UI나 별도 paging API를 추가하지 않는다.
- 조회 전용이므로 transaction을 추가하지 않는다.
- 라벨출력과 저울출력이 같은 persistence와 `BM_RICH_PRINT_LOG`에 출력 경로 구분값 없이 저장되므로 하나의 통합 목록으로 표시한다. 프린터명이나 다른 값으로 출력 경로를 추측해 분류하거나 구분 column을 추가하지 않는다.

### 5.2.3 사용자 접속 이력 보기 (`IDM_LOGIN_LOG`)

- 권한: 6.1의 `loggedIn`.
- `lib/page_login/login_history_page.dart`는 현재 사용처가 없는 demo route/page이며 `Scaffold`, `Card`, `DataTable`, 일반 `showDatePicker`를 사용하므로 직접 재사용하지 않는다.
- 해당 파일의 demo row와 page shell을 제거하고 AppBar owning command가 여는 `BlockingModelessDialogFrame`용 content widget으로 전환한다. 기존 `BM_LOGIN_LOG`를 조회하는 DAO API를 구현하고 결과는 read-only `FortuneTable`로 표시한다.
- `LoginLogDAO`를 조회까지 확장하거나 조회 책임이 다르면 `LoginHistoryDAO`를 작성한다.
- 조회 filter는 레거시 `CLoginLogDlg`와 같이 기간과 권한별 협력업체/거래처 범위만 포팅한다. 지점과 사용자는 filter로 추가하지 않는다.
- 기간은 선택한 시작일·종료일을 `yyyyMMdd`로 변환해 `LOGIN_DATE_YYYYMMDD BETWEEN 시작일 AND 종료일`로 조회하며 양끝 날짜를 모두 포함한다. timestamp 경계나 timezone 변환을 만들지 않고 역전된 선택값도 자동 교환·추가 검증 없이 DAO에 전달한다.
- 초기 협력업체·거래처는 현재 로그인 context를 사용한다.
- selector 표시와 변경 범위는 시스템 관리자는 협력업체·거래처를 모두 표시하고 변경할 수 있다. 협력업체 관리자는 협력업체 selector와 label을 숨기고 거래처 selector만 표시·변경하며, 그 외 사용자는 두 selector와 관련 label을 모두 숨기고 현재 로그인 거래처로 조회한다.
- 조회 결과가 0건이면 기존 결과를 비운 read-only table을 유지하고 `검색결과가 없습니다!` 안내를 현재 차단형 dialog 위에 한 번 표시한다. 이 상태를 DB 오류로 처리하거나 자동 retry하지 않는다.
- 사용자 ID·등급, 로그인 시각·상태·IP, 거래처와 프로그램 버전은 레거시 결과 column으로만 표시한다. `LOGIN_CONDITION`은 `LOGIN(0)`이면 `로그인`, 그 외에는 `로그아웃`으로 표시하고 별도 미확인 상태를 추가하지 않는다.
- 결과는 레거시 DAO의 `ORDER BY LOGIN_DATE ASC`대로 오래된 로그인 시각부터 표시한다. 동일 시각에 ID 등의 2차 정렬을 추가하지 않고 DB 반환 순서를 유지하며 별도 정렬 UI를 추가하지 않는다.
- 활성 child가 먼저 소비하지 않은 dialog-level Enter는 레거시의 빈 `CLoginLogDlg::OnOK`처럼 조회·닫기를 실행하지 않는다.
- 별도 route, `Scaffold`, `AppBar`, 장식용 `Card`, `DataTable`을 유지하지 않는다. 날짜 선택은 기존 `showBlockingModelessOverlayDialog` 경로로 현재 차단형 dialog 위에 표시하며, 같은 구성이 둘 이상에서 반복될 때만 공용 date picker helper로 추출한다.
- locale 표시 형식은 현 `DateFormat`을 사용하되 DB 저장 문자열 형식은 변경하지 않는다.

### 5.2.4 데이터내용 이력 조회 (`IDM_CONTENT_SAVE_LOG`)

- 레거시 기준: `OnContentSaveLog` → `CContentSaveLogDlg`와 model/DAO.
- 권한: 6.1의 `loggedIn`.
- 기존 `BM_CONTENT_SAVE_LOG`를 조회 source로 사용한다.
- 조회 filter는 레거시와 같이 기간과 거래처 범위만 포팅한다. 품목, label size, column, 사용자와 변경 시각을 filter로 추가하지 않는다.
- 기간은 선택한 시작일·종료일을 `yyyyMMdd`로 변환해 `SAVE_DATE_YYYYMMDD BETWEEN 시작일 AND 종료일`로 조회하며 양끝 날짜를 모두 포함한다. timestamp 경계나 timezone 변환을 만들지 않고 역전된 선택값도 자동 교환·추가 검증 없이 DAO에 전달한다.
- 초기 협력업체·거래처는 현재 로그인 context를 사용한다.
- 시스템 관리자는 협력업체·거래처 selector를 표시하고 선택 거래처로 조회한다. 협력업체 selector 변경 handler가 비어 있어 거래처 목록이 갱신되지 않는 레거시 동작도 그대로 유지한다.
- 협력업체 관리자는 협력업체 selector를 숨기고 거래처 selector를 표시하지만, 조회는 선택값을 사용하지 않고 현재 로그인 거래처로 고정한다. 그 외 사용자는 두 selector를 숨기고 현재 로그인 거래처로 조회한다. 무효 selector를 정상 연결하거나 조회 범위를 넓히지 않는다.
- 조회 결과가 0건이면 기존 결과를 비운 read-only table을 유지하고 `검색결과가 없습니다!` 안내를 현재 차단형 dialog 위에 한 번 표시한다. 이 상태를 DB 오류로 처리하거나 자동 retry하지 않는다.
- 사용자 ID·등급, 품목명, label size, 저장 시각·IP와 신규/수정 상태는 결과 column으로 표시한다. `SAVE_STATUS`는 `SAVE_STATUS_NEW(0)`이면 `신규`, 그 외에는 `수정`으로 표시하고 별도 미확인 상태를 추가하지 않는다. 결과 행을 더블클릭하면 `CONTENT_COLUMNS`와 `CONTENTS`를 각각 줄바꿈으로 분리하고 같은 index끼리 짝지어 둘 중 짧은 개수까지만 표시하는 read-only 상세 overlay를 연다. 누락값 보정이나 별도 before/after 비교 field·UI를 추가하지 않는다.
- 결과는 레거시 DAO의 `ORDER BY SAVE_DATE ASC`대로 오래된 저장 시각부터 표시한다. 동일 시각에 ID 등의 2차 정렬을 추가하지 않고 DB 반환 순서를 유지하며 별도 정렬 UI를 추가하지 않는다.
- 활성 child가 먼저 소비하지 않은 dialog-level Enter는 레거시의 빈 `CContentSaveLogDlg::OnOK`처럼 조회·닫기를 실행하지 않는다.
- 현재 프로젝트의 저장 로그 table이 없거나 기존 schema가 다르면 migration으로 해결하지 않는다. blocker로 기록한다.
- 조회 전용 DAO와 row model만 작성하며 원본 업무 데이터를 변경하지 않는다.

### 5.2.5 공용라벨 수정 이력 보기 (`IDM_COMMON_LABEL_LOG`)

- 레거시 메뉴는 `INACTIVE`다.
- inventory와 `legacyInactive` 숨김 policy만 유지하며 이번 포팅의 내부 기능 구현·테스트 범위에서는 제외한다.
- 별도의 활성화 요구가 생기면 `CCommonLabelLog`, 실제 schema, 현재 LabelSheet read-only preview를 다시 확인해 별도 범위로 구현한다.

### 5.2.6 발행 통계 조회 (`IDM_STATUS_PRINT`)

- 레거시 기준: `OnStatusPrint` → `CStatusPrintDlg`, `CStatusPrintModel`.
- 권한: 6.1의 `loggedIn`.
- 현행 `BM_RICH_STATUS`, `BM_RICH_STATUS_DATA` 저장 계약은 `label_print_persistence.dart`에서 확인한다.
- 조회는 레거시 `CStatusPrintModel::Search`가 호출하는 `CStatusPrintDAO::SelectDLG` 흐름으로 고정한다. filter는 기간, 권한별 거래처, 브랜드, label size, 품목명, 검색 column·값과 완전/부분 일치 조건만 포팅한다. 완전 일치는 `=`, 부분 일치는 `LIKE`를 사용하고 dialog 기본값은 레거시의 `Include=true`, `Batch=false`와 같은 부분 일치다.
- 기간은 선택한 시작일·종료일을 `yyyyMMdd`로 변환해 `RICH_DATE_YYYYMMDD BETWEEN 시작일 AND 종료일`로 조회하며 양끝 날짜를 모두 포함한다. timestamp 경계나 timezone 변환을 만들지 않고 역전된 선택값도 자동 교환·추가 검증 없이 DAO에 전달한다.
- 초기 협력업체·거래처는 현재 로그인 context를 사용하고 브랜드와 label size는 `[전체 보기]`, 검색 column은 `주원료`를 선택한다.
- 시스템 관리자만 협력업체·거래처 selector를 표시하고 변경할 수 있다. 그 외 로그인 사용자는 협력업체 selector를 숨기고 거래처를 현재 로그인 값으로 고정한다.
- 결과는 발행일, `RICH_PRINT_COUNT`, 품목명, 선택 검색 column의 값, 브랜드와 label size만 표시한다. 사용자 ID·이름·등급과 거래처 column은 레거시 활성 화면에서 주석 처리됐으므로 표시하지 않는다.
- 결과는 레거시 DAO의 `ORDER BY RICH_DATETIME ASC`대로 오래된 발행 시각부터 표시한다. 동일 시각에 ID 등의 2차 정렬을 추가하지 않고 DB 반환 순서를 유지하며 별도 정렬 UI를 추가하지 않는다.
- 결과 첫 두 요약행에는 조회된 `RICH_PRINT_COUNT`를 화면에서 합산한 `[총 발행 매수]`와 `RICH_ID_CHANGE_DELETE_DATE`가 있는 결과만 합산한 `[삭제된 품목 매수]`를 표시한다. 실제 결과가 0건이어도 두 요약행을 각각 0으로 유지하고 별도 0건 안내를 추가하지 않는다. 총 발행 매수 행 배경은 레거시 `RGB(215, 228, 188)`에 대응하는 `Color(0xFFD7E4BC)`, 삭제된 품목 매수 행과 `RICH_ID_CHANGE_DELETE_DATE`가 있는 결과 행 배경은 `RGB(247, 235, 0)`에 대응하는 `Color(0xFFF7EB00)`으로 표시한다. 별도 상태 column·아이콘·tooltip·행 비활성화나 `SelectPrintCountSum` 계열 DAO·별도 SQL aggregate를 추가하지 않는다.
- 활성 child가 먼저 소비하지 않은 dialog-level Enter는 레거시 `CStatusPrintDlg::OnOK`와 같이 현재 filter로 조회 command를 실행한다.
- 실제 결과 행을 더블클릭하면 그 row가 직접 보유한 status ID로 레거시 상세 데이터를 조회해 품명·주원료와 각 column의 이름·저장일·값을 read-only 상세 overlay에 표시한다. 두 요약행에는 상세 command를 연결하지 않고 레거시 화면의 `Row-3`·`Row-4` 행 번호 계산이나 의도치 않은 빈 행을 복제하지 않는다. 사용자 정보나 별도 통계 항목을 상세에 추가하지 않는다.
- 통계 조회는 저장 이력 생성과 분리하며 조회 시 원본 데이터를 갱신하지 않는다.

## 5.3 설정 그룹

### 5.3.1 품목별 정보 편집 (`IDM_EDIT_ITEMINFO`)

- 레거시 기준: `OnEditItemInfo` → `CMainWindowModel::OnSelectEditItemInfo` → `CItemInfoDlg`/`CItemInfoModel`.
- 권한: 레거시 `OnEditItemInfo`의 실제 로그인 검사와 같은 6.1의 `loggedIn`. 일반 사용자를 숨기거나 `managerCommand`로 좁히지 않는다.
- 현재 로그인 지점과 메인 화면에서 현재 선택된 label size의 품목별 출력 보정값을 일괄 편집하는 전용 `BlockingModelessDialog`를 구현한다. dialog 내부에 지점·브랜드·label size selector를 추가하지 않는다. 메인 화면에서 브랜드와 label size가 모두 선택됐을 때만 현재 로그인 지점 ID·선택 label size ID로 조회하고, 둘 중 하나라도 없으면 DAO를 호출하지 않은 빈 목록과 저장 비활성 상태로 연다. 다른 context나 첫 label size fallback을 추가하지 않는다. 단순 품목관리 탭 이동으로 대체하거나 품목관리의 상시 column을 늘리지 않는다.
- 반복 행은 `FortuneTable`로 표시하고 품명은 읽기 전용으로 둔다. 편집 대상은 레거시 활성 field인 줄간격 사용 여부·줄간격, 저울 바코드 사용 여부, 기본 발행 수량, 개별 label size 사용 여부·폭·높이, 개별 margin 사용 여부, 좌·우·상 margin과 좌·상 push로 제한한다.
- 줄간격, 개별 label size, margin의 세부 입력은 각 사용 여부가 켜진 행에서만 편집 가능하게 한다. 판매 시작일, 원가, 할인 관련 field처럼 레거시 dialog에서 숨긴 field를 이번 command에 추가하지 않는다.
- 기존 `ItemOfMarket`과 `ItemOfMarketDAO`를 재사용하거나 최소 확장한다. 저장은 현재 목록의 변경값을 레거시 `CItemOfMarketDAO::UpdateBatch`에 대응하는 하나의 batch/transaction으로 반영하고, 성공 후 현재 품목·출력 session이 사용하는 `ItemOfMarket` 목록만 갱신한다.
- dialog가 dirty이면 닫기·로그아웃·종료의 기존 통합 폐기 확인에 포함하고, 저장 중에는 닫기와 중복 저장을 차단한다.
- 사용자 확정에 따라 저장과 필요한 `ItemOfMarket` 목록 갱신이 성공하면 현재 목록을 committed 기준으로 반영해 dirty·write-busy를 해제하고 dialog와 현재 행 선택·scroll 상태를 유지한다. 이 화면에 공통 성공 후 overlay 제거 규칙을 적용하지 않으며 자동으로 다른 context를 조회하지 않는다.

### 5.3.2 영양성분 형식추가 (`IDM_NEW_NUTTYPE`)

- 레거시 기준: `OnInsertNewNutTypeDlg` → `CNuttypeManagerDlg`.
- 권한: 6.1의 `adminCommand`.
- `BM_RICH_NUTTYPE` 형식 목록과 `BM_RICH_NUTCOLUMN` 구성 행의 추가·수정·삭제를 포팅한다. 형식명은 필수이고 구성 행은 1개 이상이어야 한다.
- 빈 구성에서 첫 행을 추가하면 keyword는 `N01`이고, 이후 추가는 현재 표시된 마지막 행 keyword의 끝 두 자리 숫자에 1을 더해 `N%02d` 형식으로 부여한다. 행을 삭제해도 남은 keyword를 재번호하지 않으며, 중간 번호를 채우거나 정렬하지 않는다. 모든 행을 삭제한 뒤 다시 추가할 때만 `N01`부터 시작한다. keyword는 사용자가 수정하지 못하게 하고 성분명만 편집한다. 별도 행 정렬 command, 형식명·성분명 중복 검증, 신규 검증 규칙을 추가하지 않는다.
- 기존 형식 수정은 parent 이름을 갱신하고 기존 detail을 삭제한 뒤 현재 구성 행을 다시 저장한다. 형식 삭제 전에는 해당 형식을 사용하는 영양성분표가 모두 삭제된다는 경고와 삭제 확인을 한 번 표시하고, 확인하면 `BM_RICH_NUTBOX`, `BM_RICH_NUTCOLUMN`, `BM_RICH_NUTTYPE` 순서의 연관 삭제를 하나의 transaction으로 처리한다. 별도 종속성 검사나 추가 확인을 만들지 않는다.
- 관련 기존 테이블과 column이 실제 DB에 있는지 확인한 뒤 전용 model/DAO를 작성한다.
- 현재 label column/barcode 모델에 억지로 포함하지 않는다.
- 신규·수정의 형식 parent와 detail 저장도 하나의 transaction으로 처리한다. `MAX(RICH_NUTTYPE_ID)`로 방금 삽입한 parent를 추측하지 않고 현재 SQL Server 2017과 compatibility level 100에서 지원하는 동일 transaction의 생성 ID 반환을 사용한다.
- 신규·수정 입력 dialog는 저장 성공 후에도 레거시처럼 같은 입력 상태로 열린 채 유지하고 사용자가 직접 닫을 때 manager 목록으로 돌아가 reload한다. 신규 상태에서 저장 command를 다시 실행해 같은 내용이 추가되는 동작을 별도 중복 방지나 자동 수정 전환으로 바꾸지 않는다.
- dialog-level Enter도 레거시 `OnOK()`와 같이 저장 command를 실행하고 dialog를 유지한다.

### 5.3.3 영양성분표 추가 (`IDM_NEW_NUTBOX`)

- 레거시 기준: `OnInsertNewNutboxDlg` → `CNutBoxManagerDlg`.
- 권한: 6.1의 `adminCommand`.
- `BM_RICH_NUTBOX` 목록에 영양성분 형식명과 표 명칭을 표시하고 형식 ID 순서로 조회한다. 선택 행의 `RICH_NUTBOX_DATA` RTF는 목록 옆 읽기 전용 preview로 표시한다.
- 추가·수정 입력은 영양성분 형식, 표 명칭, RTF 내용, 폭만 제공한다. 신규 입력은 형식을 자동 선택하지 않고 수정 입력만 저장된 형식을 선택한다. 명칭과 0이 아닌 폭, 형식 선택을 필수로 검증하고 별도 영양성분 schema나 field를 추가하지 않는다.
- 추가·수정은 `RICH_NUTBOX_TYPE`, `RICH_NUTBOX_NAME`, `RICH_NUTBOX_DATA`, `RICH_NUTBOX_WIDTH`만 저장한다. 삭제 전에는 일반 삭제 확인을 한 번 표시하고, 확인하면 선택한 `RICH_NUTBOX_ID` 한 건만 처리한다. 형식 삭제의 연관 경고나 별도 종속성 검사를 확대 적용하지 않는다.
- 신규·수정 입력 dialog는 저장 성공 후에도 레거시처럼 같은 입력 상태로 열린 채 유지하고 사용자가 직접 닫을 때 manager 목록으로 돌아가 reload한다. 신규 상태에서 저장 command를 다시 실행해 같은 내용이 추가되는 동작을 별도 중복 방지나 자동 수정 전환으로 바꾸지 않는다.
- dialog-level Enter도 레거시 `OnOK()`와 같이 저장 command를 실행하고 dialog를 유지한다.
- 수정 입력 dialog를 닫고 목록을 reload하면 수정 전에 선택한 행 위치를 다시 선택하고 해당 RTF read-only preview를 복원한다. 추가·삭제 후에는 선택이나 preview를 임의 복원하지 않는다.
- RTF 편집·읽기 전용 preview는 현 프로젝트의 기존 RTF renderer/helper를 우선 재사용한다. `RICH_NUTBOX_DATA`를 FortuneSheet workbook으로 변환하거나 renderer 재사용을 위해 별도 저장 형식을 추가하지 않는다.
- 형식 관리와 공용 dialog/table shell은 재사용할 수 있으나 영양성분표 row model, DAO와 입력 draft는 형식 관리의 parent/detail 저장 책임과 섞지 않는다.
- 임의의 신규 nutrition schema를 만들지 않는다.

### 5.3.4 고정 항목 관리 (`IDM_FIX_COLUMN_MANAGER` / `IDM_FIX_COLUMN_MANAGE`)

- 레거시 기준: `OnFixColumnManage` → `CFixColumnEditDlg`.
- 권한: 6.1의 `systemAdminCommand`.
- 레거시 메뉴 리소스는 `IDM_FIX_COLUMN_MANAGER(32853)`를 사용하지만 `MainFrm.cpp`의 command/update message map은 `IDM_FIX_COLUMN_MANAGE(32854)`를 사용한다.
- 현재 소스에는 두 ID를 잇는 alias나 다른 활성 진입점이 없어 메뉴에서 handler까지 도달하는 경로가 확인되지 않는다. Flutter inventory에는 하나의 `fixColumnManage` ID와 두 legacy ID의 불일치 사유를 기록하고 `legacyUnreachable`로 분류한다.
- production 메뉴에서는 숨기며 이번 포팅의 dialog/model/DAO 구현·내부 schema 조사·완료 판정 범위에서 제외한다. 두 ID를 별도 메뉴로 만들거나 도달하지 않는 handler를 활성 command로 되살리지 않는다.
- 실제 배포된 레거시 binary에서 도달 경로가 확인되거나 별도 활성화 요구가 생기면 `CFixColumnEditDlg`, `SpecalKeyword`, `TColumnSpecial`과 실제 저장 source를 다시 대조해 별도 범위로 구현한다. schema가 확인되지 않으면 migration 없이 blocker 처리한다.

### 5.3.5 전자저울 관리 (`IDM_SETUP_SCALE`)

- 권한: 6.1의 `loggedIn`.
- 기존 저울출력의 `showScaleConnectSettingsDialog`, `DbScaleConnectInfoHelper`, `_openScaleConnectSettings()` owning method를 재사용한다.
- `HomePageManager` controller에 공개 command API를 노출하고, 이 API가 기존 private `_openScaleConnectSettings()`에 위임한다. private method를 외부에서 직접 호출할 수 있게 변경하거나 내용을 AppBar controller에 복제하지 않는다.
- 저울 연결 중에는 기존 정책대로 먼저 연결 해제를 요구한다.
- 포트 scan, 연결 retry 같은 추가 보완을 메뉴 포팅 범위에 넣지 않는다.

### 5.3.6 프린터 설정 (`IDM_SETUP_PRINT`)

- 현재 라벨출력과 저울출력에는 서로 다른 session/settings context가 있다.
- 레거시 단일 `IDM_SETUP_PRINT`는 provenance로만 유지하고, 이미 확정한 1→2 mapping에 따라 현 화면별 기존 owning method를 각각 연결한다. 레거시 전역 설정 dialog나 새 공용 설정 동작을 추가하지 않는다.
- AppBar `설정` popup에서는 하나의 문맥 의존적인 `프린터 설정` 항목으로 합치지 않고 `라벨출력 프린터 설정`과 `저울출력 프린터 설정`을 명시적인 두 command로 표시한다.
- 두 Flutter command는 각각 안정적인 command ID와 label을 가진다. 두 command와 레거시 `IDM_SETUP_PRINT`의 대응 관계는 포팅 inventory test fixture에 기록한다.
  - `라벨출력 프린터 설정` 공개 command는 기존 private `_openLabelPrintSettings()` owning method에 위임한다.
  - `저울출력 프린터 설정` 공개 command는 기존 private `_openScaleOutputPrinterSettings()` owning method에 위임한다.
- private owning method를 public으로 바꾸거나 설정 dialog 실행 내용을 AppBar controller에 복제하지 않는다.
- 현재 탭에 따라 같은 menu item의 의미나 label을 바꾸지 않고, 다른 탭에서 라벨출력 설정으로 자동 기본화하지 않는다.
- 두 command는 로그인 상태에서만 표시하고 각각 기존 라벨출력·저울출력 owner가 busy일 때 비활성화한다. `라벨출력 프린터 설정`에는 label size·저울 연결·현재 탭 조건을 추가하지 않는다. `저울출력 프린터 설정`은 `_effectiveLabelSize`가 없을 때만 추가로 비활성화하고 label size 선택이 필요하다는 짧은 `disabledReason`을 표시한다. 저울 연결 여부나 `useScale`을 추가 조건으로 만들지 않는다.
- 설정 저장은 기존 `LabelPrintSettingsController`/DAO를 재사용한다.
- 별도 전역 printer schema를 만들지 않는다.

### 5.3.7 거래게시판 (`IDM_TRADE_BOARD`)

- 레거시 메뉴는 `INACTIVE`다.
- inventory와 `legacyInactive` 숨김 policy만 유지하며 이번 포팅의 내부 기능 구현·source 조사 범위에서는 제외한다.
- 별도의 활성화 요구가 생기면 `CTradeBoardViewDlg`와 실제 DB/URL source를 확인해 별도 범위로 구현하며, 임의 게시판 테이블이나 외부 서비스를 만들지 않는다.

### 5.3.8 이더넷 설정 (`IDM_SETUP_ETHENET`)

- 레거시 `OnUpdateSetupEthernet`이 항상 비활성화하므로 inventory에는 유지하되 `legacyInactive`로 분류하고 production 메뉴에서는 숨긴다.
- 이번 포팅의 dialog/model/DAO 구현·내부 source 조사·완료 판정 범위에서는 제외한다.
- 별도의 활성화 요구가 생기면 `OnSetupEthenet`, `CSetupEthenet`의 대상 장치와 저장 source를 다시 확인해 별도 범위로 구현한다. OS 네트워크 어댑터 일반 설정, 관리자 권한 상승, Windows UAC 자동 실행으로 확대하지 않는다.

### 5.3.9 업데이트 메시지 (`IDM_UPDATE_NOTICE`)

- 권한: 6.1의 `loggedIn`. 비로그인에는 숨기며 `adminCommand`나 세션성 관리자 연결 상태로 범위를 바꾸지 않는다.
- 기존 `NoticeDAO`와 startup notice 흐름을 재사용한다.
- 로그인된 현재 사용자의 `BM_UPDATE_NOTICE` 단일 row를 조회해 해당 공지를 다시 연다. 공지 목록·이력·최신 공지 선택 UI나 신규 table은 추가하지 않는다.
- 사용자 등급에 따른 레거시 전체 동작을 포팅한다.
  - `SYSTEM_ADMIN_USER`: 공지 내용 편집과 전체 협력업체 또는 선택 사용자 대상 지정을 제공한다.
  - `COOP_ADMIN_USER`: 공지 내용 편집과 현재 협력업체 또는 선택 사용자 대상 지정을 제공한다.
  - 그 외 로그인 사용자: 공지를 읽기 전용으로 표시하고 `다시 보지 않기` 상태만 변경한다.
- 사용자 선택 목록은 실제 로그인 사용자의 현재 협력업체 사용자로 제한한다. 사용자 선택 checkbox가 켜지고 1명 이상 선택됐으면 시스템·협력업체 관리자 모두 선택 사용자 row만 `UN_MSG`, `UN_STATE=0`, `UN_TIME=GETDATE()`로 갱신하며 여러 사용자는 하나의 transaction으로 처리한다.
- 사용자 확정에 따라 사용자 선택 checkbox를 끄면 선택 ID를 비우고, 선택 mode에서 0명이면 전체 또는 현재 협력업체 대상으로 확대하지 않고 DML 전에 저장을 중단해 오류를 한 번 전달한다. 별도 대상 자동 선택이나 fallback을 추가하지 않는다.
- 사용자 선택 mode가 아닐 때 시스템 관리자는 `전체 협력업체`가 켜지면 전체 `BM_UPDATE_NOTICE` row의 `UN_MSG`, `UN_STATE=2`, `UN_TIME=GETDATE()`를 갱신하고, 꺼지면 현재 협력업체 row의 `UN_MSG`, `UN_STATE=0`, `UN_TIME=GETDATE()`를 갱신한다. 협력업체 관리자도 현재 협력업체 row의 `UN_MSG`, `UN_STATE=0`, `UN_TIME=GETDATE()`만 갱신한다. 일반 사용자는 현재 사용자 row의 `UN_STATE`를 `다시 보지 않기` checkbox에 따라 1 또는 0으로 바꾸고 `UN_TIME=GETDATE()`만 갱신하며 `UN_MSG`는 수정하지 않는다.
- 기능 권한은 실제 사용자 등급으로 결정하며 `IsAdminConnect`, `IsCoopAdminConnect`, `IsFirstConnectByAdmin`만으로 관리자 편집 기능을 부여하지 않는다.
- startup dialog의 private UI를 복제하지 말고 공용 notice content/component로 추출해 시작 화면과 메뉴 command가 같은 표시·편집 경로를 사용한다.
- dialog-level Enter는 레거시 default 저장 button과 같이 등급별 저장 command를 실행하고 성공하면 dialog를 닫는다.
- 기존 `BM_UPDATE_NOTICE`의 `UN_MSG`, `UN_STATE`, `UN_TIME`, 사용자·협력업체 대상 범위만 사용하고 migration을 추가하지 않는다.
- 업데이트 다운로드/자동 설치를 이번 command에 임의 추가하지 않는다.

### 5.3.10 검색출력모드 (`IDM_SEARCH_PRINT_MODE`)

- 권한: 6.1의 `loggedIn`.
- 레거시처럼 `F12`와 menu command가 같은 bool mode를 toggle한다. mode가 켜지면 메인 tab을 비활성화하고 기존 검색 button label을 `발행`으로, 꺼지면 `검색`으로 복원한다.
- mode가 꺼진 상태의 검색 button은 기존 검색·치환 흐름을 유지한다. mode가 켜진 상태에서는 현재 label size 안에서 품명 완전 일치 또는 `RICH_SEARCH_PRINT=1`인 column 내용 완전 일치로 조회한다.
- 검색 입력이 비어 있으면 DAO 조회·프린터 확인·발행 없이 오류를 한 번 표시한다. 일치 결과가 0건이어도 발행하지 않고 오류를 한 번 표시한다. 유사 검색, 다른 label size fallback이나 자동 retry를 추가하지 않는다.
- 일치 결과가 여러 건이어도 별도 선택·중복 경고·정렬을 추가하지 않고 레거시처럼 DB가 반환한 첫 행의 품목 1건을 기존 라벨출력 pipeline으로 즉시 발행한다. 발행 요청 후 검색 입력을 비운다.
- 기존 메인 검색 button은 빈 입력·0건·성공·조회 오류와 관계없이 검색출력 처리 시도를 마친 뒤 검색 입력을 비운다. 오류 때 입력을 보존하거나 성공 때만 비우는 별도 분기를 추가하지 않는다.
- 단순 bool toggle이라도 mode state는 AppBar widget이 아니라 HomePageManager/controller가 소유한다.
- popup에서는 command 앞 checkmark로 mode 활성 상태를 표시한다. segmented control은 사용하지 않는다.
- menu item을 선택하면 AppBar popup을 닫은 뒤 controller mode를 한 번 변경한다. 메뉴가 닫힌 상태에서 `F12`로 mode가 바뀌어도 다음 popup open 시 controller의 현재 값을 읽어 checkmark를 표시한다.
- 프린터 설정, 자동 증가, 출력 log 등은 현재 라벨출력 pipeline을 재사용한다. mode 전용 출력 구현이나 레거시와 다른 자동 검색/자동 발행 규칙을 추가하지 않는다.

### 5.3.11 검색출력 설정 (`IDM_SEARCH_PRINT_SETUP`)

- 권한: 6.1의 `loggedIn`.
- 레거시 `CSearchPrintSetupDlg`와 같이 브랜드, 브랜드의 label size, 해당 label size의 column 목록을 표시한다.
- dialog를 열면 현재 브랜드와 현재 label size를 우선 선택하고 해당 label size column의 DB `RICH_SEARCH_PRINT` 값을 checkbox draft에 그대로 반영한다. 브랜드를 변경하면 레거시 `CLabelSizeDAO::SelectByBrandID`의 `RICH_LABELSIZE_ORDER ASC` 반환 순서 첫 행을 선택하고 해당 DB 값을 다시 표시한다. ID·이름 정렬, 별도 최근 선택이나 사용자별 초기값을 추가하지 않는다.
- 화면에서 바꾸는 설정값은 각 column의 기존 `RICH_SEARCH_PRINT` 선택뿐이다. 개별 선택, 전체 선택, 전체 해제와 적용 command를 제공한다.
- 사용자 확정에 따라 적용은 레거시 `CColumnDAO::UpdateBatch`의 전체 범위를 복제한다. 선택 label size에서 조회한 전체 column snapshot으로 `BM_RICH_COLUMN`의 `RICH_SEARCH_PRINT`를 포함한 기존 전체 column 속성을 batch update하고, `BM_RICH_COL_MIN` 행 갱신·생성과 column type별 `BM_GS1_COLUMN_INFO`·`BM_GS1_CONTAIN_COLUMN` 갱신·삭제도 같은 분기로 수행한다. 이 DML은 하나의 transaction으로 처리하며 검색 flag만 갱신하는 최소 UPDATE로 축소하거나 다른 table·보정 로직을 추가하지 않는다.
- 적용 전 checkbox draft가 변경된 상태에서 브랜드·label size를 전환하거나 dialog를 닫으면 7.1의 공용 dirty 폐기 확인을 한 번 사용한다. 승인하면 변경을 저장하지 않고 버린 뒤 대상 selector의 DB 값으로 다시 초기화하거나 dialog를 닫고, 취소하면 현재 selector와 draft를 유지한다. selector 전환 시 자동 적용·자동 저장을 추가하지 않는다.
- 적용 성공 후 `저장되었습니다.` 안내를 한 번 표시하고 현재 전체 column snapshot을 committed 기준으로 갱신해 dirty를 해제한다. 현재 브랜드·label size·checkbox와 dialog는 그대로 유지한다. 변경이 없는 상태에서 적용을 다시 실행해도 레거시처럼 전체 batch를 수행하며 자동으로 dialog를 닫거나 적용 command를 비활성화하지 않는다.
- Enter도 레거시 default 적용 button과 `OnOK()`처럼 적용 command를 실행한다. 이 화면에만 7.1의 Enter 비저장과 성공 후 overlay 제거 규칙의 예외를 적용한다.
- 별도 검색 문구, 자동 발행 조건, 사용자별 설정이나 신규 설정 table을 추가하지 않는다.
- 기존 schema/config가 없으면 migration으로 대체하지 않는다.
- 설정 저장 commit 실패 시 dialog 입력 상태를 유지하고 오류를 전달한다.

## 5.4 후속 범위 참고: 도움말 그룹

- 다음 항목은 `파일`부터 `설정`까지인 이번 AppBar 포팅과 완료 판정에 포함하지 않는다.
- 후속 구현 요청이 있을 때 레거시 handler와 URL을 다시 확인한 뒤 별도 `도움말` 그룹으로 추가한다.

### 5.4.1 라벨매니저 정보 (`ID_APP_ABOUT`)

- 후속 구현 요청 시 앱명, `appVersion`, 빌드 정보, 저작권/지원 정보를 표시하는 작은 dialog를 구현한다.
- 후속 구현에서도 실행 환경의 비밀값, DB credential, 전체 connection string은 표시하지 않는다.

### 5.4.2 라벨지·프린터 구매, 원격지원, ITSNG 자료실

- 후속 구현 요청 시 레거시 handler에서 실제 URL과 실행 방식을 확인한다.
- 후속 구현에서는 확인된 URL만 `url_launcher` 같은 검증된 방식으로 연다.
- URL을 추측하거나 검색 결과 URL로 대체하지 않는다.
- 외부 브라우저 열기 실패는 오류를 한 번 전달하고 자동 재시도하지 않는다.
- 원격지원 프로그램을 자동 다운로드/실행하거나 권한 상승시키지 않는다.

## 6. 권한 policy 계약

### 6.1 등급 기반 policy

- `systemAdmin`: `SYSTEM_ADMIN_USER`.
- `admin`: `SYSTEM_ADMIN_USER` 또는 `COOP_ADMIN_USER`.
- `systemAdminCommand`: 레거시 `OnEnableSystemAdmin`과 같이 `IsSystemAdmin` 또는 `IsAdminConnect`를 허용한다. 협력업체 관리에 사용하며 고정 항목 관리는 inventory의 권한 metadata만 유지하되 `legacyUnreachable` 숨김이 우선한다.
- `adminCommand`: 레거시 `OnEnableAdmin`과 같이 `IsSystemAdmin`, `IsCoopAdmin`, `IsAdminConnect`, `IsCoopAdminConnect`, `IsFirstConnectByAdmin` 중 하나를 허용한다. 거래처 관리, 관리자 복사, 영양성분 형식·표에 사용한다.
- `managerCommand`: 레거시 `OnEnableManager`와 같이 `IsSystemAdmin`, `IsCoopAdmin`, `IsAdminConnect`, `IsCoopAdminConnect` 중 하나를 허용한다. 지점 관리, 사용자 관리, 검색 및 치환에 사용하며 `IsFirstConnectByAdmin`과 `MANAGER_USER`를 자동 포함하지 않는다.
- 세 command policy의 세션성 연결 상태는 6.2의 신뢰 가능한 source가 포팅된 경우에만 포함한다. 해당 상태가 없을 때 `systemAdminCommand`의 기본 등급은 `systemAdmin`, `adminCommand`와 `managerCommand`의 기본 등급은 `admin`이며, 임의로 허용 범위를 넓히지 않는다.
- `editable`: 현재 메인 업무 편집 정책인 client 제외 등급. 메뉴별 legacy admin policy와 혼동하지 않는다.
- `loggedIn`: 로그인된 모든 등급.
- `always`: 비로그인에서도 허용되는 종료 등.

### 6.2 세션성 관리자 연결 상태

- 레거시에는 `IsAdminConnect`, `IsCoopAdminConnect`, `IsFirstConnectByAdmin`가 있다.
- 현 프로젝트에 대응 상태와 신뢰 가능한 source가 없으면 `UserGrade`만으로 허용 범위를 넓히지 않는다.
- 거래처·사용자 관리의 `Connect`를 위해 별도 session model에 원래 관리자, `IsAdminConnect`, `IsCoopAdminConnect`, `IsFirstConnectByAdmin`에 대응하는 상태를 둔다. 로그인 이력 조건을 위한 master-key 로그인 상태도 같은 session 수명으로 보존하되 권한 flag나 `IsFirstConnectByAdmin`으로 사용하지 않는다. 최초 로그인과 두 `Connect` owning command만 각 대응 상태를 설정하며 로그아웃은 이력 기록을 시도한 뒤 모두 초기화한다.
- 중첩 `Connect`에서는 원래 관리자를 덮어쓰지 않고 레거시 `GetConnectOrigin() == nullptr` 조건과 같이 최초 원본을 유지한다. 연결 후 권한 policy는 이 session model의 신뢰 가능한 상태만 사용한다.
- query parameter, 사용자 ID 문자열, UI checkbox만으로 관리자 연결 상태를 위조하지 않는다.

### 6.3 policy 테스트 필수 matrix

command마다 모든 등급 조합을 반복하지 않는다. 공용 policy table test에서 비로그인, 관리자 계열, 일반 사용자, `tester01` override, `legacyInactive`, `legacyUnreachable`의 대표 경계를 고정한다. `systemAdminCommand`, `adminCommand`, `managerCommand`는 `IsFirstConnectByAdmin` 포함 여부와 `MANAGER_USER` 제외를 포함한 서로 다른 경계를 한 번씩 검증하고, command별 test는 해당 command에 별도 권한이나 busy/dirty 조건이 있을 때만 추가한다. admin-connect session은 실제 신뢰 가능한 상태가 구현된 command에만 검증한다.

## 7. 화면 및 DAO 구현 공통 기준

### 7.1 메뉴 dialog 표시 원칙

- AppBar 메뉴에서 새로 여는 관리·편집·조회 화면은 `OverlayEntry`의 root에 `BlockingModelessDialog`를 두고, 실제 창은 `BlockingModelessDialogFrame`으로 구성한다.
- 공용 wrapper는 구조상 modeless overlay이지만 배경의 pointer/touch와 처리되지 않은 key event를 차단한다. dialog가 열린 동안 배경 탭이나 AppBar command를 조작할 수 있게 우회하지 않는다.
- widget tree의 key 차단만으로 `HardwareKeyboard.instance.addHandler` 전역 handler가 차단된다고 가정하지 않는다. 차단 surface가 보이는 동안 F1/F2/F3 탭 이동, F5 새로고침, F12 검색출력 mode를 실행하지 않으며 dialog 내부의 Tab, Enter, Esc와 셀 편집 shortcut은 유지한다.
- `DbReconnectOverlay`처럼 widget tree 안에서 `ModalBarrier`를 렌더링하는 기존 전역 차단 UI도 같은 단축키 차단 결과를 보장한다.
- 기존 품목관리·공용라벨관리·라벨출력·자동품목갱신·저울출력처럼 상시 반복 작업으로 이미 존재하는 메인 탭은 그대로 재사용한다. 다만 레거시 `품목별 정보 편집`은 품목관리 탭과 저장 책임이 다른 출력 보정값 일괄 편집 기능이므로 5.3.1의 전용 차단형 dialog로 구현한다.
- 로그인, 로그아웃, 종료, 즉시 적용되는 검색출력 mode toggle처럼 별도 편집 화면이 없는 command도 억지로 dialog를 만들지 않는다.
- 이미 동작하는 저울·프린터 설정 등의 `showDialog`/`showGeneralDialog` owning dialog는 기존 command를 그대로 호출하고 같은 dialog를 `BlockingModelessDialog`로 한 번 더 감싸거나 coordinator 소유로 이전하지 않는다.
- 신규 관리·편집·조회 기능은 별도 `Navigator` route page, 새 메인 탭 또는 Windows native child window로 구현하지 않는다. 예외가 필요하면 장시간 상시 작업이어야 하며 근거와 영향 범위를 `SESSION_HANDOFF.md`에 기록하고 별도 승인 후 진행한다.
- 신규 AppBar dialog의 owning controller는 동일 dialog를 중복으로 열지 않는다. 차단형 dialog가 열린 동안 AppBar와 전역 command가 차단되므로 서로 다른 dialog 요청을 위한 전역 queue, stack 또는 focus 전환 처리는 추가하지 않는다.
- 작은 기존 설정 dialog는 현재 크기를 유지하되 화면보다 커지지 않도록 clamp한다.
- 신규 관리·조회 dialog는 보편적인 화면 비율을 강제하지 않는다. 입력 수, table column과 command 영역에 맞는 기능별 content 크기를 먼저 정하고, `MediaQuery`에서 가로 48px, 세로 64px 이상의 바깥 여백을 뺀 사용 가능 영역으로 clamp한다.
- 1168x720은 현재 라벨 항목 편집창처럼 넓은 table·편집 영역이 필요한 dialog의 참고 상한으로만 사용한다. 내용이 적은 dialog를 이 크기로 늘리지 않으며 더 넓은 결과 column이 확인된 경우에만 기능별 max width를 확장한다.
- 제목 bar에는 전체 한글 기능명과 닫기 icon을 표시한다.
- 명시적 draft 저장이나 command 영역이 필요한 신규 dialog는 `BlockingModelessDialogFrame.footer`에 화면에 필요한 상태와 `취소`/`저장`, `취소`/`적용`, `닫기` command를 직접 구성한다. 같은 footer 구조가 둘 이상에서 반복될 때만 공용 widget으로 추출한다.
- 즉시 적용형·단순 설정형 dialog와 기존 owning dialog는 현재 동작에 지속적인 footer가 없으면 억지로 추가하지 않는다.
- dialog를 열면 현재 작업의 첫 번째 주요 입력이나 table에 초기 focus를 두고, 비활성 control을 건너뛰며 마우스 없이 주요 입력과 command를 이동할 수 있게 한다. 화면마다 동일한 고정 Tab 영역 순서를 강제하지 않는다.
- Esc는 현재 focus를 가진 child에 실제 취소할 상태가 있을 때만 그 상태를 먼저 취소한다. 활성 셀 편집이나 비어 있지 않은 다중 선택이 Esc를 처리하면 dialog를 닫지 않고, 처리할 상태가 없으면 dialog 닫기 요청으로 이어진다. 일반 단일 행 선택은 별도 취소 단계로 간주하지 않는다.
- 사용자가 해당 dialog만 직접 닫을 때는 clean 상태를 즉시 닫고 그 dialog가 dirty일 때만 폐기 확인을 거친다. 로그아웃·메뉴 종료·OS 종료는 dialog별 확인창을 띄우지 않고 공용 lifecycle guard의 전체 dirty 통합 확인을 사용한다. dialog-level Enter는 현재 focus child가 셀·입력 편집으로 먼저 소비하지 않았고 개별 command에 예외가 명시되지 않았을 때 dialog 전체 저장 command를 실행하지 않는다.
- 화면별 Enter 예외는 활성 레거시 동작만 따른다. 협력업체·거래처·지점·사용자 추가/수정 입력은 적용 command를 실행하고 검증을 통과하면 입력 dialog를 닫아 parent 저장·목록 갱신으로 돌아간다. 품목별 정보 편집은 저장 command가 활성일 때 저장하고 dialog를 유지하며, 영양성분 형식·표 입력은 저장 후에도 dialog를 유지한다. 업데이트 메시지는 등급별 저장 후 dialog를 닫고, 사용자 관리 검색 입력은 이름 검색을 실행한다. 발행내역과 발행 통계는 현재 filter로 조회하고 사용자 접속 이력과 데이터내용 이력은 조회·닫기를 실행하지 않는다. 검색출력 설정은 5.3.11의 적용 후 유지 계약을 따른다. 네 관리 목록과 관리자 복사에서 child가 소비하지 않은 Enter는 수정·복사·닫기를 실행하지 않는다. 이 예외를 다른 dialog로 확대하지 않는다.
- dialog를 닫으면 가능한 경우 이를 연 AppBar control로 focus를 돌린다. 공용 wrapper 확장은 실제로 여러 dialog에서 같은 처리가 중복될 때 수행한다.
- dirty, write-busy 또는 active editing처럼 실제 종료 차단 상태가 있는 신규 dialog owner만 exit snapshot을 `LifecycleManager.notifyExitRequested()` 흐름에 제공한다. dialog별 observer가 각자 종료 확인창을 띄우지 않는다.
- 읽기 전용 dialog는 종료용 dirty/write-busy 상태를 추가하지 않고 닫은 뒤 늦은 조회 결과를 폐기한다. 공용 lifecycle guard는 신규 dialog와 `HomePageManager` snapshot을 함께 평가하고 통합 dirty 확인이 끝난 뒤 열린 overlay를 정리한다. HomePage나 AppBar가 manager의 private 상태를 각각 복제하거나 별도 확인창을 만들지 않는다.
- 저장·삭제 transaction busy 중에는 중복 실행과 닫기를 비활성화한다.
- footer가 없는 즉시 적용형·단순 설정형 dialog는 busy를 시작한 control 가까이에 작업 성격에 맞는 spinner 또는 짧은 상태 문구를 표시하고 관련 입력과 command만 비활성화한다. 장시간 저장처럼 상태 설명이 필요한 경우에만 spinner와 문구를 함께 표시하며, 진행 표시 없이 닫기만 비활성화해 화면이 멈춘 것처럼 보이게 하지 않는다.
- 읽기 전용 조회 busy 중에도 닫기 경로를 유지한다. 실제 조회 취소 API가 있을 때만 `조회 취소` command를 표시한다. 취소할 수 없는 DB Future는 dialog를 닫은 뒤 늦게 도착한 결과만 폐기하며 자동 retry나 복구 저장소를 추가하지 않는다.
- dialog 안에서 확인·경고나 date/time picker를 열 때는 root overlay의 현재 차단창 위에 삽입되는 `showBlockingModelessOverlayDialog` 경로를 사용한다. 같은 picker 구성이 반복될 때만 공용 helper로 추출한다.
- dropdown처럼 별도 overlay surface가 필요한 control은 현재 `_ModelessDropdownField`/`_DialogDropdown` 패턴을 재사용하고 dialog 위 표시와 화면 경계 clamp를 보장한다. 같은 구현이 두 화면 이상에서 중복될 때 공용 control로 추출한다.
- 중첩 확인창이나 picker를 닫으면 가능한 경우 이를 연 control로 focus를 복원한다.
- commit 전 DML/validation 단계의 확정 실패는 dialog와 draft를 유지하고 오류를 한 번 전달한다. `DbCommitOutcomeUnknown`과 commit 성공 후 reload 실패는 1.3의 결과별 계약을 적용해 같은 draft를 재저장하지 못하게 한다.
- 단, 5.1.3~5.1.6의 추가·수정 입력은 사용자 확정과 활성 레거시 동작에 따라 적용 시 child dialog를 먼저 닫고 parent validation·DML을 실행한다. parent validation·DML이 실패하면 manager와 기존 목록을 유지하고 오류를 한 번 전달하되 child를 자동 재개방하거나 draft를 복구하지 않는다. 이 예외를 다른 입력 dialog로 확대하지 않는다.
- commit과 필요한 reload가 모두 성공하면 committed 상태를 반영한 뒤 overlay를 제거하고 controller, listener, focus node를 dispose한다.
- 5.3.1 품목별 정보 편집은 사용자 확정과 레거시 동작에 따라 저장 성공 후 committed 기준과 dirty·write-busy만 갱신하고 dialog와 현재 목록 상태를 유지한다. 이 화면에 공통 성공 후 overlay 제거 규칙을 적용하지 않는다.
- 단, 5.3.2 영양성분 형식과 5.3.3 영양성분표의 신규·수정 입력 dialog는 사용자 확정과 레거시 동작에 따라 저장 성공 후에도 같은 입력 상태로 열린 채 유지한다. 이 두 화면에 공통 성공 후 overlay 제거 규칙, 자동 draft 초기화, 신규에서 수정 mode로의 자동 전환이나 중복 저장 차단을 적용하지 않는다.
- 5.3.11 검색출력 설정도 사용자 확정과 레거시 동작에 따라 적용 성공 후 committed 기준과 dirty만 갱신하고 dialog를 유지하며 Enter로 적용 command를 실행한다. 변경이 없는 재적용도 허용하고 이 화면에만 공통 성공 후 overlay 제거와 Enter 비저장 규칙을 적용하지 않는다.

### 7.2 FortuneTable 사용 원칙

- 관리 목록, 이력, 통계, 검색 결과처럼 동일한 column 구조의 row가 반복되는 화면은 `FortuneTable<T>`로 구현한다.
- 신규 화면에서 Flutter `DataTable`, `Table`, row별 `ListView` 조합 또는 기능별 자체 table widget으로 대체하지 않는다.
- column은 `FortuneTableColumn<T>`으로 선언하고 공용 header, column resize, 정렬, 선택, checkbox, 편집 callback 중 필요한 기존 API를 재사용한다.
- 관리 화면의 row draft와 저장 상태는 dialog/controller가 소유한다. `FortuneTable` 내부에 DAO 호출이나 업무 저장 로직을 넣지 않는다.
- 조회 화면은 `FortuneTable`을 read-only로 구성한다. 레거시 동작과 데이터 성격상 필요한 filter, 조회 command, 건수 또는 합계는 table 바깥의 owning dialog가 관리한다.
- 필요한 공용 table 기능이 없으면 개별 화면에 wrapper나 중복 구현을 추가하기 전에 `third_party/fortune_sheet/lib/src/fortune_table.dart`의 공용 API로 확장하고 `test/fortune_table_test.dart`에서 검증한다.
- `FortuneSheet`/LabelSheet는 workbook·라벨 시트 편집 또는 read-only 시트 미리보기에만 사용하며, 일반 관리 목록과 조회 결과 table 용도로 사용하지 않는다.
- 단일 상세값, key-value form, 선택 항목이 없는 짧은 안내 목록처럼 반복 column table이 아닌 UI는 `FortuneTable` 적용 대상이 아니다.
- 레거시 동작을 `FortuneTable`로 표현할 수 없는 기술적 blocker가 확인되면 임의 대체 table을 만들지 말고 원인과 필요한 공용 API를 `SESSION_HANDOFF.md`에 기록한 뒤 해결한다.

### 7.3 관리 dialog

- 운영 도구 성격에 맞게 밀도 높은 `FortuneTable` + 기능에 맞는 명확한 command 영역을 사용한다.
- 장식용 카드 중첩, hero, 과도한 animation을 사용하지 않는다.
- 협력업체·거래처·지점의 레거시 기본 조회에는 `ORDER BY`가 없으므로 초기 목록은 DAO 반환 순서를 유지하고 이름 정렬·중복 검증을 추가하지 않는다. 사용자 목록만 레거시 DAO의 거래처 ID, 지점 ID 순서를 유지한다. 별도 정렬 command를 추가하지 않는다.
- 행 단위 CRUD command는 작업 빈도와 현재 owning 화면의 패턴에 따라 테이블 상단 toolbar, 측면 command rail 또는 context menu에 배치한다. 자주 쓰는 핵심 command를 icon button으로 표시하면 tooltip을 제공한다.
- 지원하지 않는 row command는 비활성 button으로 남기지 않고 렌더링하지 않는다. 관리자 복사, 검색출력 설정처럼 row CRUD가 아닌 dialog에 CRUD toolbar/rail을 억지로 추가하지 않는다.
- 현재 품목관리처럼 context menu가 주 command인 작업은 같은 패턴을 사용할 수 있다. toolbar나 command rail을 추가할 때는 context menu와 command 구현을 공유한다.
- 명시적 draft 저장 흐름이 있는 신규 관리 dialog는 `BlockingModelessDialogFrame.footer`에 필요한 상태와 `취소`/`저장` 또는 `취소`/`적용`을 배치한다. 공용 footer widget은 같은 구조가 반복될 때만 추가한다.
- 즉시 적용형·단순 설정형 관리 dialog는 footer 없이 title bar 닫기와 본문 command만 사용할 수 있다.
- 추가/수정은 draft에서 수행하고 저장/취소를 명시한다.
- 협력업체·거래처·지점·사용자 추가/수정 child dialog는 UI draft와 입력 검증만 소유하고 DAO·SQL을 호출하지 않는다. 취소하면 child만 닫고 parent DML·reload·성공 안내를 모두 실행하지 않는다. 적용하면 child를 닫고 반환된 draft로 parent/controller가 필요한 validation을 수행한 뒤 본체 DML을 최대 한 번 실행한다.
- 네 관리 dialog는 parent 저장과 필요한 transaction이 성공한 뒤 현재 범위 목록을 DAO 반환 순서로 다시 조회하고 성공 안내를 한 번 표시한다. reload 후 행 선택을 해제하고 선택·scroll을 복원하지 않으며 command 상태는 각 본문의 context·selection 규칙으로 다시 계산한다. 지점 관리도 commit 전에 reload하거나 성공 안내를 표시하지 않는다.
- 삭제는 개별 command에 명시된 비밀번호 gate, 확인 문구와 연관 삭제 범위만 따른다. 공통 사전 종속성 검사나 추가 확인 단계를 만들지 않는다.
- 저장 성공 후 해당 목록과 개별 command에 명시된 app cache만 reload한다.
- 각 dialog의 owning controller가 해당 overlay와 open/close command를 관리한다.
- 협력업체·거래처·지점·사용자 관리의 유효 행 더블클릭만 선택 후 수정 입력 dialog를 열고 header·빈 영역은 무시한다. 다른 관리·조회 table에 더블클릭 수정 규칙을 공통 적용하지 않는다.

### 7.4 조회 dialog

- 조회 결과는 read-only `FortuneTable`로 표시하고, filter, 명시적 조회 button, 건수와 합계는 레거시 동작과 해당 데이터에 필요한 항목만 제공한다. 자동 조회에는 불필요한 조회 button을 추가하지 않고 일반 이력에는 의미 없는 합계를 만들지 않는다.
- 발행내역, 사용자 접속 이력, 데이터내용 이력, 발행 통계는 dialog를 열 때 selector와 date filter만 초기화하고 최초 자동 조회를 실행하지 않는다. 화면별 명시적 조회 command를 기다리며 시작일·종료일은 레거시가 별도 `SetTime`을 호출하지 않는 date control 기본값과 같이 현재 로컬 날짜로 초기화한다.
- 네 조회의 기간 조건은 각 본문에 명시된 저장 날짜 key에 선택한 `yyyyMMdd` 양끝을 포함하는 `BETWEEN`으로 적용한다. timestamp 시작·종료 경계나 timezone 변환을 추가하지 않고 시작일·종료일 역전도 자동 교환·별도 검증하지 않는다.
- 네 조회 결과의 초기 순서는 각 레거시 DAO의 날짜·시각 column `ASC`를 유지한다. 동일 시각에 ID·이름 등의 2차 정렬을 추가하지 않고 DB 반환 순서를 유지하며 `FortuneTable`의 별도 정렬 UI를 활성화하지 않는다.
- 조회 중 중복 요청만 막고 일반적인 자동 retry는 추가하지 않는다.
- 조회 중에도 닫기 command를 사용할 수 있어야 하며 닫힌 dialog에 늦은 결과를 적용하지 않는다. 실제 취소 가능한 조회에만 별도 `조회 취소` command를 제공한다.
- 빈 결과는 정상 상태로 표시하고 오류와 구분한다. 단, 사용자 접속 이력과 데이터내용 이력은 레거시처럼 결과 table을 비운 뒤 `검색결과가 없습니다!` 안내를 현재 차단형 dialog 위에 한 번 표시한다. 이 안내를 다른 조회 dialog로 확대하지 않는다.
- export가 레거시에 있거나 명시 요구가 있을 때만 추가한다.

### 7.5 오류 처리

- DAO 오류를 빈 목록이나 `false`로 바꾸지 않는다.
- DB 오류는 원인 문자열을 보존해 command 계층으로 전달한다.
- 차단형 overlay가 열린 동안 실패는 dialog 내부 inline 상태 또는 `showBlockingModelessOverlayDialog`로 한 번 알리고 draft를 유지한다. 뒤쪽 `ScaffoldMessenger` snackbar를 오류 전달 경로로 사용하지 않는다.
- snackbar는 차단형 overlay가 제거된 뒤 표시하는 비차단 성공 알림 또는 overlay 밖의 기존 화면 알림에만 사용한다. 사용자 확인이나 action이 필요한 오류를 snackbar로 표시하지 않는다.
- 같은 오류를 DAO, controller, UI에서 각각 중복 로그로 남기지 않는다.
- 권한 부족은 DB command 실행 전 policy에서 차단한다.

## 8. 구현 단계

### Phase 0. inventory와 command contract 고정

1. 파일/검색/설정의 Flutter owning action마다 `AppMenuCommandId`, group, label, optional icon, optional `shortcutLabel`, permission, `legacyInactive`, `legacyUnreachable`을 순수 table로 작성한다.
2. 모든 command의 레거시 handler, legacy command ID 대응 관계와 현 구현 상태는 포팅 대조용 test fixture로 고정한다. 하나의 legacy ID에서 분리된 여러 Flutter command는 서로 다른 안정 ID를 갖는다.
3. 권한 matrix unit test를 먼저 작성한다.
4. 아직 기능이 없는 command도 ID를 예약하되 실제 menu에는 기능 완료 시점에만 연결한다.

완료 기준:
- 파일/검색/설정 command 누락 없음.
- 권한별 visible/enabled 상태와 `legacyInactive`/`legacyUnreachable` 숨김이 테스트로 고정됨.

### Phase 1. AppBar shell과 기존 기능 연결

1. AppBar 그룹 아이콘과 좁은 화면의 2단계 overflow를 구현한다. 기존 desktop window 초기화 동작은 변경하지 않는다.
2. login/logout/exit를 기존 HomePage command에 연결한다.
3. 저울 설정, 라벨출력/저울출력 프린터 설정을 HomePageManager controller로 연결한다.
4. update notice 공용 dialog를 추출해 메뉴에서 연결한다.
5. AppBar menu·modal dialog·modeless overlay·`DbReconnectOverlay`가 보이는 동안 전역 F1/F2/F3/F5/F12를 차단하고 non-modal `PreviewFloatingWindow`만 보일 때는 기존 단축키를 유지한다.
6. 신규 관리·조회 dialog의 owning controller에 `BlockingModelessDialog` open/close, 동일 dialog 중복 방지, 초기 focus와 중첩 modal/picker pattern을 적용한다. 편집 또는 저장이 있는 dialog에만 실제 dirty, write-busy, active editing 상태를 추가한다.
7. AppBar popup을 닫은 뒤 owning command를 한 번 실행하고, dialog를 열면 내부로 focus를 넘긴다.
8. `HomePageManager`와 신규 dialog owner가 실제 busy, active editing, dirty 표시명·폐기 callback을 exit snapshot으로 제공하고, 공용 lifecycle guard가 차단 상태를 먼저 평가한 뒤 모든 dirty를 한 번 확인하고 overlay를 정리하도록 연결한다.
9. 명시적 draft 저장·command 영역은 기존 frame footer slot에 구성하고, 행 CRUD command는 작업 빈도와 현재 owning 화면에 맞는 toolbar, rail 또는 context menu에 배치한다.

완료 기준:
- 기존 화면 버튼과 AppBar 메뉴가 같은 owning command를 호출함.
- 편집 차단을 우회하지 않음.
- 신규 관리·조회 command가 별도 route page, 새 메인 탭 또는 native child window를 만들지 않음.
- AppBar popup을 닫은 뒤 owning command가 한 번만 실행됨.
- 재연결 차단 UI가 표시되는 동안 전역 F-key와 배경 focus 이동이 차단됨.
- floating preview `OverlayRoute`만 표시된 상태에서는 기존 Home 전역 F-key가 오차단되지 않음.
- 넓은 폭과 좁은 대표 폭에서 AppBar와 현재 핵심 탭의 주요 command에 접근할 수 있음.
- clean 로그아웃/종료에는 일반 확인창이 없고, 신규 dialog와 기존 메인 탭의 busy/activeEditing은 먼저 차단되며 모든 dirty는 한 번의 통합 확인으로 처리됨.
- 일반·master-key·`IsFirstConnectByAdmin` 로그인의 session 상태와 `LOGIN` 기록 조건, 일반·master-key·관리자 `Connect`의 명시적 로그아웃 및 앱 종료별 `LOGOUT` 기록 조건이 활성 레거시와 일치함. 이력 저장 실패는 상태 전환을 취소하거나 자동 재시도하지 않음.
- 업데이트 메시지의 실제 등급별 대상 우선순위와 `UN_STATE`가 일치하고, 사용자 선택 0건이 더 넓은 대상으로 확대되지 않음.
- wide/narrow widget test 통과.

### Phase 2. 읽기 전용 이력/통계

1. 로그인 이력 demo를 실제 `BM_LOGIN_LOG` 조회로 교체한다.
2. 발행내역 조회를 현 persistence schema 기준으로 구현한다.
3. 발행 통계를 구현한다.
4. 데이터내용 이력 schema를 확인해 구현한다.
5. 공용라벨 이력은 inventory의 legacyInactive 숨김 상태만 유지하고 활성화 요구 전에는 구현하지 않는다.

완료 기준:
- 조회 기능에 DML이 없음.
- 레거시 filter/column/권한 테스트 통과.
- 발행내역의 selector 표시 범위와 실제 query 범위, 검색 종류별 빈 입력, 검색어와 독립적인 별도 합계, 저장값·출력값 상세 비교가 레거시와 일치함.
- 네 조회 dialog가 현재 로그인 context, command별 기본 selector와 현재 로컬 날짜만 초기화하고 최초 자동 조회 없이 명시적 조회를 기다림.
- 사용자 접속 이력의 등급별 selector 표시·숨김과 사용자 접속·데이터내용 이력 0건 안내가 레거시와 일치함.
- 큰 결과에서도 UI overflow/중복 조회 없음.

### Phase 3. 계층형 관리 기능

다음 순서로 구현한다.

1. 협력업체 관리.
2. 거래처 관리.
3. 지점 관리.
4. 사용자 관리.
5. 관리자 복사.

순서 이유:
- 하위 selector와 foreign key 의미가 상위 entity에 의존한다.
- 각 단계에서 실제 schema와 레거시 DAO를 확인할 수 있다.

완료 기준:
- CRUD 및 selector 권한이 레거시와 일치.
- 협력업체·거래처·지점·사용자 목록의 유효 행 더블클릭만 수정 진입점이고 manager 목록 Enter 무동작, 각 추가/수정 입력의 Enter 적용·child 종료 후 parent 저장과 취소 DML 0건이 레거시 및 사용자 확정과 일치함.
- 네 관리의 성공 reload 후 선택 해제와 화면별 command 상태 재계산, 거래처·지점·사용자 selector cascade 및 사용자 `전체 표시` command 상태가 레거시와 일치함.
- 협력업체·거래처·지점 입력에는 레거시에 없는 필수값·중복 검증이 없고 거래처 이름만 50자 제한을 유지하며 사용자 비밀번호는 기존 `RICH_PWD` 문자열을 변환 없이 저장함.
- 협력업체·거래처·지점 삭제의 시스템 비밀번호 gate와 사용자 삭제의 일반 확인이 서로 확대되지 않고 구분됨.
- 지점 추가·삭제가 기존 품목의 `ItemOfMarket` 연관 행을 같은 transaction에서 생성·삭제함.
- 거래처·사용자 `Connect`가 서로 다른 레거시 연결 플래그 mapping을 유지하고 원래 관리자를 한 번 보존하며, 거래처 Connect 대상 부재 시 상태 변경 전에 중단하고 확정된 대상의 업무 context 전환·관리자 접속 로그만 수행함.
- 관리자 복사의 일반 label size·브랜드 분기, 실제 삭제·복사 범위, 브랜드 복사 단일 server-side batch, 선택적 품목 프로시저와 성공 후 owner 갱신이 레거시 범위와 일치함.
- 다중 DML transaction commit/rollback 테스트 통과.
- 저장 후 해당 목록과 개별 command에 명시된 app cache만 갱신됨.

### Phase 4. 설정 데이터 관리

1. 품목별 정보 편집.
2. 영양성분 형식.
3. 영양성분표.
4. 검색출력 설정과 mode.
5. 검색 및 치환.

완료 기준:
- schema 미확인 기능을 임의 구현하지 않음.
- mode/settings/data 저장 책임이 분리됨.
- 검색/치환 결과 table의 변경값 확인, 이동별 선택 source·미저장 변경 폐기, 레거시 확인 흐름과 transaction 검증 통과.
- 검색/치환의 초기 filter·두 진입점과 실제 등급별 쓰기 UI, 검색출력의 빈 입력·0건 결과 흐름이 레거시 범위와 일치함.
- 검색출력 설정의 현재 context·DB 값 초기화와 미적용 draft 폐기 확인 검증 통과.
- 검색출력 설정의 label size 순서, 전체 `CColumnDAO::UpdateBatch` DML과 transaction, 적용 후 dialog 유지·dirty 해제·Enter 적용이 레거시 및 사용자 확정과 일치함.
- 품목별 정보 편집은 현재 로그인 지점·메인 선택 label size만 사용하고 미선택 시 빈 상태이며, 저장·Enter 후 committed 기준·dirty를 갱신한 채 dialog를 유지함. 영양성분 keyword는 삭제 후 재번호하지 않음.
- 영양성분 형식·표의 삭제 확인, 저장 후 입력 dialog 유지, 영양성분표 신규 형식 미선택과 수정 후 선택·preview 복원이 레거시와 일치함.

### Phase 5. 숨김 command 최종 검증

1. 거래게시판, 공용라벨 이력, 이더넷 설정을 inventory에 유지한다.
2. 고정 항목 관리를 `legacyUnreachable`과 두 legacy ID의 불일치 사유와 함께 inventory에 유지한다.
3. `legacyInactive`와 `legacyUnreachable` command는 production 메뉴에서 숨김 상태를 유지한다.

완료 기준:
- `legacyInactive`와 `legacyUnreachable` command가 production 메뉴에 노출되지 않음.

## 9. 테스트 지시

### 9.1 순수 unit test

- `test/app_menu_policy_test.dart`
  - runtime command inventory와 상태 policy.
  - 대표 권한 경계와 로그인/편집/busy/`legacyInactive`/`legacyUnreachable` 상태.
- 포팅 대조용 inventory fixture test
  - 레거시 handler와 legacy command ID 대응 관계.
  - 레거시 `IDM_SETUP_PRINT`에서 분리한 프린터 설정 1→2 command mapping.
- 해당 policy focused test에서 세 legacy command policy의 차이, 각 command mapping, 사용자 관리와 지점 관리의 `MANAGER_USER` 숨김, `품목별 정보 편집`·`전자저울 관리`·`검색출력모드`·`검색출력 설정`·`업데이트 메시지`의 `loggedIn`, 이더넷 설정의 `legacyInactive`, 고정 항목 관리의 `legacyUnreachable` 상태와 production 숨김을 검증한다.
- 로그인·로그아웃 focused test에서 `SYSTEM` 직접·시스템 비밀번호와 시스템 관리자 자기 비밀번호는 `IsFirstConnectByAdmin`이지만 master-key가 아니고, 비시스템 사용자의 레거시 직접 비밀번호만 master-key인지 검증한다. 일반·`IsFirstConnectByAdmin` 로그인은 `LOGIN` 1회, master-key 로그인은 0회인지 확인한다. 일반 명시적 로그아웃은 `LOGOUT` 1회, master-key·관리자 `Connect` 중 명시적 로그아웃은 0회, 일반·관리자 `Connect` 중 앱 종료는 1회, master-key 종료는 0회인지 확인한다. 이력 저장은 session 초기화 전 값을 사용하고 오류 1회 뒤에도 로그인 초기화 또는 로그아웃·종료 정리를 계속하며, 자동 재시도와 종료 요청·detached 중복 기록은 0회인지 검증한다.
- 해당 owning controller focused test에서 저울 연결 설정과 라벨출력/저울출력 프린터 설정의 공개 command가 기존 private owning method에 한 번 위임하는지 검증한다. 두 프린터 command의 로그인·owner busy 조건, 라벨출력 command에 label size 조건이 없는 것과 저울출력 command만 `_effectiveLabelSize`를 요구하는 것을 함께 검증한다.
- 품목별 정보 편집 focused test에서 로그인 사용자 전체의 command 표시, 현재 로그인 지점·메인 선택 label size 목록, dialog 내부 selector 부재와 브랜드·label size 미선택 시 DAO 0회·빈 목록·저장 비활성, 레거시 출력 보정 field의 활성 조건과 일괄 저장, 성공 후 필요한 `ItemOfMarket` 목록 갱신·committed 기준·dirty 해제와 dialog·행 선택·scroll 유지, 저장 command가 활성일 때 Enter 저장, dirty/busy 종료 차단을 검증한다. 이 command에 `F1` shortcut metadata나 실행 mapping을 추가하지 않는다.
- 영양성분 형식 focused test에서 형식명 필수, 구성 행 1개 이상, 빈 구성의 `N01`과 마지막 표시 keyword 기준 증가, 중간 행 삭제 후 재번호하지 않는 동작, parent/detail transaction과 형식 삭제의 연관 경고 1회·연관 삭제를 검증한다. 저장 button과 dialog-level Enter 모두 저장하고 입력 dialog와 같은 신규·수정 상태가 유지되며 신규 저장을 다시 실행할 수 있는지 확인한다. 정렬 command, 별도 중복 검증, 자동 수정 mode 전환을 추가하지 않는다.
- 영양성분표 focused test에서 형식·명칭·RTF·폭 CRUD, 신규 형식 미선택·수정 시 저장 형식 선택, 명칭·폭·형식 검증, 일반 삭제 확인 1회와 선택 행 RTF read-only preview를 검증한다. 저장 button과 dialog-level Enter 모두 저장하고 입력 dialog와 같은 신규·수정 상태가 유지되는지, 수정 dialog를 닫은 뒤에만 기존 행 선택·preview가 복원되고 추가·삭제 후에는 복원되지 않는지 확인한다.
- 검색출력 mode focused test에서 `F12`와 menu toggle이 같은 controller state를 사용하고 tab/button 상태가 전환되는지 검증한다. 빈 입력은 조회·발행 0건, 검색 결과 0건은 발행 0건과 각 오류 1회인지 확인하고, 현재 label size의 품명 또는 `RICH_SEARCH_PRINT` column 완전 일치 첫 행 1건만 기존 pipeline으로 즉시 발행하며 모든 처리 시도 후 입력을 비우는지 검증한다.
- 관리 dialog focused test에서 command visibility와 selector 활성 상태를 분리해 거래처·지점·사용자 관리의 대표 권한 경계를 검증한다. 지점 관리는 현재 로그인 협력업체·거래처 초기값과 `IsManager`의 두 selector 잠금, `IsCoopAdmin`의 협력업체만 잠금, `IsSystemAdmin`·`IsAdminConnect`·`IsCoopAdminConnect`의 두 selector 활성화를 확인하되 `MANAGER_USER` command 접근을 허용하지 않는다.
- 관리 dialog focused test에서 협력업체·거래처·지점·사용자 목록의 유효 행 더블클릭이 선택 후 수정 입력을 열고 header·빈 영역은 무시하는지 확인한다. 네 manager 목록과 관리자 복사의 dialog-level Enter는 무동작이고, 각 추가/수정 입력은 Enter로 적용해 검증 성공 시 닫힌 뒤 parent 저장·목록 갱신으로 이어지는지 검증한다. 사용자 관리 검색 입력의 Enter는 이름 검색만 실행한다.
- 관리 dialog focused test에서 네 child 입력의 취소는 parent validation·DML·reload·성공 안내 0회인지, 적용은 child 종료 후 필요한 parent validation과 본체 DML 최대 1회인지 확인한다. parent validation·DML 실패는 manager·기존 목록 유지, 오류 1회, child 재개방·draft 복구 0회이고 성공 때만 reload·선택 해제·화면별 command 상태 재계산·성공 안내 1회인지 검증한다. 지점 transaction은 commit 전에 reload·성공 안내를 실행하지 않는다.
- 관리 dialog focused test에서 별도 수정 button·context command가 없고 협력업체·거래처의 추가 상시 사용, 거래처 삭제·Connect의 미선택 안내, 지점의 거래처 context별 추가·삭제 활성과 미선택 삭제 안내를 검증한다. 사용자 관리는 유효 지점·`전체 표시`에 따른 추가 활성과 선택별 삭제·Connect 활성, reload 후 오래된 row index·Connect 상태 제거를 확인한다.
- 관리 dialog focused test에서 거래처 관리의 협력업체 변경은 거래처 reload·선택 해제, 지점 관리의 협력업체 변경은 거래처 재조회 후 자동 선택 없이 지점 clear·추가/삭제 비활성, 거래처 선택은 지점 reload·command 활성인지 검증한다. 사용자 관리의 협력업체 변경은 거래처만 남기고 지점·사용자 clear, 거래처 변경은 첫 반환 지점·사용자 조회 또는 지점 없음의 빈 목록·추가 비활성, `전체 표시` on/off는 현재 협력업체·지점 범위와 selector·추가 상태를 각각 적용하는지 확인한다.
- 협력업체·거래처·지점 관리 focused test에서 삭제 전 공용 시스템 비밀번호 계산과 각 1회의 확인을 검증하고, 사용자·영양성분 삭제에는 이 gate가 추가되지 않는지 확인한다.
- 협력업체·거래처·지점 관리 focused test에서 협력업체 ID·이름과 거래처·지점 이름의 빈 값·중복 검증을 새로 추가하지 않고 거래처 이름에만 50자 제한을 적용하는지 확인한다. 협력업체 관리는 ID·이름 추가/수정과 목록 refresh, 기존 FK cascade를 사용하는 단일 삭제를 검증하며 저장 후 로그인 session context reload나 강제 로그아웃은 검증 대상에 추가하지 않는다.
- 지점 관리 focused test에서 지점 추가와 기존 품목별 `ItemOfMarket` 일괄 생성, 지점 삭제 전 `ItemOfMarket` 삭제의 statement 순서와 transaction을 검증한다.
- 거래처·사용자 관리 focused test에서 원래 관리자를 한 번만 보존하고 거래처 Connect의 `(true, false)`/`(false, true)`와 사용자 Connect의 `(true, true)`/`(false, true)` mapping을 각각 검증한다. 거래처 Connect는 첫 반환 지점의 첫 grade 2 사용자를 선택하고 지점 또는 대상 사용자가 없으면 연결 플래그·context·화면 초기화·접속 로그가 모두 0건인지 확인한다. 성공 시에만 사용자·지점·거래처·협력업체 context와 관리자 접속 로그를 한 번 반영한다.
- 사용자 관리 focused test에서 ID·비밀번호·확인·이름 필수, 비밀번호 일치, ID 중복, 생성·수정 등급과 신규 기본값, `IsFirstConnectByAdmin`에 따른 ID·비밀번호 column 표시, 이름 순환 검색과 일반 삭제 확인을 검증한다. 관리 row의 grade와 `RICH_PWD` 문자열은 DB 값을 변환 없이 decoding·저장하며 로그인 전용 `clientUserTestOverrideId`가 적용되지 않는지 확인한다.
- 관리자 복사 focused test에서 시스템 관리자만 협력업체를 변경하고 두 복사 분기가 사용하는 selector와 checkbox event 순서를 검증한다. 일반 label size 복사의 기존 column 삭제 범위, 대상 ID·이름 유지와 유효 `RICH_FORM_SHEET`·column 복사, 브랜드 복사의 단일 `DbTransactionStatement` 안 `OUTPUT INSERTED`·table variable 기반 브랜드·전체 label size ID mapping을 확인한다. 내부 transaction 제어문, 동적 statement callback, `SelectLastInsert`, XML payload가 없는지 검증한다. 데이터 손실 경고 취소와 대상 첫 지점·프로시저 capability 실패는 DML 0건인지, `품목까지 복사`는 첫 지점으로 세 프로시저를 정해진 순서·parameter로 호출하는지, 성공 후 기존 owner reload/clear가 한 번씩 실행되는지 검증한다. 레거시가 실행·복사하지 않는 연관 table test를 추가하지 않는다.
- 업데이트 메시지 focused test에서 `loggedIn` 표시와 비로그인 숨김, 현재 사용자별 단일 row 조회, 실제 등급별 편집 범위와 대상 우선순위를 검증한다. 선택 사용자는 현재 협력업체 범위이고 다중 선택은 하나의 transaction이며, checkbox 해제 시 ID가 비워지고 선택 mode 0건은 DML 없이 중단되는지 확인한다. 시스템 관리자의 전체 `UN_STATE=2`·현재 협력업체 `UN_STATE=0`, 협력업체 관리자의 현재 협력업체 `UN_STATE=0`, 일반 사용자의 자기 `UN_STATE` 0/1·`UN_TIME`만 갱신과 세션성 연결 상태만으로 편집 권한이 확대되지 않는지 검증한다.
- 업데이트 메시지 focused test에서 저장 button과 dialog-level Enter가 같은 등급별 저장 command를 한 번 실행하고 성공 후 dialog를 닫는지 검증한다.
- 검색·치환 focused test에서 기본 `품명`, 브랜드·label size filter 해제·비활성, 검색어와 활성 selector 필수 조건, AppBar의 무자동 조회와 기존 메인 검색값의 1회 자동 조회를 검증한다. 현재 거래처의 `품명`·`주원료` 부분 일치와 선택적 브랜드·label size 범위, 실제 등급이 non-editable일 때 쓰기 UI 숨김·검색/이동 유지, 변경 행 저장을 확인한다. 품목편집 이동은 현재 선택 행 1건을 사용하고, 출력 이동은 체크 1건 이상·동일 브랜드/label size를 요구해 첫 체크 context와 전체 체크 ID를 사용하며, 두 이동은 미저장 변경을 확인·저장 없이 폐기하는지 검증한다. 주석 처리된 `추가 주원료` 검색은 inventory에 추가하지 않는다.
- 발행내역 focused test에서 현재 협력업체·거래처와 `품명` 초기값, 시스템 관리자·협력업체 관리자·그 외 사용자의 selector 표시 범위를 검증한다. 선택 협력업체가 query에 쓰이지 않고 거래처 `[전체 보기]`의 내역·총 누계·기간 합계가 전 협력업체 범위인지, 특정 거래처의 `품명`·`사용자ID`만 거래처 ID를 결합하는지 확인한다. `품명` 빈 값의 `LIKE '%%'`, `사용자ID` 빈 값 완전 일치, `거래처` 입력의 selector 표시명 초기화·빈 값 대체와 이름 완전 일치를 검증한다. `RICH_DATE_YYYYMMDD BETWEEN`의 양끝 포함과 역전 범위 무보정을 확인한다. 총 누계·기간별·단일 거래처의 0이 아닌 label size 합계가 별도 query이며 검색 종류·검색어를 적용하지 않는지, 0건에도 총 누계·기간별 합계가 유지되는지 검증한다. 합계행은 `[총 누계]` `Color(0xFFE6B9B8)` → `[기간별 합계]`·`[라벨사이즈별 합계]` `Color(0xFFD7E4BC)` → 실제 결과 순서이고 상세 command가 없는지 확인한다. 결과의 `RICH_DATETIME ASC`와 동일 시각 무정렬, button·검색 입력·dialog-level Enter의 동일 조회 1회를 검증한다. 실제 결과 row는 read model을 직접 보유하고 상세는 같은 column 순서의 저장값·출력값 두 표와 다른 출력값 행의 `Color(0xFFFFC000)` 배경만 검증한다. 출력 경로 구분이나 pagination이 없는 것은 별도 기능 test로 확대하지 않고 query/model 계약에서 확인한다.
- 사용자 접속 이력 focused test에서 현재 협력업체·거래처 초기값, 시스템 관리자의 두 selector 표시·변경, 협력업체 관리자의 협력업체 selector·label 숨김과 거래처 selector만 표시·변경, 그 외 사용자의 두 selector·관련 label 숨김과 현재 거래처 조회를 검증한다. `LOGIN_DATE_YYYYMMDD BETWEEN`의 양끝 포함·역전 범위 무보정, `LOGIN_DATE ASC`와 동일 시각 무정렬, `LOGIN(0)`의 `로그인`·그 외 `로그아웃` 표시를 확인한다. dialog-level Enter는 조회·닫기 0회이고, 0건에서는 빈 table과 `검색결과가 없습니다!` 안내 1회인지 검증한다.
- 데이터내용 이력 focused test에서 현재 협력업체·거래처 초기값, 시스템 관리자의 무효 협력업체 selector와 선택 거래처 조회, 협력업체 관리자의 표시되지만 무시되는 거래처 selector와 현재 거래처 조회, 그 외 사용자의 현재 거래처 조회를 구분해 검증한다. `SAVE_DATE_YYYYMMDD BETWEEN`의 양끝 포함·역전 범위 무보정, `SAVE_DATE ASC`와 동일 시각 무정렬, `SAVE_STATUS_NEW(0)`의 `신규`·그 외 `수정` 표시를 확인한다. dialog-level Enter는 조회·닫기 0회이고, 0건에서는 빈 table과 `검색결과가 없습니다!` 안내 1회인지 검증한다. 행 더블클릭 상세는 `CONTENT_COLUMNS`와 `CONTENTS`를 줄바꿈으로 분리해 짧은 쪽 개수까지만 index pairing하고 before/after 보정을 추가하지 않는지 확인한다.
- 발행 통계 focused test에서 현재 협력업체·거래처, 브랜드·label size `[전체 보기]`, 검색 column `주원료` 초기값과 시스템 관리자만 협력업체·거래처 selector를 변경하는 권한을 확인한다. `SelectDLG`의 `RICH_DATE_YYYYMMDD BETWEEN` 양끝 포함·역전 범위 무보정과 거래처·브랜드·label size·품목명·검색 column/값·완전/부분 일치 조건, 부분 일치 기본값과 활성 결과 column을 검증한다. 결과의 `RICH_DATETIME ASC`와 동일 시각 무정렬, button·dialog-level Enter의 동일 조회 1회를 확인한다. 조회 결과의 `RICH_PRINT_COUNT`로 총 발행 매수·삭제 품목 매수를 계산하고 0건에도 두 요약행을 0으로 유지한다. 총합 행 `Color(0xFFD7E4BC)`, 삭제 합계·결과 행 `Color(0xFFF7EB00)` 배경만 적용하며 두 요약행에는 상세 command가 없는지 확인한다. 실제 결과 row는 status ID를 직접 보유하고 상세가 품명·주원료와 column 이름·저장일·값만 표시하는지 검증한다. 사용자 등급 decoding/filter나 상태 UI, `SelectPrintCountSum` 계열 합계 query test를 추가하지 않는다.
- 검색출력 설정 focused test에서 현재 브랜드·label size와 DB `RICH_SEARCH_PRINT` 초기값, 브랜드 변경 시 `RICH_LABELSIZE_ORDER ASC` 첫 label size 선택, column의 개별 선택·전체 선택·전체 해제와 적용을 검증한다. 적용은 조회한 전체 column snapshot으로 `BM_RICH_COLUMN` 전체 속성, `BM_RICH_COL_MIN`, column type별 `BM_GS1_COLUMN_INFO`·`BM_GS1_CONTAIN_COLUMN`을 레거시 `CColumnDAO::UpdateBatch`와 같은 분기·범위의 한 transaction에서 처리하는지 확인하고 검색 flag 최소 UPDATE로 축소하지 않는다. 성공 안내 1회, committed 기준 갱신·dirty 해제와 dialog·selector·checkbox 유지, Enter 및 변경 없는 재적용의 전체 batch 실행을 검증한다. 미적용 draft가 있는 selector 전환·닫기는 공용 폐기 확인을 한 번 사용하고 취소 시 현재 상태를 유지하며 승인 시 자동 저장 없이 대상 DB 값으로 초기화하거나 닫는지 확인한다.
- 네 조회 dialog focused test에서 시작일·종료일이 현재 로컬 날짜이고 command별 selector 초기화만으로 DAO 조회가 실행되지 않으며 명시적 조회 command에서 최초 호출되는지 확인한다.
- command policy test에서 발행내역, 사용자 접속 이력, 데이터내용 이력, 발행 통계, 품목별 정보 편집, 전자저울 관리, 검색출력모드, 검색출력 설정과 업데이트 메시지가 `loggedIn`이고 비로그인에는 숨겨지는지 검증한다.
- 단축키 차단 helper를 별도로 추가한 경우 차단 surface 활성/중첩/종료와 non-modal preview 제외 결과만 검증한다. route identity나 lease 내부 구조는 테스트 계약으로 고정하지 않는다.
- 각 신규 model/DAO codec test.
- 공용 `DbClient.transaction`의 `BEGIN TRANSACTION`, `COMMIT`, 가능한 rollback, 원래 오류 보존과 `DbCommitOutcomeUnknown` 변환은 기존 `test/db_transaction_test.dart`와 `test/db_client_test.dart`에서 한 번 검증한다. 신규 DAO test에서 이 제어 SQL 문자열을 중복 검사하지 않는다.
- 신규 DAO transaction test는 `DbTransactionStatement` 순서, SQL·parameter, command 계약상 정확한 영향 행을 요구하는 본체 DML과 결과 decoding을 확인한다. 선택적 연관 삭제·상태 갱신의 0건을 실패로 만들지 않고, server-side SQL batch가 자체 rowcount 검증에 `THROW`를 사용하는 경우에만 해당 batch의 `THROW`와 오류 전달을 검사한다. 기존 저장 프로시저의 미확인 rowcount·반환값 test를 추가하지 않는다.
- compatibility 100 금지 함수 미사용은 SQL을 추가하거나 변경한 DAO에만, XML의 한글, null, 빈 문자열, `& < > ' "` 처리는 XML payload codec을 사용하는 기능에만 검증한다.

### 9.2 widget test

- `test/app_menu_bar_test.dart`
  - 넓은 화면 그룹 아이콘 3개.
  - 좁은 화면 overflow 1개.
  - overflow 첫 단계의 그룹 3개와 2단계 command submenu/panel, 좌우키 진입·복귀와 단계별 Esc.
  - 넓은 폭과 좁은 대표 폭에서 높이 clamp, scroll, title ellipsis와 주요 command 접근성.
  - 메뉴 collapse 후 긴 title의 한 줄 ellipsis와 서버 상태/로그인 command 유지.
  - Tab focus, Enter/Space 열기, 방향키 이동, Enter 실행, 메뉴 취소 시 그룹 focus 복원과 dialog 실행 시 내부 초기 focus 인계.
  - 메뉴가 열린 동안 `HardwareKeyboard` 화면 전역 F1/F2/F3/F5/F12 command 차단, 닫힌 뒤 기존 단축키 복원.
  - popup을 닫은 뒤 owning command가 한 번 실행되고 dialog command는 내부 focus로 인계됨.
  - 비로그인·저권한 상태에서 종료만 남아도 `파일/관리`가 중립적인 `Icons.menu`로 표시.
  - separator와 command label.
  - popup이 command의 간결한 한글 기본 label을 그대로 표시함.
  - `검색출력모드`의 `F12`만 명령명과 분리된 오른쪽 보조 영역 및 semantics에 표시되고, 기존 품목관리 탭 shortcut인 `F1`은 `품목별 정보 편집`에 표시·연결되지 않음.
  - 권한 부족·`legacyInactive`·`legacyUnreachable` command 숨김과 transient dirty/busy command의 inline disabled reason 표시. 문구를 실제로 줄이는 renderer만 전체 tooltip을, 기본 text semantics가 부족한 custom renderer만 별도 semantics를 검증한다.
  - 로그인/로그아웃 command는 inventory에 존재하지만 `파일/관리` popup에는 렌더링되지 않고 현재 상태의 AppBar 고정 아이콘 하나만 표시.
  - 대표 command를 통해 별도 command ID·label 렌더링과 주입된 검색출력 mode 상태에 맞는 checkmark 표시.
  - 서버 상태와 로그아웃 버튼 위치 유지.
- 공용 dialog test에서 `BlockingModelessDialog`의 배경 입력 차단, 첫 주요 입력 focus, Esc와 중첩 picker z-order를 한 번 검증한다. dirty와 write-busy는 이를 지원하는 편집 dialog에서, read-busy 중 닫기와 늦은 결과 폐기는 대표 읽기 전용 dialog에서 검증한다. 각 command test는 해당 화면의 filter, draft, save/cancel, 실패 상태처럼 고유한 흐름만 추가한다.
- 대표 route dialog, AppBar menu, `BlockingModelessDialog`, `DbReconnectOverlay`에서 전역 F-key 차단을 검증하고, 중첩 surface가 남으면 차단이 유지되며 마지막 surface 종료 후 복원되는지 확인한다. non-modal floating preview만 보일 때는 차단하지 않는다.
- 로그아웃·종료 test에서 신규 dialog 또는 기존 메인 탭의 write busy와 active editing은 dirty 확인 전에 종료를 차단하고, 양쪽 owner의 여러 dirty는 한 번만 확인하며, 승인된 폐기 callback은 각각 한 번만 실행되고 취소 시 상태가 유지되며, clean 상태는 일반 확인 없이 진행되는지 검증한다.
- 대표 저장 dialog test에서 commit 전 확정 실패는 draft를 유지하고, `DbCommitOutcomeUnknown`과 commit 후 reload 실패는 안내 후 dialog를 닫으며 같은 draft의 재저장 UI나 별도 읽기 전용·수동 재조회 mode를 만들지 않는지 검증한다.
- 사용자 접속 이력 focused test에서 demo row·route page 없이 차단형 dialog content와 read-only `FortuneTable`, overlay date picker를 사용하는지 검증한다.
- 반복 행 화면은 `FortuneTable`을 사용한다. 공용 API를 확장한 경우에만 `test/fortune_table_test.dart`에 해당 동작과 관련 회귀를 추가한다.
- `legacyInactive`와 `legacyUnreachable` command가 production 메뉴에 렌더링되지 않는지 검증한다.

### 9.3 integration test

- AppBar 메뉴 → 기존 핵심 탭 또는 owning 설정 dialog 연결, 신규 관리·조회 command → 차단형 overlay dialog 표시.
- 편집 dirty 중 context 변경 command 차단.
- 실제 server integration은 기본 test 실행에서 제외하고 명시적 opt-in 설정이 있을 때만 실행한다. 조회 DAO는 데이터 변경 없는 read-only 경로만 검증한다.
- CRUD integration은 대상이 비운영 테스트 DB임이 명시적으로 식별되고, 전용 ID 범위·초기 상태·정리 절차가 준비된 경우에만 실행한다. 이 조건 중 하나라도 없거나 운영 DB 여부를 판별할 수 없으면 실행하지 않는다.
- 테스트를 위해 운영 DB row·권한을 수정하거나 테스트 table을 생성하지 않는다. 실제 server integration의 대상과 실행 명령은 `SESSION_HANDOFF.md`에 기록한다.
- commit·rollback 실패 simulation은 fake driver 기반 unit test에서 수행한다. 실제 server integration은 정상 transaction 경로만 검증한다.

### 9.4 회귀 검증

기능별 focused test와 함께 실제로 재사용하거나 수정한 owner의 기존 suite만 실행한다. AppBar shell 변경은 `app_menu_bar`와 사용자 policy test를, `FortuneTable` 공용 API 변경은 `fortune_table_test.dart`를, 기존 출력·설정 command 연결 변경은 해당 session/widget test를 회귀 범위로 선택한다. 변경하지 않은 전체 suite를 일률적인 완료 조건으로 두지 않는다.

## 10. 명령별 완료 체크리스트

각 command는 아래 항목 중 해당 command의 성격과 실제 구현 범위에 적용되는 항목을 충족해야 완료다. 비해당 항목을 충족하기 위해 별도 UI, DB 접근 또는 transaction을 추가하지 않는다.

- [ ] 레거시 handler/dialog/model/DAO 확인.
- [ ] DB를 사용하는 command는 실제 기존 DB schema 확인.
- [ ] command ID/label/group과 optional icon/`shortcutLabel` 등록.
- [ ] 해당 command의 권한·로그인·`legacyInactive`·`legacyUnreachable` 또는 busy/dirty 조건 확인.
- [ ] 기존 기능이면 owning command 재사용.
- [ ] 미구현 기능이면 필요한 model/DAO와 `BlockingModelessDialog` 기반 화면 구현.
- [ ] 기존 핵심 탭 이동 외에 별도 route page, 신규 메인 탭, native child window를 추가하지 않음.
- [ ] 신규 dialog를 여는 command는 주요 입력, Esc, 오류와 종료 흐름이 공용 계약을 따르고, 편집·저장이 있는 경우에만 실제 dirty/write-busy/active editing 상태를 처리함.
- [ ] 관리 목록 또는 조회 결과에 반복 행 table이 있는 command는 공용 `FortuneTable`을 사용함.
- [ ] 행 CRUD command는 작업 빈도와 현재 owning 화면에 맞는 toolbar/rail/context menu를 사용하고 비CRUD dialog에는 불필요한 CRUD command가 없음.
- [ ] 조회 command는 데이터 성격에 필요한 조회 UI와 오류 흐름을 구현함.
- [ ] 명시적 draft 저장 흐름이 있는 command만 저장/취소 UI를 구현함.
- [ ] 쓰기 transaction이 있는 command는 statement·parameter·결과 decoding을 검증하고, UI는 확정 실패·`DbCommitOutcomeUnknown`·commit 후 reload 실패를 1.3 계약대로 구분함.
- [ ] DB를 사용하는 command는 DB migration을 사용하지 않음.
- [ ] focused test 통과.
- [ ] 변경한 owner의 관련 회귀 test 통과.
- [ ] `SESSION_HANDOFF.md`에 구현/검증/commit 기록.

## 11. 금지 사항

- 동작하지 않는 메뉴를 활성 상태로 먼저 노출하지 않는다.
- 좁은 화면 overflow에 모든 command를 하나의 긴 평면 목록으로 표시하지 않는다.
- 미구현 command를 snackbar `준비 중`으로 완료 처리하지 않는다.
- 레거시 INACTIVE command를 임의 활성화하지 않는다.
- 레거시 메뉴에서 handler 도달 경로가 확인되지 않은 command를 임의 활성화하지 않는다.
- schema가 없다는 이유로 앱 시작 시 table/column을 자동 생성하지 않는다.
- DB migration script를 추가하거나 실행하지 않는다.
- generic catch에서 DB 오류를 성공/빈 결과로 바꾸지 않는다.
- commit 실패를 무시하거나 자동 retry하지 않는다.
- rollback이 가능하지 않은 상태에서 억지 rollback command를 반복하지 않는다.
- 관리 기능을 하나의 거대한 dialog/model/DAO에 합치지 않는다.
- HomePage와 HomePageManager에 같은 업무 command를 중복 구현하지 않는다.
- 메뉴 widget에서 직접 SQL을 실행하지 않는다.
- shortcut을 command label 문자열에 직접 합치거나, 차단 surface가 보이는 동안 화면 전역 `HardwareKeyboard` command를 실행하지 않는다.
- AppBar popup을 닫기 전에 dialog·탭 이동·toggle·종료 command를 시작하거나 command를 중복 실행하지 않는다.
- 좁은 폭을 지원하려고 서버 상태·로그인 command를 숨기지 않는다.
- 비활성 popup item의 실행 불가 사유를 hover tooltip으로만 제공하지 않는다.
- 지원하지 않는 CRUD command를 비활성 toolbar/rail button으로 표시하지 않는다.
- 같은 footer 구조가 반복되기 전에 새 공용 footer widget을 선행 추가하지 않는다.
- 즉시 적용형·단순 설정형 또는 기존 owning dialog에 footer를 억지로 추가하지 않는다.
- footer 없는 dialog에서 진행 표시 없이 닫기와 입력만 비활성화하지 않는다.
- 신규 관리·편집·조회 command를 별도 route page, 신규 메인 탭 또는 Windows native child window로 구현하지 않는다.
- `BlockingModelessDialog` 안에서 일반 `showDialog`, `showDatePicker`, `showTimePicker`로 중첩 modal을 열지 않는다.
- 같은 신규 AppBar dialog의 `OverlayEntry`를 중복으로 쌓지 않는다.
- 신규 dialog에 실제 dirty, write-busy 또는 active editing이 있는데 이를 확인하지 않고 메뉴 종료나 OS 창 닫기를 진행하지 않는다. 읽기 전용 dialog에 종료 차단 상태를 임의로 추가하지 않는다.
- HomePage나 AppBar가 `HomePageManager`의 기존 출력·연결·저장 busy, activeEditing, dirty private 상태를 각각 복제해 판정하거나 clean 로그아웃에 일반 확인창을 추가하지 않는다.
- 차단형 overlay가 열린 동안 뒤쪽 `ScaffoldMessenger` snackbar로 오류나 사용자 action을 요구하지 않는다.
- 관리·조회용 반복 행 table을 `DataTable`, `Table`, row별 `ListView` 또는 기능별 자체 table widget으로 새로 구현하지 않는다.
- 테스트를 위해 운영 사용자 권한이나 운영 DB row를 수정하지 않는다.
- 비운영 DB 식별, 전용 ID 범위와 정리 절차 없이 실제 server CRUD integration을 실행하지 않는다.
- 사용자 소유 변경 `lib/core/app.dart`를 이 작업에 포함하지 않는다.
- release build, installer, 원격 push는 명시 요청 없이 수행하지 않는다.

## 12. 최종 완료 상태

전체 포팅 완료 시 다음 상태여야 한다.

- AppBar에는 폭에 따라 그룹 아이콘 3개 또는 overflow 메뉴 1개가 표시된다.
- 기존 desktop window 초기화 동작을 변경하지 않고 넓은 폭과 좁은 대표 폭에서 AppBar와 핵심 탭의 주요 command에 접근할 수 있다.
- 통합 overflow는 3개 그룹의 2단계 구조와 화면 높이 clamp·scroll·키보드 reveal을 제공한다.
- AppBar title은 메뉴 collapse 후에도 공간이 부족하면 한 줄 ellipsis되고 서버 상태와 현재 로그인/로그아웃 command는 유지된다.
- AppBar menu는 키보드만으로 열기·이동·실행·닫기가 가능하다. 메뉴만 취소하면 원래 trigger에 focus가 복원되고 top-level dialog를 실행하면 dialog 내부로 focus가 인계된다.
- AppBar menu는 간결한 한글 기본 label을 표시하고 실제 활성 shortcut을 오른쪽 보조 영역과 semantics에 표시한다.
- AppBar menu와 차단형 dialog가 보이는 동안 전역 F1/F2/F3/F5/F12의 배경 command 실행을 막고 마지막 차단 surface가 닫힌 뒤 복원한다. non-modal floating preview만으로는 차단하지 않는다.
- AppBar menu command는 popup이 닫힌 뒤 정확히 한 번 실행되며 새 dialog를 열면 첫 주요 입력으로 focus를 넘긴다.
- 서버 상태와 로그인/로그아웃 영역은 기존 위치와 동작을 유지한다.
- 파일/검색/설정의 모든 레거시 command가 inventory와 권한 policy에 존재한다.
- 현재 동작 기능은 중복 구현 없이 같은 command로 연결된다.
- 미구현 기능은 실제 `BlockingModelessDialog` 기반 화면, model/DAO, DB 계약과 테스트를 갖는다.
- 기존 핵심 탭 이동과 기존 owning 설정 dialog를 제외한 신규 관리·편집·조회 화면은 공용 차단형 overlay dialog로 일관되게 열린다.
- 신규 AppBar dialog는 owning controller가 동일 dialog 중복 실행과 close를 관리하고, 실제 편집·저장 상태가 있을 때만 exit snapshot을 제공한다. 종료 요청의 dirty 요약·폐기 callback 수집 외에 서로 다른 dialog를 위한 전역 coordinator는 추가하지 않는다.
- 로그아웃·메뉴 종료·OS 창 닫기는 신규 dialog와 기존 메인 탭의 exit snapshot을 함께 평가한다. 읽기 전용 dialog는 닫고 늦은 결과를 폐기하며, write-busy와 activeEditing은 dirty 확인 전에 종료를 막고 모든 dirty 상태는 한 번의 통합 확인으로 처리한다. clean 상태에는 일반 확인창을 추가하지 않는다.
- 차단형 dialog의 모든 중첩 modal/picker는 현재 dialog 위에 표시되고, form-only dialog도 첫 활성 입력에 초기 focus가 설정된다.
- 모든 관리 목록과 조회 결과의 반복 행 table은 공용 `FortuneTable`을 사용한다.
- 명시적 draft 저장 흐름이 있는 신규 관리 dialog는 기존 frame footer slot에 필요한 command를 구성하고, 즉시 적용형·단순 설정형과 기존 owning dialog는 필요한 command 영역만 유지한다. 행 CRUD command는 작업 빈도와 owning 화면에 맞는 toolbar, rail 또는 context menu를 사용한다.
- footer 없는 busy 상태는 실행 control 가까이에 표시되고, 차단형 overlay 오류는 inline 또는 공용 overlay dialog로 전달된다.
- `legacyInactive`와 `legacyUnreachable` command는 inventory에 사유와 함께 유지하되 production 메뉴에서는 숨기며 활성화 요구나 실제 도달 경로 확인 전에는 내부 구현을 완료 조건으로 삼지 않는다.
- DB schema는 변경되지 않는다.
- 저장 오류는 호출자에게 전달되고 가능한 transaction만 rollback된다. `DbCommitOutcomeUnknown`과 commit 후 reload 실패는 같은 draft를 재저장하지 않도록 안내 후 dialog를 닫고, 다시 열 때 최신 데이터를 조회한다.
- 과도한 fallback, retry, 자동 보정 없이 레거시 업무 동작과 현 UI 구조가 일관되게 결합된다.
