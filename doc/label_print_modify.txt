# 라벨출력 탭 구현 작업지시서

## 0. 최우선 원칙

- 이 문서는 `doc/label_print.txt`를 구체화한 구현 기준이다.
- 동작 기준은 레거시 `.tmp/LabelManager`의 라벨출력과 현재 Flutter 앱의 품목관리 미리보기·공용라벨 인쇄 구현이다.
- 보완·예외 처리를 과도하게 추가하지 않는다. 실제 화면, 출력, 자동증가와 발행 이력에 필요한 model, DAO, 공용 API만 작성한다.
- 현재 LabelSheet/FortuneSheet workbook을 유일한 렌더 원본으로 사용하고 별도 RTF·문자열 인쇄 엔진을 만들지 않는다.
- 현재 `pubspec.yaml`의 hosted `printing` package와 기존 `RawPrinterWin32`, PDF/EZPL builder를 사용한다. package local fork, Windows native plugin 개조, spooler job 추적 기능을 이번 작업에 추가하지 않는다.
- SQL은 parameterized query와 기존 `DbClient.transaction()` 경계를 사용한다. SQL Server 오류는 사용자에게 전달하고 활성 transaction은 가능한 경우 rollback한다.
- SQL Server table, column, index, trigger, procedure를 생성·변경·삭제하지 않고 data/schema migration도 구현하지 않는다.
- 출력 journal, checksum, process crash 복구, 관리자 DB 판정, 앱 재시작 후 출력 복원은 이번 범위에 포함하지 않는다.
- 같은 앱 instance의 중복 발행만 메모리 busy 상태로 차단한다. 여러 client/PC의 동시 자동증가 발행과 번호 예약은 지원하지 않는다.
- 이번 기능은 Windows desktop 전용이다. 다른 platform에는 라벨출력 탭과 발행 command를 노출하지 않는다.
- 배포 build, installer 생성과 원격 push는 별도 요청이 있을 때만 수행한다.

이 문서에서 `accepted`는 앱이 호출한 출력 API가 오류 없이 완료된 상태다. PDF는 `Printing.directPrintPdf()`가 `true`를 반환한 경우, RAW는 `RawPrinterWin32.sendRaw()`가 exception 없이 완료된 경우다. 이는 실제 용지 출력 완료나 이후 printer 오류까지 보장한다는 뜻이 아니다.

## 1. 구현 목적

- 메인 `라벨출력(F3)` placeholder를 실제 라벨 발행 화면으로 교체한다.
- 품목관리에서 `발행`이 체크된 품목을 출력 작업 목록으로 가져온다.
- 출력 작업 행을 선택하면 오른쪽에 해당 품목의 실제 출력 workbook을 자동 표시한다.
- 행별 발행매수와 레거시 출력 override를 적용해 여러 품목을 한 번에 발행한다.
- 공용라벨관리의 프린터 설정과 현재 PDF/EZPL 출력 엔진을 공용화한다.
- 실제 `accepted`된 unit만 자동증가 값과 기존 발행 이력 table에 반영한다.

## 2. 레거시 동작 기준

### 2.1 출력 작업 목록

품목관리의 발행 체크 행만 출력 목록으로 가져온다. 출력 작업 표는 다음 11개 항목을 가진다.

1. 발행매수
2. 라벨크기명
3. 품명
4. 출력 폭
5. 출력 높이
6. 왼쪽 여백
7. 오른쪽 여백
8. 위쪽 여백
9. 왼쪽 밀기
10. 위쪽 밀기
11. 줄간격

행 기본값은 `BM_ITEM_OF_MARKET`의 사용 설정을 우선한다. item override를 사용하지 않는 항목은 현재 라벨크기와 공용 프린터 설정을 사용한다. 출력 화면에서 바꾼 값은 현재 출력 session에만 적용하고 품목관리 원본에 저장하지 않는다.

발행매수 `0`은 개별 행에서 허용한다. 전체 매수 합계가 `0`이면 발행하지 않는다. 음수와 정수가 아닌 값은 허용하지 않는다.

### 2.2 선택과 미리보기

- 출력 목록을 만든 뒤 첫 행을 선택한다.
- 선택 행이나 출력 설정이 바뀌면 별도 버튼 없이 미리보기를 갱신한다.
- 오른쪽 미리보기는 읽기 전용이며 품목관리 `출력내용 미리보기`와 같은 materializer를 사용한다.
- 이미 화면에 실제 출력 미리보기가 있으므로 별도 `미리보기` 버튼은 두지 않는다.

### 2.3 검색

- 검색 대상은 현재 발행 체크된 출력 작업 목록이다.
- 품명 또는 `TColumn.searchPrint == true`인 column content와의 exact match만 찾는다.
- 입력값과 대상값을 trim하고 대소문자를 무시해 비교한다. 부분 검색은 하지 않는다.
- 같은 값이 여러 개면 현재 선택 다음 결과를 찾고 마지막 뒤에는 처음으로 순환한다.
- 검색은 일치 행을 선택하고 미리보기만 갱신한다. 자동 발행하지 않는다.

### 2.4 발행

- copies가 1 이상인 row를 row 순서와 copy index 순서의 `LabelPrintUnit`으로 펼친다.
- copies가 0인 row는 materialize, 출력, 자동증가와 이력에서 제외한다.
- 자동증가가 있는 row는 copy index별 값으로 materialize한다.
- 최종 physical page spec이 같은 인접 unit만 한 print group으로 묶고 unit 순서를 바꾸지 않는다.
- group을 순서대로 출력하고 한 group이 실패하거나 사용자가 취소를 요청하면 다음 group을 보내지 않는다.
- 이미 `accepted`된 group의 unit만 자동증가와 이력 저장 대상으로 삼는다.

## 3. 현재 코드 재사용 경계

### 3.1 데이터

다음 기존 데이터와 DAO를 재사용한다.

- `ItemOfMarketDAO.selectByItemOfMarketAndLabelSizeId()`
- `TColumnDAO.selectByLabelSizeId()`
- `TColumnContentDAO.selectScopedByItemIds()`
- 현재 label size, customer, market, user session
- `ItemManagerDraftController`의 현재 working value
- `LabelPrinterPreferences`
- `LabelSheetPrintOptions`, `LabelSheetPrintPageMetrics`
- `RawPrinterWin32`, `PrinterProfile`, 현재 PDF/EZPL builder

현재 label-size session에 이미 읽은 데이터가 있으면 출력 탭에서 같은 SELECT를 반복하지 않는다. label size 변경, 품목 저장, 공용라벨 저장 후에는 상위 session reload 결과로 목록과 미리보기를 갱신한다.

### 3.2 공용 API로 추출할 기능

기존 private 구현을 복사하지 말고 소유 모듈의 공용 API로 추출한다.

1. 품목 출력 workbook materialize
   - 현재 품목 미리보기의 keyword, text, barcode, image 치환을 공용 service로 이동한다.
   - 품목관리 미리보기와 라벨출력이 같은 함수와 입력 DTO를 사용한다.
2. 읽기 전용 출력 미리보기
   - 현재 품목 출력 미리보기 widget을 공용화한다.
   - 원본 workbook을 수정하지 않고 private copy를 렌더한다.
3. 프린터 설정
   - 공용라벨 설정의 preference load/save, printer picker, parsing/validation과 field widget을 공용화한다.
   - 라벨출력 설정에는 printer, left/right/top margin, left/top push, 자동 줄간격, 추가 영역과 방향을 표시한다.
   - 공용 preference에 없는 `rightMargin`, `leftPush`, `topPush`는 같은 문자열 저장 방식으로 추가하고 기존 key가 없으면 `0.0`을 사용한다.
4. 출력 dispatcher
   - 현재 공용라벨의 printer 확인, PDF fallback과 EZPL RAW 분기를 공용 service로 추출한다.
   - dispatcher는 UI를 직접 표시하지 않고 성공 여부 또는 exception을 반환한다.

## 4. 화면 구성

### 4.1 전체 레이아웃

- 상단의 기존 검색 영역을 `라벨출력` 탭에서도 표시한다.
- 본문은 왼쪽 출력 작업 표와 오른쪽 읽기 전용 미리보기로 구성한다.
- 좁은 화면에서는 두 영역이 겹치지 않도록 세로 배치 또는 탭 전환을 사용한다.
- 하단 command bar는 `[프린터 설정]  선택된 프린터명  [발행]` 순서다.
- 긴 프린터명은 말줄임과 tooltip을 사용한다.
- 발행 중에는 같은 위치를 `[발행 취소]`로 바꾸고 중복 발행을 막는다.
- Windows가 아닌 platform에서는 화면을 구성하지 않는다.

### 4.2 출력 작업 표

- 품목관리와 같은 `FortuneTable` 또는 공용 table style을 사용한다.
- 품목 추가·삭제·Excel·저장 footer는 표시하지 않는다.
- 발행매수와 숫자 출력 설정만 inline 편집한다. 라벨크기명과 품명은 읽기 전용이다.
- 품목관리 발행 체크가 바뀌면 item ID를 stable key로 목록을 동기화한다.
- 남아 있는 행의 session edit와 현재 선택은 유지한다.
- printer 설정 변경 시 item override나 session edit가 없는 fallback 값만 갱신한다.

### 4.3 미리보기

- 선택 행이 없으면 `발행할 품목을 선택하세요.`를 표시한다.
- 저장된 공용라벨 workbook이 없거나 현재 workbook으로 변환되지 않은 라벨은 저장 필요 안내를 표시한다.
- 실제 출력과 같은 크기, margin/push, 방향, 추가 영역과 줄간격을 적용한다.
- 셀 편집, 저장, 삽입과 인쇄 toolbar는 노출하지 않는다.

### 4.4 프린터 설정

- 공용 preference와 printer picker를 사용하는 라벨출력 전용 dialog를 둔다.
- 매수는 출력 작업 행이 소유하므로 dialog에 매수와 내부 발행 버튼을 두지 않는다.
- 하단에는 `[취소] [적용]`만 둔다. `적용`은 설정을 저장하고 현재 fallback 행을 갱신할 뿐 출력하지 않는다.
- 화면 하단의 `[발행]`만 실제 출력 진입점이다.

## 5. 상태와 model

### 5.1 `LabelPrintRowDraft`

최소 필드:

- stable `itemId`
- 원본 `ItemOfMarket`
- `copies`
- `widthMm`, `heightMm`
- `leftMarginMm`, `rightMarginMm`, `topMarginMm`
- `leftPushMm`, `topPushMm`
- `lineSpacingPercent`
- 각 editable 값의 `item override`/`preference fallback`/`session edited` 출처

입력 규칙:

- `copies`: 0 이상의 정수
- `widthMm`, `heightMm`: 양의 정수 mm
- margin: 0 이상의 실수 mm
- push: signed 실수 mm
- 줄간격: 30~300 정수 percent, 값이 없으면 100

### 5.2 물리 출력 계산

- 저장 workbook의 `gridClientWidthMm/gridClientHeightMm`를 source viewport로 사용한다. 없으면 라벨 저장을 요청하고 출력하지 않는다.
- source logical pixel과 mm의 기존 공용 변환을 사용한다.
- row 크기가 source 크기와 달라도 workbook content를 확대·축소하거나 text를 다시 배치하지 않는다.
- source를 row label 영역에 1:1로 배치하고 margin과 signed push를 적용한 뒤 label 영역 밖을 clip한다.
- horizontal/vertical 방향과 extra area는 기존 `LabelSheetPrintOptions`/`LabelSheetPrintPageMetrics` 계산을 공용 확장해 적용한다.
- 줄간격은 `FortuneOutputLayoutOptions` 같은 공용 출력 option으로 preview, PDF와 EZPL bitmap fallback에 동일하게 전달한다.
- DPI는 현재 `RawPrinterWin32.queryPrinterDpi()`와 `PrinterProfile` fallback을 사용한다. 별도 native DPI API를 추가하지 않는다.
- Godex EZPL은 현재 engine이 지원하는 plain-paper mode와 정밀 좌표/EZPL command/bitmap fallback 구조를 유지한다.

### 5.3 command/result

다음 immutable DTO 또는 동등 구조만 둔다.

- `LabelPrinterSnapshot`: 선택된 `Printer`와 표시용 name
- `LabelPrintCommand`: label/customer/market/user, printer, 설정, row, column/content와 saved workbook snapshot
- `LabelPrintUnit`: item ID, row index, copy index와 materialize projection. 한 unit은 한 장이다.
- `LabelPhysicalPageSpec`: PDF 또는 EZPL backend의 최종 page 크기와 필요한 DPI/media 값
- `LabelPrintJobGroup`: 순서가 연속되고 page spec이 같은 unit 목록
- `LabelPrintDispatchResult`: group과 `accepted`/`failed`, 오류
- `LabelPrintResult`: 요청/accepted 매수, group 결과와 `completed`/`partiallyAccepted`/`cancelled`/`failed`

process 복구용 manifest, generation, checksum과 별도 persistence state machine은 만들지 않는다.

### 5.4 session controller

- checked item ID와 출력 row 동기화
- item ID 기반 선택 유지
- row-local copies/출력 설정 편집
- 검색 cursor와 exact-match 선택
- 총 발행매수 계산
- 현재 앱 instance의 출력 busy와 cancellation request
- label-size session 교체 시 이전 row와 선택 폐기

## 6. workbook materialize

### 6.1 source of truth

- 현재 label-size session에 마지막으로 저장·조회된 공용라벨 workbook을 사용한다.
- 공용라벨 편집 화면의 저장 전 mutable workbook을 참조하지 않는다.
- materialize는 원본을 변경하지 않는 private copy를 반환한다.

### 6.2 치환 규칙

품목관리 `출력내용 미리보기`와 같은 공용 materializer를 사용한다.

- `#ITEMNAME`: 품명
- `#ELEMENT`: 현재 품목 working value의 주원료/함량
- `# + TColumn.keyword`: 해당 item의 최종 column value
- 일반/날짜/시간: 기존 format과 range 규칙
- 바코드/QR/GS1: 기존 `ItemCodeDataResolver` 결과
- 연결 이미지: 현재 image resolver 결과. 값이나 파일이 없으면 출력 workbook에서 제외
- 고정 이미지와 고정 바코드: 원본 workbook 값 유지
- resolver 오류: placeholder를 출력하지 않고 item/copy/column을 알린 뒤 발행 중단

미리보기는 copy index 0, 실제 출력은 각 unit의 copy index projection을 사용한다.

## 7. 실제 출력

### 7.1 group 생성

- row 순서와 copy index 순서대로 unit을 만든다.
- 각 unit을 공용 materializer와 print job builder로 page payload에 추가한다.
- backend와 최종 physical page spec이 같은 인접 unit만 한 group으로 묶는다.
- PDF group은 같은 page format의 multi-page PDF 한 건, EZPL group은 각 label이 `^P1`인 RAW payload 한 건으로 만든다.
- spec이 바뀌면 새 group을 만들고 뒤에서 같은 spec이 다시 나와도 앞 group과 합치지 않는다.

### 7.2 dispatch

- 선택 printer를 command 시작 시 snapshot하고 출력 도중 다시 선택하지 않는다.
- Godex RAW가 가능한 group은 기존 EZPL builder와 `RawPrinterWin32.sendRaw()`를 사용한다.
- file port이거나 RAW를 사용할 수 없는 group은 기존 PDF builder와 `Printing.directPrintPdf()`를 사용한다.
- PDF document page와 `directPrintPdf(format:)`은 같은 page format을 사용한다.
- PDF `false`/exception과 RAW exception은 해당 group 실패다.
- group 실패 후에는 다음 group을 보내지 않는다.
- OS 출력 call을 앱 cancellation로 강제 중단하지 않는다. call 실행 전 또는 group 사이에 취소를 확인하고, 실행 중 요청된 취소는 call 완료 후 다음 group부터 적용한다.
- 별도 timeout, spooler polling, job ID 추적과 앱 재시작 복구는 구현하지 않는다.

모든 unit을 한 OS print call로 합치지 않는다. 같은 page spec의 인접 unit만 group으로 묶어 기존 builder가 다룰 수 있는 단위로 순차 실행한다.

## 8. 데이터 저장

### 8.1 자동증가

`LabelPrintAutoIncrementDAO` 또는 동등 책임을 둔다.

- 기존 barcode/date/time helper를 사용해 원본값과 copy index에서 출력값과 다음 값을 계산한다.
- `autoInc == false`인 column은 변경하지 않는다.
- 해당 `(columnId,itemId)`의 accepted unit이 있을 때만 저장한다.
- accepted unit 중 가장 큰 copy index의 projection을 마지막 값으로 본다.
- `autoIncSave == true`이면 마지막 accepted copy 다음 값을, `false`이면 마지막 accepted copy에 출력한 값을 저장한다.
- `autoIncUpdate`는 활성 레거시 출력 결과에 영향을 주지 않으므로 저장 분기에 사용하지 않는다.
- 대상 row가 없거나 update affected row가 예상과 다르면 SQL 오류로 처리하고 transaction을 rollback한다.
- 여러 client/PC의 동시 번호 예약은 구현하지 않는다.

### 8.2 발행 이력

`LabelPrintHistoryDAO` 또는 동등 책임을 둔다.

- 기존 `BM_RICH_PRINT_LOG`, `BM_RICH_STATUS`, `BM_RICH_STATUS_DATA`만 사용한다.
- 자동증가 행은 accepted copy별 `printCount=1`, 일반 행은 accepted copies 합계 한 건으로 기록한다.
- `RICH_COLUMNS`, `RICH_PRINT_CELLS`, `RICH_SAVE_IN_DB_CELLS`는 레거시처럼 `주원료` 뒤 column order 값들을 `|`로 연결하고 trailing `|`를 남긴다. 값 안의 `|`도 레거시처럼 escaping하지 않는다.
- `RICH_PRINT_CELLS`에는 실제 출력 projection, `RICH_SAVE_IN_DB_CELLS`에는 command 시작 시점의 DB 원본값을 기록한다.
- `BM_RICH_STATUS` order는 같은 customer의 `MAX(RICH_STATUS_ORDER)` 다음 값으로 정하고 고객 이력이 없으면 1부터 시작한다.
- status ID와 detail ID는 레거시 형식과 column order를 따른다.
- 여러 client/PC의 동시 order 충돌 방지는 이번 범위에 포함하지 않는다.

### 8.3 transaction과 오류

- accepted unit의 자동증가 update, print log와 status/detail insert를 기존 `DbClient.transaction()` 한 번으로 실행한다.
- SQL은 parameterized query를 사용하고 문자열을 직접 이어 붙이지 않는다.
- 기존 table/column이 없거나 문자열 길이·형식이 맞지 않으면 SQL Server 오류를 그대로 사용자에게 전달한다.
- transaction 중 오류가 발생하면 가능한 경우 rollback하고 `인쇄 작업은 접수되었으나 자동증가 값과 발행 이력을 저장하지 못했습니다.`를 표시한다.
- DB 저장 실패 후 물리 출력을 자동 재실행하지 않는다. 별도 DB 재시도 UI, durable recovery 상태와 관리자 commit 판정은 구현하지 않는다.
- commit 성공 후에는 자동증가 대상 `(columnId,itemId)`의 draft/snapshot만 commit 값으로 갱신한다. 화면 동기화가 실패하면 DB 성공을 되돌리지 않고 현재 session reload를 안내한다.

## 9. 품목관리 발행 체크 연동

- `ItemManage`에 `publishCheckedItemIds`와 변경 callback 또는 동등 controller API를 추가한다.
- 상위 `HomePageManager`가 현재 label-size session의 checked item ID를 소유한다.
- 우클릭 발행 체크, checkbox와 전체 해제가 모두 같은 callback을 사용한다.
- label size 변경, 품목 삭제와 session reload 때 존재하지 않는 ID를 제거한다.
- 품목관리 draft가 있으면 materialize는 화면에 표시된 현재 working value를 사용한다.
- 활성 cell edit는 command 생성 전에 확정하고 validation 오류가 있으면 발행하지 않는다.
- 출력은 품목관리 draft 전체를 DB 저장하지 않는다. 자동증가 대상만 8.1 경계에서 저장한다.

## 10. 발행 pipeline

1. 메모리 busy flag를 원자적으로 설정해 같은 앱 instance의 중복 발행을 막는다.
2. pending cell edit를 확정하고 현재 label/customer/market/user, printer, 설정, row, column/content와 saved workbook을 immutable command로 만든다.
3. 출력 행 존재, 전체 copies 1 이상, printer 선택, 저장 workbook과 숫자 설정을 검증한다.
4. row/copy 순서대로 unit과 자동증가 projection을 만든다. resolver 오류가 있으면 출력하지 않는다.
5. 공용 materializer와 print job builder로 group payload를 만든다.
6. group dispatch 직전에 cancellation을 확인하고 PDF 또는 RAW dispatcher를 순서대로 호출한다.
7. group이 failed이면 뒤 group을 중단한다. active call 완료 뒤 취소가 확인되면 뒤 group을 중단한다.
8. accepted unit이 없으면 DB를 변경하지 않는다.
9. accepted unit이 있으면 자동증가와 발행 이력을 한 SQL transaction으로 저장한다. 오류를 전달하고 가능한 경우 rollback한다.
10. commit 성공 시 대상 draft/snapshot과 선택 행 미리보기를 갱신한다.
11. 결과를 사용자에게 알리고 `finally`에서 busy flag와 UI lock을 해제한다.

출력 중 화면의 mutable 전역 데이터를 다시 읽지 않는다. 실패·취소 전에 accepted된 unit은 결과와 DB 저장 대상에 포함하고, failed 또는 보내지 않은 unit은 제외한다.

## 11. 오류와 취소

- 데이터 조회 실패: 기존 데이터를 빈 성공 결과로 바꾸지 않고 오류를 표시한다.
- workbook 없음/decode/materialize 오류: 물리 출력 전에 대상 item/copy/column을 표시한다.
- printer 미선택 또는 제거: 설정을 요청하고 출력하지 않는다.
- 첫 group 실패: `failed`, accepted unit 0으로 종료하고 DB를 변경하지 않는다.
- 앞 group accepted 후 다음 group 실패: `partiallyAccepted`로 표시하고 accepted unit만 DB에 반영한다.
- 준비 중 또는 group 사이 취소: 뒤 group을 보내지 않는다.
- active OS call 중 취소: call을 중단하지 않고 결과를 기다린 뒤 다음 group을 보내지 않는다.
- 마지막 group이 이미 accepted된 뒤 취소 요청이 확인되면 완료 결과와 함께 이미 접수됐음을 안내한다.
- 자동증가/이력 SQL 실패: 물리 출력 결과와 구분해 표시하고 가능한 rollback만 수행한다.
- 화면 동기화 실패: DB 저장 성공과 구분해 표시하고 현재 session reload를 안내한다.

## 12. 구현 대상 파일 가이드

실제 이름은 기존 구조에 맞춰 조정하며 이미 같은 책임의 파일이 있으면 새 wrapper를 늘리지 않는다.

- `lib/models/label_print.dart`: row draft, session controller, command/unit/group/result
- `lib/models/label_print_auto_increment.dart`: 자동증가 순수 projection과 update DAO
- `lib/models/label_print_history.dart`: history DTO와 DAO
- `lib/page_home/label_print.dart`: 출력 작업 table, preview와 command bar
- `lib/printing/label_output_materializer.dart`: 품목관리와 라벨출력 공용 workbook materialize
- `lib/printing/label_print_dispatcher.dart`: 공용 PDF/EZPL 분기와 typed result
- `lib/printing/label_sheet_print_job.dart`: 공용 physical page 계산과 PDF/EZPL group builder 확장
- `lib/page_home/label_print_settings_dialog.dart`: 공용 preference/printer/validation field를 사용하는 설정 UI
- `lib/home_page_manager.dart`: tab placeholder 교체, session/checked item 연결과 출력 busy 소유
- `lib/printing/label_printer_preferences.dart`: right margin과 signed push preference 추가
- `third_party/fortune_sheet/lib/`: 공용 줄간격 출력 option이 필요한 최소 painter/capture 확장

`third_party/printing`, native Windows plugin, DB schema와 installer는 이번 구현 대상이 아니다.

## 13. 테스트

### 13.1 model 테스트

- checked item add/remove/reorder와 item ID 선택 유지
- item override > preference fallback > session edit 초기화·유지
- copies 0 허용, 음수/비정수 거부와 전체 0 발행 차단
- margin/push/크기/줄간격 validation
- rightMargin/leftPush/topPush 기존 key 누락 시 `0.0` fallback과 round-trip
- exact search와 중복 결과 순환
- unit 순서, 같은 page spec 인접 group과 spec 변경 분리
- 자동증가 copy별 projection과 accepted 최대 copy index의 저장값
- cancellation request와 busy 상태

### 13.2 materialize/print 테스트

- `#ITEMNAME`, `#ELEMENT`, 일반/날짜/시간, barcode/QR/GS1와 연결 이미지 치환
- 품목관리 미리보기와 라벨출력 copy index 0 결과 동등성
- 원본 workbook 불변
- 저장 source viewport, 1:1 배치, clip, margin+push, 방향, extra area와 줄간격
- PDF page 수와 EZPL `^P1` 수가 unit 수와 일치
- 같은 page spec의 인접 unit은 한 dispatcher call, spec 변경은 순서가 유지된 별도 call
- PDF true/false/exception과 RAW 성공/exception mapping
- 첫 group 실패와 일부 group accepted 후 실패의 후속 전송 차단
- 준비 중/group 사이/active call 중 취소

### 13.3 DAO 테스트

- 기존 item/column/content 조회 DAO 재사용
- 자동증가 `(columnId,itemId)` update와 affected row 검증
- `autoIncSave`별 마지막 출력값/다음 값
- history/status/detail의 자동증가 copy/일반 row 집계
- 레거시 `|` wire와 projected/DB 원본 mapping
- customer status order의 기존 max 다음 값과 첫 값 1
- accepted unit이 없을 때 DB 작업 없음
- 일부 accepted 후 transaction 성공/rollback
- commit 후 대상 draft/snapshot만 갱신

### 13.4 widget 테스트

- `label_print` placeholder 제거와 실제 화면
- 품목관리 발행 체크와 출력 목록 동기화
- 첫 행 선택, 선택·설정 변경 미리보기
- 검색 영역과 exact-match 선택
- `[프린터 설정] 프린터명 [발행]` 순서
- 발행 중 취소 button과 중복 발행 차단
- 설정 dialog의 항목, `[취소] [적용]`, 내부 발행/매수 없음
- 900x600과 일반 desktop viewport overflow 없음
- non-Windows에서 tab/command 미노출

### 13.5 통합 검증

- `flutter analyze`
- 관련 focused test
- 전체 `flutter test`
- `git diff --check`
- 실제 Windows printer 수동 확인
  - 일반 printer/PDF
  - Godex G500 RAW
  - 한 품목 여러 장
  - 여러 품목 혼합 매수와 page spec별 group
  - 자동증가 copy별 값
  - 준비 중/group 사이/active call 중 취소
  - 첫 group 실패, 일부 group accepted 후 실패
  - 출력 접수 후 DB 저장 실패의 오류 구분과 rollback

실제 SQL Server나 printer를 사용할 수 없으면 해당 수동 항목만 `미검증`으로 남긴다.

## 14. 구현 순서

1. 품목관리 발행 체크를 controlled state로 올리고 출력 row/session model을 구현한다.
2. 품목 출력 materializer와 읽기 전용 preview를 공용 API로 추출한다.
3. right margin/signed push와 줄간격을 기존 공용 설정·print job에 최소 확장한다.
4. 라벨출력 화면, table, 검색, 선택과 자동 미리보기를 구현한다.
5. 기존 PDF/EZPL dispatcher를 공용 service로 추출하고 단일 row·copy 출력을 연결한다.
6. 여러 row/copy와 page spec group 출력을 연결한다.
7. 자동증가와 발행 이력 transaction, commit 후 draft 동기화를 구현한다.
8. focused test, analyze, 전체 test와 실제 printer 수동 검증을 수행한다.

각 단계는 관련 focused test를 통과한 뒤 다음 단계로 진행한다.

## 15. 완료 조건

- `라벨출력(F3)` 탭이 실제 화면으로 동작한다.
- 품목관리 발행 체크 행만 출력 목록에 나타나고 item ID 기준으로 동기화된다.
- 행별 매수·크기·여백·밀기·줄간격이 레거시 우선순위로 초기화되고 session에서 편집된다.
- 선택 행의 실제 출력 미리보기가 자동 표시되고 품목관리와 같은 materializer를 사용한다.
- 검색은 발행 목록의 레거시 exact 대상만 선택하며 자동 발행하지 않는다.
- 공용 printer 설정과 현재 hosted `printing`/PDF/EZPL/RAW engine을 재사용한다.
- 같은 최종 page spec의 인접 unit은 순서를 유지해 group 출력된다.
- 취소 또는 group 실패 시 active call 결과를 보존하고 뒤 group을 보내지 않는다.
- accepted unit만 자동증가와 기존 발행 이력 table에 한 transaction으로 저장된다.
- SQL 오류를 사용자에게 전달하고 가능한 경우 rollback하며 자동 재출력하지 않는다.
- DB migration, local printing fork, native plugin 개조, durable recovery 기능을 추가하지 않는다.
- Windows desktop에서만 기능을 노출한다.
- 관련 focused test, `flutter analyze`, 전체 `flutter test`, `git diff --check`가 통과한다.

## 16. 레거시 및 현재 코드 근거

레거시:

- `.tmp/LabelManager/LabelManager/LabelPrintDlg.cpp`
- `.tmp/LabelManager/LabelManager/LabelPrintModel.cpp`
- `.tmp/LabelManager/LabelManager/SearchPrintDlg.cpp`
- `.tmp/LabelManager/LabelManagerLib/PrintLog.cpp`
- `.tmp/LabelManager/LabelManagerLib/StatusPrint.cpp`

현재 Flutter:

- `lib/home_page_manager.dart`
- `lib/page_home/item_manage.dart`
- `lib/page_home/item_code_data_resolver.dart`
- `lib/page_label_sheet/label_sheet_workbench.dart`
- `lib/printing/label_printer_preferences.dart`
- `lib/printing/label_sheet_print_job.dart`
- `lib/printing/printer_profiles.dart`
- `lib/printing/raw_printer_win32.dart`
